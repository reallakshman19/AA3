# EP-LC1321-COG-0004 — Isolate selector policy from kernel workflow prerequisites

CHAIN_ID: LOAD-CALC-1321-COMPONENT-COG-FALLBACK
LEG_ID: LEG-A
ENDPOINT_ID: EP-LC1321-COG-0004
PREVIOUS_ENDPOINT: EP-LC1321-COG-0003

CREATED_AT: 2026-08-28T12:24:00+05:30
ENDPOINT_REASON: BLOCKER_CHANGE

TASK / ISSUE: Advanced_Analysis #1321 — configurable component CoG/application-point fallback
PR: #1508
BRANCH: agent/issue-1321-component-cog-fallback-policy

CHECKPOINT_HEAD: cc4ab412c93c945f4734120ab7c68d8b903d6aad
MAIN_HEAD_OBSERVED: 777049b8ef6b1355685d7121e602c10e0f6e1dbc
MERGE_BASE: 777049b8ef6b1355685d7121e602c10e0f6e1dbc

STATE: ACTIVE
EXECUTION_MODE: AUTO
AUTO_STATE: RUNNING
SCOPE_AUTHORITY: LOCKED_TO_APPROVED_MISSION
MERGE_AUTHORITY: OWNER_ONLY_NOT_GRANTED
CRITICALITY: ENGINEERING_CRITICAL_POLICY / NUMERICAL_KERNEL_PROTECTED
COORDINATION: SAFE_ENGINEERING

### Mission

Correct one source-inspection coupling defect before executable qualification: keep component-CoG fallback as method-selection/policy authority without making a selector-only policy a prerequisite of lower-level support-load kernel workflows.

### This leg completed

- Exact PR patch was audited after the DNS execution blocker.
- `PROJECT_DATA_REQUIREMENTS.loadCalcProjectBasis` was traced to `support-load-distribution-v3.js`, where the qualified-case-mass low-level execution seam validates that workflow before numerical calculation.
- `PROJECT_DATA_REQUIREMENTS.authorizedGravityLoads` was confirmed to be another load execution requirement set.
- `PROJECT_DATA_REQUIREMENTS.nonFeaPolicy` is the appropriate policy-readiness inventory for the new fallback setting.
- Therefore the current source cut is over-broad by two requirement entries even though the selector logic itself remains bounded.

### Currently in progress

A two-line authority-boundary correction plus focused regression assertions.

### Remaining work

- Remove `loadCalculation.componentCogFallback` from `loadCalcProjectBasis`.
- Remove it from `authorizedGravityLoads`.
- Keep it in `nonFeaPolicy`.
- Restore the unrelated benchmark-array formatting drift in the same file.
- Add focused assertions proving the field is policy-required but not a kernel/load-workflow prerequisite.
- Re-run exact-head validation if infrastructure becomes available; otherwise preserve NOT_RUN.

### Exact next action

Edit only `src/workspace/project-data/project-data-fields.js` and `scripts/non-fea-component-cog-fallback-policy-check.mjs` to enforce the selector-vs-kernel requirement boundary described above; do not modify `support-load-distribution-v3.js` or any numerical/statics module.

### Known / proven

- `support-load-distribution-v3.js` explicitly invokes `validateProjectDataProfile(..., configuration.profileWorkflow || 'loads', ...)` and the qualified-case-mass seams set `profileWorkflow: 'loadCalcProjectBasis'`.
- The fallback policy is consumed by empirical gravity method selection before support-load execution, not by the force/lever/reaction equations.
- Product defaults are materialized for normal Project Data/Common Input flows, so visibility/default provenance does not require kernel workflows to re-demand the policy.
- `nonFeaPolicy` is the correct policy-level requirement set to retain the new field.

### Not proven

- No executable regression has run.
- No runtime failure from the over-broad requirement has been observed because checkout remains unavailable.
- The proposed correction remains source-inspection reasoning until tests execute.

### NOT_RUN

All commands listed in EP-LC1321-COG-0003 remain NOT_RUN. DNS failure remains the latest execution observation.

### Active hypothesis

Removing the fallback field from only `loadCalcProjectBasis` and `authorizedGravityLoads` eliminates unintended selector-to-kernel coupling while retaining full UI visibility, Product/Project provenance, Common policy readiness, and governed method-selection behavior.

### Falsifier

The hypothesis is false if source trace shows either lower-level load workflow directly consumes `componentCogFallback`, if removing the requirement makes the governed selector unable to receive an effective policy in normal Common Input, or if focused/aggregate execution later proves a legitimate policy-readiness path depended on those two requirement memberships.

### Protected invariants

- No change to selector behavior introduced by EP-LC1321-COG-0002.
- No change to Product default version/value/provenance.
- No change to Basic UI exposure.
- No change to support-load numerical/statics files.
- No change to route/source-axis/support capability mechanics.
- No tolerance/benchmark/oracle changes.
- `NOT_RUN` remains visible.

### Do not redo

- Do not move the policy decision into the support-load kernel.
- Do not introduce a second workflow or resolver solely for this correction.
- Do not rewrite earlier endpoints.

### Do not change

- `src/workspace/engineering-loads/support-load-distribution-v3.js`.
- `src/workspace/engineering-loads/support-load-static-accounting.js`.
- EMP.1/WRC/LAFEA/workflow/release authority.

### Expected next-leg files / domains

Exactly two material files: `project-data-fields.js` and the focused CoG policy regression. Relay endpoint/index files may also change.

### Inputs

- PR #1508 exact patch at `cc4ab412c93c945f4734120ab7c68d8b903d6aad`.
- `src/workspace/project-data/project-data-fields.js`.
- `src/workspace/engineering-loads/support-load-distribution-v3.js` read-only trace.
- `src/workspace/project-data/non-fea-project-data-view-v2.js` read-only Project-basis/UI trace.

### Benchmarks

- Focused CoG policy regression — IMPLEMENTATION_COUPLED, NOT_RUN.
- No independent numerical benchmark required for this requirement-membership correction; numerical equations remain untouched.

### Common / governing documents

- Common `engineering-pr-delivery-v2@10d667ce715bb52e1f73035c6fa326db77d0f9dd`.
- Advanced_Analysis `AGENTS.md` at `main@777049b8ef6b1355685d7121e602c10e0f6e1dbc`.

### Authoritative sources

- Issue #1321 for configurable fallback policy.
- Existing code ownership: selector consumes fallback authority; support-load kernel owns numerical execution and must not gain an unrelated selector prerequisite.

### Production paths

- `src/workspace/project-data/project-data-fields.js` — correction target.
- `src/workspace/engineering-loads/empirical-gravity-method-selection.js` — protected/current consumer.
- `src/workspace/engineering-loads/support-load-distribution-v3.js` — read-only/protected caller proving coupling risk.

### Validation / test paths

- `scripts/non-fea-component-cog-fallback-policy-check.mjs` — add requirement-boundary assertions.
- All EP-LC1321-COG-0003 executable checks remain required afterward.

### Changed during this leg

Relay endpoint only before the isolated correction.

### Validation summary

- Coupling defect: PASS / SOURCE_INSPECTION / first boundary identified.
- Numerical defect: NOT_PROVEN.
- Exact-head execution: NOT_RUN due DNS before checkout.

### Open risks / questions

- Aggregate execution may reveal another legitimate workflow requirement not visible by source search.
- Main may move during correction; same-domain drift must be reconciled before validation/merge.

### Next-agent qualification

QUALIFICATION_BASIS_HEAD: cc4ab412c93c945f4734120ab7c68d8b903d6aad
QUESTION_SET_ID: QS-LC1321-COG-0004
QUESTION_SET_STATUS: CURRENT

#### Q1 — Production Trace
Trace the `loadCalcProjectBasis` requirement set into the qualified-case-mass support-load kernel and separately trace `componentCogFallback` into the empirical method selector. Identify the first boundary proving the policy is not a numerical-kernel input.

#### Q2 — Current Unresolved Problem / Failure Isolation
Explain the two-entry over-broad requirement defect, the minimum two-file correction, predicted behavior, and the source/executable observation that would falsify the correction.

#### Q3 — Authority / Invariant
Why must `componentCogFallback` remain in `nonFeaPolicy` but not become a prerequisite of `loadCalcProjectBasis` or `authorizedGravityLoads`? Name the protected numerical files and one invalid shortcut.

#### Q4 — Independent Validation
Classify the new focused assertions as implementation-coupled evidence and state when an independent numerical free-body check would become necessary despite this policy-only correction.

#### Q5 — Next Contribution / Minimal Patch
Name the exact lines/domains to change in `project-data-fields.js` and the exact assertions to add to the focused check, plus the stop condition preventing any edit to `support-load-distribution-v3.js`.
