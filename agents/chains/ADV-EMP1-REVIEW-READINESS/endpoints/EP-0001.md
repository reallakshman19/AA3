# EP-0001 — Review → Readiness Integration Prework

STATUS: ACCEPTED_PREWORK
DATE_UTC: 2026-09-01

## Custody

COMMON_PROTOCOL: engineering-pr-delivery-v2
COMMON_PROTOCOL_BASIS: 293a3db7993a6945c01adc592a7ff14a339c504a
WORK_ITEM_SOURCE: OWNER_DIRECT
WORK_ITEM_KEY: owner-direct:advanced-analysis:emp1-review-readiness-integration-v1
WORK_ITEM_MODE: EXCLUSIVE
MAIN: 93d208dc1298570f20ce170edbd4f881039d22f0
STACK_BASE_PR: 1620
STACK_BASE_BRANCH: agent/emp1-engineering-review-v1
STACK_BASE_HEAD: a9a43258cb499fcf838341f183fd1d76c4894ced
BRANCH: agent/emp1-review-readiness-integration-v1

## Owner-directed mission

Continue the EMP.1 governance sequence by composing the existing engineering-review state into the existing readiness projection.

This is intentionally stacked on Draft PR #1620 because `emp1-engineering-review-state/v1` is not on main yet.

## Existing authority owners retained

- `src/core/emp1/emp1-engineering-review-record.js` owns review-record creation, evidence binding, deterministic review identity, and current/stale classification.
- `src/core/emp1/emp1-readiness-projection.js` owns the seven-dimension governance/readiness composition.
- `runEmp1()` remains A → B → C execution owner.
- currentness/C-state owners remain unchanged.
- release qualification and code compliance remain separate existing authorities.

The readiness layer may consume the review-state conclusion but must not re-read source/A/B/C hashes, recalculate review staleness, or create a second review-authority path.

## Intended contract

Keep the existing call valid:

```js
projectEmp1Readiness(productProjection)
```

Add optional composition:

```js
projectEmp1Readiness(productProjection, { reviewState })
```

Accepted review states are the exact existing schema states:

```text
NOT_REVIEWED
REVIEW_ACCEPTED
REVIEW_REJECTED
REVIEW_STALE
```

For a current calculation, readiness overall may advance to:

```text
READY_FOR_ENGINEERING_REVIEW
REVIEW_ACCEPTED
REVIEW_REJECTED
REVIEW_STALE
```

Source/method/calculation blockers and stale calculation states retain precedence over review status.

## Authority boundary

The readiness integration:

- does not create or hash a review record;
- does not compare evidence hashes to detect staleness;
- does not execute EMP.1 or any WRC method;
- does not modify applicability/method/source authority;
- does not produce code compliance;
- does not produce release qualification;
- does not claim cryptographic signature or professional seal authority.

## Compatibility

With no `reviewState`, the existing readiness/dashboard behavior remains:

```text
review.state = NOT_REVIEWED
current calculation overall = READY_FOR_ENGINEERING_REVIEW
```

The existing professional workflow view can therefore remain unchanged in this slice. Supplying review state from controller/storage into the UI is a separate downstream activity.

## Validation truth

Executable validation: NOT_RUN.

Focused checker to add:

```bash
node scripts/emp1-review-readiness-integration-check.mjs
```

It must cover all four review states, malformed review-state rejection, review precedence under stale/blocked calculation, default compatibility, and authority-boundary preservation.

## Exact next action

Modify only `src/core/emp1/emp1-readiness-projection.js`, add the focused checker, inspect the stacked diff relative to PR #1620 head, record material custody, and open a Draft PR against `agent/emp1-engineering-review-v1`. Do not merge.
