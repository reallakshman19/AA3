# EP-0002 — V3 migration and owner-authorized merge gate

CHAIN_STATE_VERSION: 3
COMMON_PROTOCOL: engineering-pr-delivery-v2
COMMON_PROTOCOL_BASIS: 9573ec2bba234cc7cd6abcf73c4c3f3e2bc0892c
COMMON_PROTOCOL_STATUS: CURRENT
CHAIN_ID: LOAD-CALC-1321-RESULT-EXCEPTION-PRESENTATION
LEG_ID: LEG-A-MIGRATION
ENDPOINT_ID: EP-0002
PREVIOUS_ENDPOINT: LEGACY_PROVENANCE agents/agentchain/LOAD-CALC-1321-RESULT-EXCEPTION-PRESENTATION/EP-LC1321-RESPRES-0001.md
CUSTODY_EPOCH: 2
CREATED_AT: 2026-08-29T09:44:05+05:30
ENDPOINT_REASON: V3_PROTOCOL_MIGRATION_BEFORE_OWNER_AUTHORIZED_MERGE

TASK / ISSUE: Advanced_Analysis #1321
PR: #1523
BRANCH: agent/issue-1321-result-exception-presentation-custody
CHECKPOINT_HEAD: 2a71b652b6e2b805c161a8ae1565558bd9d44ffc
MAIN_HEAD_OBSERVED: ac9b2e2aee61e020620ae694fa36962c3632a8ca
MERGE_BASE_AFTER_RECONCILIATION: ac9b2e2aee61e020620ae694fa36962c3632a8ca
STATE: READY_FOR_OWNER_AUTHORIZED_MERGE
MERGE_AUTHORITY: OWNER_GRANTED_FOR_PR_1523
EXECUTABLE_VALIDATION: NOT_RUN

## Mission

Preserve current-system overall exception-result status in the Load Calc completion presentation and hand the next bounded presentation defect to canonical V3 custody. No numerical, statics, allocation, equilibrium, source/default, result-contract, release, trust or workflow authority is changed.

## Protocol/drift reconciliation

PR #1523 was authored before `Advanced_Analysis/AGENTS.md` adopted the canonical V3 chain store. Live Common was re-grounded at `9573ec2bba234cc7cd6abcf73c4c3f3e2bc0892c`; the project overlay at `main@ac9b2e2aee61e020620ae694fa36962c3632a8ca` declares `LEGACY_RELAY_WRITES: FORBIDDEN`.

Drift from the PR's prior base to current main does not touch the three Load Calc engineering/test paths. The only obsolete overlap is the legacy shared `agents/agentchain.md` mutation. Reconciliation therefore:

- retains the exact three engineering/test blobs from PR head `2a71b652b6e2b805c161a8ae1565558bd9d44ffc`;
- retains pre-V3 PR1523 workreport/claims/status and legacy endpoint strictly as historical provenance;
- inherits current main's `agents/agentchain.md` unchanged, so the obsolete PR delta disappears from the effective merge;
- establishes this chain's active authority only under `agents/chains/.../ACTIVE.md` and immutable V3 endpoints.

## Completed engineering patch

- `src/workspace/load-calc-result-presentation.js`: presentation classifier prefers an existing current-system `execution.resultStatus` over its narrower distribution status when supplied.
- `src/workspace/load-calc-current-system-view.js`: only the ordinary raw completion message is replaced when the current-system overall result is stricter; unrelated messages are preserved.
- `scripts/load-calc-current-system-result-presentation-custody-check.mjs`: focused discriminator for `distribution.status=CALCULATED` versus `execution.resultStatus=CALCULATED_WITH_EXCEPTIONS`.

## Validation truth

Executable repository checkout/Actions remained unavailable before executable steps. Therefore:

- focused result-presentation custody check — NOT_RUN;
- current-system routing/runtime regressions — NOT_RUN;
- aggregate non-FEA/import/advanced-shell/build/diff checks — NOT_RUN.

Source inspection and GitHub mergeability are not executable PASS.

## Proven next unresolved boundary

Current `src/workspace/engineering-loads/support-load-distribution-v3.js` deliberately publishes support reactions for both `CALCULATED` and `CALCULATED_WITH_EXCEPTIONS`. Current `src/workspace/sequential-sketcher/support-load-presenter.js` accepts an empirical OPE result only when `loadCase.status === 'CALCULATED'`. A valid reaction attached to a `CALCULATED_WITH_EXCEPTIONS` case can therefore disappear from SVG callouts, the property inspector and table summary even though the calculation contract marks it publishable.

## Exact next action

For PR #1523: re-ground live main/head/files/reviews/checks; if this V3 reconciliation remains a non-overlapping presentation-only overlay, transition the draft to review-ready and squash-merge using the exact expected head under the Owner's existing authorization. Preserve executable validation as NOT_RUN.

After merge: begin a new bounded material leg from exact post-merge main. Before coding, create its V3 pre-work endpoint, then repair only `SupportLoadPresenter` exception-status custody with a focused falsifier. Do not alter support-load statics or status production.

## Protected invariants

- `CALCULATED_WITH_EXCEPTIONS` may remain publishable only because the existing support-load contract already says so.
- No reaction magnitude, sign, allocation, equilibrium or moment demand may be recomputed in the presenter.
- No result status may be promoted/demoted by presentation code.
- No benchmark/oracle/tolerance, Product/Project/source/default authority, workflow, release/trust/publication, EMP.1/WRC/LAFEA or PR #1519 scope may change.
- Owner authorization applies to PR #1523 only; it does not authorize the successor merge.

## Inputs

- Advanced_Analysis Issue #1321.
- current project overlay `AGENTS.md` at `ac9b2e2aee61e020620ae694fa36962c3632a8ca`.
- PR #1523 exact head `2a71b652b6e2b805c161a8ae1565558bd9d44ffc`.
- `src/workspace/engineering-loads/support-load-distribution-v3.js`.
- `src/workspace/sequential-sketcher/support-load-presenter.js`.

## Benchmarks / discriminators

Current-system banner discriminator for this PR:

```text
distribution.status = CALCULATED
execution.resultStatus = CALCULATED_WITH_EXCEPTIONS
=> exception presentation wins
=> Loads remains open
=> engineering result unchanged
```

Next-leg presenter discriminator:

```text
OPE loadCase.status = CALCULATED_WITH_EXCEPTIONS
verticalForceN = finite
support-load contract publishable = true
=> SVG / inspector / table must retain that exact reaction
=> exception limitation must be visible
```

## Common / governing documents

- live Common `engineering-pr-delivery-v2` at `9573ec2bba234cc7cd6abcf73c4c3f3e2bc0892c`;
- `skills/engineering-pr-delivery-v2/references/repository-agent-policy.md`;
- `skills/engineering-pr-delivery-v2/references/qualification.md` and relay/chain/AUTO references;
- `Advanced_Analysis/AGENTS.md`.

## Authoritative sources

Existing repository support-load result/status contracts are the only authority for this presentation work. No new external numerical authority is introduced.

## Production paths

Current PR changed:
- `src/workspace/load-calc-result-presentation.js`
- `src/workspace/load-calc-current-system-view.js`

Next-leg read/write candidate:
- `src/workspace/sequential-sketcher/support-load-presenter.js`

Read-only numerical/status authority:
- `src/workspace/engineering-loads/support-load-distribution-v3.js`
- current empirical execution/store/decorate paths.

## Validation / test paths

Current PR:
- `scripts/load-calc-current-system-result-presentation-custody-check.mjs`
- documented current-system/aggregate checks when execution becomes available.

Next leg:
- focused presenter-custody regression to be identified/added from exact post-merge main before material coding.

## Incoming qualification — exactly Q1–Q5

### Q1 — Production trace
Trace one OPE support reaction from `caseStatus()` and `supportResults()` in `support-load-distribution-v3.js`, through engineering-store entity decoration, into `qualifiedEmpiricalResult()` in `support-load-presenter.js`, then through `getResultCallouts()`, `formatLoadInspectorProperties()` and `getTableSummary()`. Identify the exact equality test that discards a contract-publishable `CALCULATED_WITH_EXCEPTIONS` reaction.

### Q2 — Exact presentation reconstruction
For an existing publishable OPE row with `status=CALCULATED_WITH_EXCEPTIONS` and `verticalForceN=12345.6`, calculate the exact underlying `forceN`, SVG kN display, inspector N/kN display and table kN display produced by the current presenter's formatting rules. State which values must remain byte/number-equivalent after the patch and what exception-status evidence must be added without changing the reaction.

### Q3 — Authority / invariant
Explain why changing the presenter to admit the already-publishable `CALCULATED_WITH_EXCEPTIONS` status is a custody fix, while changing `caseStatus()`, `supportResults()` publishability, allocation, equilibrium, reaction magnitude, result schema or semantic hash would exceed this leg's authority. Name the exact no-patch boundary.

### Q4 — Independent falsifier
Construct a second exact fixture with a finite empirical reaction under `CALCULATED_WITH_EXCEPTIONS` and an exception ledger, plus a blocked/failed or non-finite control. Show the expected callout/inspector/table outcomes and prove that the patch preserves fail-closed behavior for non-publishable/non-finite results while exposing the valid exception-qualified reaction and limitation.

### Q5 — Minimal next contribution
From exact post-#1523 main, identify the smallest production/test file set that can repair all three presenter surfaces without touching mechanics. State the first focused command to run, the protected unchanged domains, and the rollback/falsifier condition that requires stopping rather than broadening into support-load numerics.
