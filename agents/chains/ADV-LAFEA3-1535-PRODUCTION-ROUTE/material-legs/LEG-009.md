# LEG-009 — bounded BM-005 hosted execution runner

CHAIN_ID: ADV-LAFEA3-1535-PRODUCTION-ROUTE
LEG_ID: LEG-009-BM005-HOSTED-RUNNER
PREWORK_ENDPOINT: EP-0028
WORK_ITEM_KEY: github:reallaksh19/Advanced_Analysis#1569
PARENT_WORK_ITEM: github:reallaksh19/Advanced_Analysis#1535
OWNER_PROGRESSION_COMMAND: PROCEED_NEXT
BASE_HEAD: fad372eaf55487b10a2cbcad5ff8438b71e031ff
COMMON_PROTOCOL_BASIS: 9573ec2bba234cc7cd6abcf73c4c3f3e2bc0892c

## Material change

Added exactly one execution-infrastructure file:

- `.github/workflows/lafea3-bm005-qualification.yml`

The workflow is intentionally bounded to BM-005 evidence production. It:

1. uses `pull_request` and manual `workflow_dispatch` triggers;
2. checks out `github.event.pull_request.head.sha || github.sha` with full history;
3. asserts `git rev-parse HEAD == EXPECTED_HEAD`, clean worktree and `git diff --check`;
4. uses Node 22;
5. syntax-checks only the two BM-005 scripts;
6. executes `node scripts/lafea.3-bm005-ordinary-route-check.mjs` without modifying repository files;
7. captures stdout, stderr and exit code under `/tmp`;
8. uploads those receipts even when the harness fails;
9. propagates the harness exit code so CI green cannot mask BM-005 failure.

Permissions are `contents: read`. There is no package installation, source mutation, commit, push, release action, secret use or authority mutation.

## Protected domains unchanged

No changes to element formulation, stiffness/load assembly, solver, recovery, mesher algorithms/quality thresholds, pressure/load semantics, physical-probe mathematics, convergence mathematics/policy, frozen oracle/mesh/probe/tolerances, roadmap, UI/browser logic, code assessment, report semantics or release authority.

## Validation truth before PR-trigger execution

PASS_STATIC_SOURCE:
- workflow scope is one YAML file plus same-chain relay;
- exact-head checkout/clean-tree guard is explicit;
- BM-005 stdout/stderr/exit receipts are outside the worktree;
- workflow cannot manufacture benchmark PASS: the harness owns PASS/FAIL and the final step propagates its exit code;
- `releaseQualified` is not mutated by the workflow.

NOT_RUN:
- the new GitHub Actions workflow itself;
- BM-005 harness under hosted checkout;
- persisted exact-head BM-005 report;
- BM-006 browser replay.

Existing runner-health evidence immediately before this leg: exact-main Pages run `33305892856`; rerun build job `99244574139` completed failure with no steps and no log blob. If this new workflow shows the same pre-step signature, classify `BM-005 NOT_RUN_EXECUTION_BLOCKED`, not BM-005 FAIL.

## Exact next action

Open Draft PR for the runner branch and inspect its LAFEA.3 BM-005 workflow run. If the job reaches checkout and the harness, classify the actual BM-005 result from retained stdout/exit artifact. If the job terminates before step 1, do not patch engineering code; record hosted-runner evidence and return to BLOCKED/READ_ONLY. If BM-005 emits PASS, persist the exact report in a later evidence-only leg and proceed to BM-006.

AGENT_LEG_COMPLETE: FALSE
PR_COMPLETE: FALSE
CHAIN_COMPLETE: FALSE
