# LEG-016 — M4 source-cited physics/response/recovery fixture freeze

STATUS: ACCEPTED_MATERIAL
DATE_UTC: 2026-09-07
PREWORK_ENDPOINT: EP-0028
MATERIAL_BASELINE: b940563f6e6b1e0d755ac9c7994a2e9983a34561
MATERIAL_HEAD: b0943ae5dc605af745d60eea0cd955e569c1cae8

CHAIN_ID: ADV-BM-MESH-1652
WORK_ITEM_KEY: github:reallaksh19/Advanced_Analysis#1652
REPO: reallaksh19/Advanced_Analysis
BRANCH: engineering/bm-mesh-1652-m2-data
PR: 1663

## Material diff

Exactly three material files changed across three commits:

1. `validation/lafea-benchmark-data/MESH/convergence/m4-physics-response.json` — added;
2. `validation/lafea-benchmark-data/MESH/sources/source-registry.json` — source custody extended through S-030;
3. `validation/lafea-benchmark-data/MESH/bucket-manifest.json` — M4 definition state advanced from missing physics to frozen fixture / execution protected.

No runner, `src/**`, solver, compiler, benchmark-program, workflow or roadmap file changed.

## Frozen LAFEA.3 M4 fixture

The continuum fixture reuses the independently frozen B01 plane-stress affine oracle:

- `E = 200000 MPa`;
- `nu = 0.3`;
- thickness `10 mm`;
- `epsilon_x = 0.00005`;
- `epsilon_y = -0.000015`;
- `gamma_xy = 0`;
- physical-coordinate displacement field `u_x = 0.00005*x`, `u_y = -0.000015*y`.

The field is projected as prescribed displacement on frozen geometry boundary nodes selected by physical geometry membership, never node IDs. Interior nodes remain solver unknowns.

LAFEA.3 selected displacement uses the existing `lafea-continuum-physical-probe/v1` `DISPLACEMENT_X` / `ELEMENT_SHAPE_INTERPOLATION` contract at the already-frozen physical probes. Strain energy is read from the accepted load-case result.

Analytical reference energies and probe values are retained from the closed-form field for diagnostics but do not create a separate M4 acceptance threshold.

## Frozen LAFEA.4 M4 fixture

The shell fixture reuses existing `prescribedPatchSource` defaults:

- formulation `CST_DKT_TRI3_THIN_SHELL_V1`;
- `E = 200000 MPa`;
- `nu = 0.3`;
- thickness `2 mm`;
- membrane field `epsilon_x = 0.001`, `epsilon_y = 0`, `gamma_xy = 0`, zero curvature;
- `UX = 0.001*x`, with `UY=UZ=R1=R2=0`.

The field is prescribed only on the external boundary of the frozen conforming two-patch mesh. Interior seam nodes are not boundary-constrained.

Shell selected displacement is limited to unique-owner CST triangle barycentric interpolation of global nodal UX at the already-frozen physical coordinates. Cross-element averaging, stress recovery and moving maxima are forbidden for this response.

## Convergence authority

Only existing production convergence quantities are frozen for M4:

- `STRAIN_ENERGY` — default relative-change limit 2%;
- `SELECTED_DISPLACEMENT` — default relative-change limit 1%.

At least three levels are required, finest last. `MONOTONIC` behavior is required through the existing `qualifyConvergenceSet`. No limit overrides are authorized. Raw singular peak stress remains forbidden.

## Source custody additions

- S-024 — B01 closed-form affine physics oracle;
- S-025 — B01 affine physics qualification witness;
- S-026 — canonical LAFEA.3 continuum solver contract;
- S-027 — LAFEA.4 production-route physics qualification witness;
- S-028 — canonical LAFEA.4 shell solver contract;
- S-029 — CST membrane kinematics contract;
- S-030 — frozen BM-MESH M4 physics/response fixture.

## Authority boundary

LEG-016 does not execute a solver, compiler, convergence set or benchmark stage and does not claim M4 PASS. It does not mutate the staged runner. Production source, thresholds, convergence limits, release authority, temperature authority, PR rebase and merge remain unchanged/protected.
