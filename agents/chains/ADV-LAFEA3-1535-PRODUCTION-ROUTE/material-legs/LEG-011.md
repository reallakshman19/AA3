# LEG-011 — exact-main BM-005 push trigger

CHAIN_ID: ADV-LAFEA3-1535-PRODUCTION-ROUTE
LEG_ID: LEG-011-BM005-EXACT-MAIN-PUSH
PREWORK_ENDPOINT: EP-0032
WORK_ITEM_KEY: github:reallaksh19/Advanced_Analysis#1535
SUBORDINATE_VV_WORK_ITEM: github:reallaksh19/Advanced_Analysis#1569
AGENT_INSTANCE_ID: chatgpt:bed54f20-6ae2-4756-ac8d-388d7a1f1e8f
OWNER_PROGRESSION_COMMAND: PROCEED_NEXT
BASE_HEAD: 22cd57a4eff48a505bab4a081debf2ff11748cdb
COMMON_PROTOCOL_BASIS: 293a3db7993a6945c01adc592a7ff14a339c504a

## Material change

Changed exactly one execution-transport file:

- `.github/workflows/lafea3-bm005-qualification.yml`

Added:

```yaml
push:
  branches: [main]
  paths:
    - scripts/lafea.3-bm005-ordinary-route-check.mjs
    - scripts/lafea.3-bm005-report-contract.mjs
    - validation/lafea-benchmark-data/BM005/**
    - .github/workflows/lafea3-bm005-qualification.yml
```

Existing `workflow_dispatch`, `pull_request`, `contents: read`, exact-head checkout, clean-tree guard, Node syntax checks, harness execution, `/tmp` evidence capture, artifact upload and exit propagation are unchanged.

## Why this is required

The connected GitHub interface exposes no workflow-dispatch mutation. A PR run proves only the PR head. The `push` trigger lets the eventual Owner-authorized merge commit on `main` self-trigger BM-005 with `EXPECTED_HEAD=github.sha`, producing evidence for the exact integrated candidate rather than a predecessor head.

## Protected boundaries unchanged

No changes to solver/formulation/stiffness/load assembly/recovery; mesher/quality thresholds; load/pressure semantics; fixed-probe/convergence mathematics or policy; frozen benchmark/oracle/mesh/probe/tolerances; report semantics; roadmap; UI/browser; code assessment; release authority.

## Validation truth

PASS_STATIC_SOURCE:
- push event is restricted to branch `main`;
- path filter is identical to the existing BM-005 material trigger set;
- push event resolves `EXPECTED_HEAD` and checkout ref through existing `${{ github.event.pull_request.head.sha || github.sha }}`, therefore to `github.sha` on push;
- no repository write permission or write-back step exists.

NOT_RUN:
- new PR-triggered workflow run for this branch;
- exact-main push execution (requires later merge);
- BM-005 harness;
- persisted BM-005 report;
- BM-006 browser replay.

BM-005 remains `NOT_RUN_EXECUTION_BLOCKED`; BM-006 `NOT_RUN`; core FEA `NOT_PROVEN`; release `FALSE` until executable evidence exists.

AGENT_LEG_COMPLETE: TRUE
PR_COMPLETE: FALSE
CHAIN_COMPLETE: FALSE
