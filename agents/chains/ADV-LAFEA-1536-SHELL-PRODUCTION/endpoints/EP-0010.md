# EP-0010 — MITC recovery implemented; shared thin/thick qualification pre-work

CHAIN_STATE_VERSION: 3
CHAIN_ID: ADV-LAFEA-1536-SHELL-PRODUCTION
ENDPOINT_ID: EP-0010
PARENT_ENDPOINT: EP-0009
CUSTODY_EPOCH: 10
MATERIAL_BATCH_HEAD: a675457755e92e4ba6b10f8e1475f6169f3dd450
COMMON_PROTOCOL: engineering-pr-delivery-v2
COMMON_PROTOCOL_BASIS: 9573ec2bba234cc7cd6abcf73c4c3f3e2bc0892c
COMMON_PROTOCOL_STATUS: CURRENT
ROADMAPS: docs/conceptcumroadmapLAFEA.md@088f4cebfd954e5d1e37da855c95142712463a31
ROADMAP_REVIEW_STATUS: COMPLETE
HANDOVER_READY: TRUE

## Material result

The fourth experimental MITC adoption batch adds formulation-aware recovery from retained solved displacement and element evidence without publishing a production shell result contract.

`src/core/local-shell/mitc-adoption-recovery.js` now:

- extracts solved global generalized displacement by each element's retained `globalDofOrdering`;
- applies the retained MITC beta-slope transformation before evaluating formulation B operators;
- recovers membrane strain, curvature and tied transverse-shear strain at MITC integration points;
- recovers `BOTTOM/MIDSURFACE/TOP` in-plane strain/stress through `epsilon(z)=epsilon_m+z*kappa` using the existing plane-stress material matrix;
- reuses the existing same-point plane-stress invariant owner and explicitly labels that von Mises authority as in-plane only;
- retains transverse shear separately as `gamma_xz/gamma_yz`, effective average shear stress `kG*gamma`, and shear resultant `kGt*gamma`;
- explicitly records `transverseShearIncludedInInPlaneVonMises: false`;
- independently integrates membrane, bending and shear strain energy and compares their sum against `0.5*q_local^T*K_local*q_local`, failing closed on mismatch;
- retains `EXPERIMENTAL_NONPRODUCTION` and production-qualification `false`.

Focused `scripts/lafea.4-mitc-adoption-recovery-check.mjs` contains independent controls:

- affine MITC4 membrane field -> exact constant strain, closed-form plane-stress stress/von-Mises and membrane energy;
- prescribed global rigid motion -> zero membrane/bending/shear strains and zero energy, directly falsifying the previously corrected beta/physical-rotation mapping;
- pressure-loaded cantilever -> non-zero transverse-shear resultants and independent membrane+bending+shear energy reconstruction;
- explicit MITC3 affine membrane field -> constant TRI3 recovery with zero spurious shear.

## Validation truth

Executable Node validation remains `NOT_RUN` in this environment. The focused checks are committed but not executed here. Source inspection and independent analytical derivations support the expected values but are not software PASS. Parent #1541 exact-head runner remains the prerequisite for final numerical qualification.

No production result schema, stage registry, stage composition, presenter, UI, owner roadmap or workflow file changed.

## Next bounded material batch — cross-formulation qualification

Build shared physical benchmarks that run the legacy production CST/DKT route and the experimental MITC route on comparable geometries. This batch is evidence only; it must not alter either formulation to force agreement.

Required benchmark families:

1. **Thin cantilever limit** — as `t/L -> small`, both routes converge toward Euler-Bernoulli/Kirchhoff bending response under mesh refinement. Compare each independently against the analytical beam/strip oracle; do not require element-by-element equality.
2. **Moderate-thickness cantilever** — use Timoshenko/Reissner-Mindlin theory. MITC must recover the added physical shear compliance while CST/DKT remains Kirchhoff-only. Require the expected ordering `delta_MITC > delta_DKT` and compare both to their applicable analytical limits.
3. **Pure membrane shared case** — both routes recover the same affine membrane stress state independent of transverse-shear formulation.
4. **Pressure/equilibrium shared case** — both routes preserve total force/moment and reaction equilibrium using their formulation-consistent nodal load integrations.
5. **Negative/falsifier controls** — do not loosen tolerances or modify formulations when comparison does not converge; classify geometry/discretization/model-form differences explicitly.

### Handover snapshot

Repo: `reallaksh19/Advanced_Analysis`
Task: #1536 — MITC adoption qualification
Chain / endpoint: `ADV-LAFEA-1536-SHELL-PRODUCTION` / `EP-0010`
PR: #1542 Draft stacked on #1541
Branch / material head: `codex/lafea-1536-mitc-adoption` / `a6754577…`
Merge authority: OWNER_ONLY
State: IN_PROGRESS / HELD / NOT_REQUIRED / WRITE_ALLOWED / AUTO RUNNING
Roadmap: `docs/conceptcumroadmapLAFEA.md@088f4ceb…`
Inputs: legacy `calculateLocalShell()` + experimental MITC solve/recovery
Benchmarks: analytical Euler-Bernoulli/Timoshenko + affine membrane + equilibrium
Blocker: executable qualification NOT_RUN
Exact next action: add shared physical benchmark evidence only; no contract/registry/UI promotion.

Q1: Trace comparable CST/DKT and MITC cantilever models from source geometry/DOFs through each actual solve/recovery route and identify all non-equivalent discretization assumptions.
Q2: Derive Euler-Bernoulli and Timoshenko tip-deflection values for the selected strip, including the exact shear contribution and expected thin/moderate-thickness ratios.
Q3: State the authority invariant: thin-limit convergence is a physics comparison, not permission to tune MITC or DKT until they numerically match.
Q4: Independently reconstruct affine membrane stress and pressure force/moment equilibrium for both routes; identify decisive falsifiers.
Q5: Define the minimal benchmark-only patch and NO-PATCH condition before any versioned production result-contract or route-promotion work.

### Takeover qualification pack

#### Q1 — Cross-route production trace
Repository anchors: `calculate.js`, `element.js`, `dkt.js`, `mitc-adoption-solve.js`, `mitc-adoption-recovery.js`, fixture/model builders.
Required reconstruction: build geometrically comparable strip models, identify TRI3-vs-QUAD4 mesh topology, common node/director basis, restraints, load reference and extraction point, then trace CST/DKT through `calculateLocalShell()` and MITC through the explicit adoption solve/recovery route. Record which comparison quantities are physically common and which are formulation-specific.
Fail if: direct element helper output is substituted for either end-to-end route.

#### Q2 — Numerical beam/strip oracle
For span `L`, width `b`, thickness `t`, tip force `P`, `I=bt^3/12`, `A=bt`, derive `delta_b=P L^3/(3EI)` and `delta_s=P L/(kGA)`, with `k=5/6`. Thin limit requires `delta_s/delta_b -> 0`; moderate thickness must retain the non-negligible ratio. State the selected numerical values and expected ordering before reading FE output.
Falsifier: MITC collapses toward the Kirchhoff value when the analytical shear fraction is material, or DKT is advertised as matching Timoshenko total deflection without shear kinematics.

#### Q3 — Authority invariant
CST/DKT remains the qualified Kirchhoff thin-shell route; MITC remains experimental Reissner-Mindlin evidence. Shared cases may demonstrate convergence/physics but cannot themselves switch registry, formulation identity, result schema or release authority. Invalid shortcut: alter shear correction/tolerances or mesh independently until both solvers match.

#### Q4 — Independent validation
Affine membrane case: derive constant plane-stress tensor from prescribed strain and require both routes to recover it. Pressure case: independently integrate total traction and first moment, then require applied-load and support-reaction equilibrium in both routes. Compare common quantities using predeclared tolerances; formulation-specific shear remains MITC-only.
Fail if: either implementation's result becomes the sole oracle for the other.

#### Q5 — Minimal patch
Safe boundary: benchmark/fixture scripts only plus, if necessary, pure helper functions under scripts; no production mechanics, schema, registry, presenter, UI, roadmap or workflow changes. Roll back/escalate if comparable end-to-end cases cannot be constructed without changing formulation authority. NO-PATCH if a mismatch reveals unresolved mechanics/sign/geometry ownership; diagnose the first wrong boundary instead of tuning expected values.
