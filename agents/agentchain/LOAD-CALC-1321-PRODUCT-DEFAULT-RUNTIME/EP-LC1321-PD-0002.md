# EP-LC1321-PD-0002 — Product engineering defaults ordinary-resolution implementation

CHAIN_ID: LOAD-CALC-1321-PRODUCT-DEFAULT-RUNTIME
LEG_ID: LEG-A
ENDPOINT_ID: EP-LC1321-PD-0002
PREVIOUS_ENDPOINT: EP-LC1321-PD-0001

CREATED_AT: 2026-08-28T01:03:10Z
ENDPOINT_REASON: PR_TRANSITION

TASK / ISSUE: Advanced_Analysis #1321 — zero-blocker configurable-default architecture with unified effective-value resolution
PR: #1504
BRANCH: agent/issue-1321-product-engineering-default-runtime

CHECKPOINT_HEAD: ecbc03b71c14c91311722425ba1d18be88b44025
MAIN_HEAD_OBSERVED: 81c2e780a03d565345457b07609fe19d8d1421e4
MERGE_BASE: 81c2e780a03d565345457b07609fe19d8d1421e4

STATE: SOURCE_COMPLETE_EXECUTION_NOT_RUN

### Mission

Connect the existing versioned target-level `PRODUCT_DEFAULT` engineering provider to the ordinary current Common Input field-resolution path required by Issue #1321, without inventing engineering values, changing numerical mechanics, or creating a second resolver.

### This leg completed

- Added `PRODUCT_DEFAULT` to the core Common enrichment authority vocabulary as the lowest current authority.
- Added `PRODUCT_DEFAULT` only to component engineering field authority groups that already support Project configured defaults.
- Left support type/state/sensitivity authority rules unchanged.
- Left all existing higher authority ranks unchanged; no source/master/override/derivation reordering was performed.
- Wired `createNonFeaProductEngineeringDefaultProvider()` into `buildCurrentPreFeaRequestInput()`.
- Added Product engineering records after accepted enrichment and Project configured-default records in the same ephemeral `createNonFeaEnrichmentSidecar()` bundle.
- Kept the single existing `resolveNonFeaEnrichment()` call as the only ordinary field-resolution path.
- Returned the Product engineering-default provider receipt from the current pre-FEA request for auditability.
- Left `LOAD_CALC_ENGINEERING_PRODUCT_DEFAULTS_EMPTY_V1` unchanged with zero shipped target-level engineering values.
- Added focused falsifier `scripts/non-fea-product-engineering-default-runtime-resolution-check.mjs` and registered it in the Non-FEA aggregate runner.
- Opened draft PR #1504 from exact `main@81c2e780a03d565345457b07609fe19d8d1421e4`.
- Retried faithful checkout; DNS still prevents executable qualification.

### Focused falsifier design

The new check uses real providers and the real core resolution ledger:

1. Product-only missing `ELASTIC_MODULUS` → `PRODUCT_DEFAULT` must be selected and the enriched projection must retain Product provenance.
2. Project configured `ELASTIC_MODULUS` + Product default → Project must be selected and Product must remain visible as a shadowed candidate.
3. Source-explicit `UNIT_PIPE_WEIGHT` + Product default → current source authority must be selected and Product must remain visible as a shadowed candidate.
4. Product authority on `RESTRAINT_TYPE` must still be rejected.
5. The shipped Product engineering table must remain empty.
6. Ordinary Common Input source must continue to contain exactly one `resolveNonFeaEnrichment()` call and must not route through the separate authoring seam.

### Exact source delta at PR creation

Expected final net scope after this endpoint/index update is seven files:

1. `agents/agentchain.md`
2. `agents/agentchain/LOAD-CALC-1321-PRODUCT-DEFAULT-RUNTIME/EP-LC1321-PD-0001.md`
3. `agents/agentchain/LOAD-CALC-1321-PRODUCT-DEFAULT-RUNTIME/EP-LC1321-PD-0002.md`
4. `scripts/non-fea-product-engineering-default-runtime-resolution-check.mjs`
5. `scripts/run-non-fea-checks.mjs`
6. `src/core/non-fea-enrichment/index.js`
7. `src/workspace/non-fea-common-input-runtime.js`

Before EP-LC1321-PD-0002 was created, the exact six-file implementation diff was 0 behind main and showed:
- core resolver: 4 additions / 2 replacements, all Product-authority additions after collateral wording cleanup;
- Common Input runtime: 20 additions / 2 comment replacements;
- focused check: new;
- aggregate runner: exactly one registration;
- relay index + EP-LC1321-PD-0001.

### Validation truth

PASS by source inspection:
- live Issue #1321 authority/DoD grounding;
- current Common Input trace;
- Product engineering provider trace;
- core Product-authority omission diagnosis;
- workspace field-registry compatibility;
- exact core diff audit after removal of one collateral wording edit;
- runtime diff audit;
- unchanged empty shipped Product engineering profile;
- open PR #1502 engineering-path independence.

BLOCKED / NOT_RUN:
- faithful branch checkout: BLOCKED — `Could not resolve host: github.com`;
- `node scripts/non-fea-product-engineering-default-runtime-resolution-check.mjs`: NOT_RUN;
- `node scripts/run-non-fea-checks.mjs`: NOT_RUN;
- `npm run check:imports`: NOT_RUN;
- `node scripts/advanced-shell-contract-check.mjs`: NOT_RUN;
- `npm run build`: NOT_RUN;
- `git diff --check`: NOT_RUN.

No NOT_RUN item is represented as PASS.

### Important unresolved authority question discovered

The legacy core enrichment resolver's existing higher-authority order is currently:

`SOURCE_EXPLICIT > SOURCE_INHERITED > EXACT_APPROVED_MASTER > ACCEPTED_OVERRIDE > CONFIGURED_DERIVATION > PROJECT_CONFIGURED_DEFAULT > PRODUCT_DEFAULT`

because the core rank array is ordered that way and existing migration regression explicitly expects source explicit to beat accepted override.

Issue #1321's later unified effective-value policy states:

`ACCEPTED_OVERRIDE > SOURCE_EXPLICIT > SOURCE_INHERITED > EXACT_APPROVED_MASTER > CONFIGURED_DERIVATION > PROJECT_CONFIGURED_DEFAULT > PRODUCT_DEFAULT`.

This PR intentionally does **not** reconcile that discrepancy. Reordering the legacy core hierarchy could alter existing migration/source semantics and requires a separately bounded authority decision plus regression plan. The present leg only installs the missing Product layer beneath Project configured defaults.

### Coordination state

Open draft EMP.1 PR #1502 is engineering-path independent but also changes `agents/agentchain.md` under v2. Therefore:
- engineering overlap: SAFE;
- relay-index overlap: COORDINATION_REQUIRED.

If #1502 merges before #1504 is reviewed/merged, re-ground #1504 on live `main` and preserve all #1502 relay rows/log entries exactly. Do not overwrite its relay history with this branch's older index base.

### Exact next action

1. Re-ground live `main` and PR #1504 head.
2. If #1502 or another v2 relay chain merged, reconcile `agents/agentchain.md` additively and preserve all concurrent rows/logs.
3. If a faithful checkout becomes available, run the focused check, aggregate Non-FEA suite, imports, advanced-shell contract, build, and `git diff --check`; record exact results in a new immutable endpoint.
4. Review the higher-authority hierarchy discrepancy separately; do not change it inside #1504 without a new bounded authority endpoint/decision.
5. Keep #1504 draft/unmerged until owner merge authority is explicit.

### Known / proven

- Product engineering records emitted by the existing provider use authority `PRODUCT_DEFAULT`, exact ENTITY selectors, and version/profile/default semantic-hash provenance.
- Prior to this PR, core `createNonFeaEnrichmentRecord()` rejected those records and ordinary Common Input did not consume the provider.
- After the source patch, `PRODUCT_DEFAULT` is the last current core authority and is permitted only on the existing component default-eligible authority groups.
- Project configured defaults still precede Product defaults in the core candidate order.
- Support authority arrays were not expanded.
- Ordinary Common Input still makes one `resolveNonFeaEnrichment()` call.
- The built-in target-level Product engineering table remains empty.

### Not proven

- Executable correctness of the new focused test or patched runtime on a faithful repository checkout.
- Aggregate regression/import/build success.
- Any actual generic Product engineering numerical values.
- Whether the legacy core higher-authority order should be changed to match the Issue #1321 later effective-value resolver.
- Merge compatibility after any future #1502/main movement.

### Protected invariants

- No invented OD, wall, material density, insulation, fluid, component mass, or modulus defaults.
- One ordinary field-resolution path only.
- `PROJECT_CONFIGURED_DEFAULT` outranks `PRODUCT_DEFAULT`.
- Existing higher authority ordering remains unchanged in this PR.
- Invalid explicit evidence must not be silently converted into Product fallback.
- Support type/state authority is unchanged.
- No support-load statics, route allocation, CoG, source-axis, equilibrium, solver, tolerance, WRC/EMP.1/LAFEA, workflow, or release mechanics change.
- `NOT_RUN` never becomes PASS by source inspection.

### Do not redo

- Do not recreate already-merged #1321 D1-D5 UX/defaults/observability/Run-routing/moment/gravity/hand-calc/Method-Basis/X-Y-Z work.
- Do not populate the empty Product engineering table as part of #1504.
- Do not create another effective-value resolver.

### Inputs

- Advanced_Analysis Issue #1321 live owner-authored requirements.
- Advanced_Analysis `main@81c2e780a03d565345457b07609fe19d8d1421e4`.
- PR #1504 branch checkpoint `ecbc03b71c14c91311722425ba1d18be88b44025`.
- Open PR #1502 changed-file inventory for v2 relay coordination.

### Benchmarks

- `scripts/non-fea-product-engineering-default-profile-check.mjs` — provider/profile/scope/hash contract.
- `scripts/non-fea-configured-default-resolution-check.mjs` — current core Project-default/source behavior.
- `scripts/non-fea-enrichment-migration-check.mjs` — existing legacy core authority behavior, intentionally not reordered here.
- `scripts/non-fea-common-enriched-effective-default-authoring-check.mjs` — existing separate Project/Product authoring composition seam.
- New `scripts/non-fea-product-engineering-default-runtime-resolution-check.mjs` — ordinary-resolution falsifier, execution NOT_RUN.

### Common / governing documents

- `reallaksh19/Common@10d667ce715bb52e1f73035c6fa326db77d0f9dd/skills/engineering-pr-delivery-v2/SKILL.md`.
- `reallaksh19/Common@10d667ce715bb52e1f73035c6fa326db77d0f9dd/skills/engineering-pr-delivery-v2/references/agentchain-schema.md`.
- Advanced_Analysis root `AGENTS.md` @ `81c2e780a03d565345457b07609fe19d8d1421e4`.

### Production paths

- `src/core/non-fea-enrichment/index.js`
- `src/workspace/non-fea-common-input-runtime.js`
- `src/workspace/project-data/non-fea-field-registry.js` (authority reference, unchanged)
- `src/workspace/project-data/non-fea-product-engineering-default-profile.js` (provider/profile, unchanged)

### Validation / test paths

- `scripts/non-fea-product-engineering-default-runtime-resolution-check.mjs`
- `scripts/run-non-fea-checks.mjs`
- existing provider/configured-default/enrichment checks listed above

### Changed during this leg

- Created branch and durable pre-mutation endpoint EP-LC1321-PD-0001.
- Registered the active chain in `agents/agentchain.md`.
- Patched core authority acceptance for Product defaults at the bottom of existing default layers.
- Patched ordinary Common Input to consume the existing Product engineering provider in the same resolver sidecar.
- Added/registered the focused falsifier.
- Removed one collateral error-message wording change found during exact diff audit.
- Opened draft PR #1504.
- Created this immutable PR-transition endpoint.

### Open risks / questions

- Executable qualification is still unavailable because the environment cannot resolve github.com for a faithful checkout.
- PR #1502 may merge first and requires additive relay-index reconciliation.
- Higher-authority precedence mismatch between legacy core enrichment and the later #1321 effective resolver remains unresolved and should be the next authority-focused slice after #1504, not silently merged into it.

### Next-agent qualification

QUALIFICATION_BASIS_HEAD: ecbc03b71c14c91311722425ba1d18be88b44025
QUESTION_SET_ID: QS-LC1321-PD-RUNTIME-0002
QUESTION_SET_STATUS: CURRENT

#### Q1 — Production Trace

Trace `buildCurrentPreFeaRequestInput()` from accepted sidecar through Project configured-default provider, Product engineering-default provider, the single ephemeral sidecar, `resolveNonFeaEnrichment()`, and enriched projection. State the exact candidate insertion order and explain why Product cannot outrank Project under the current core rank array.

#### Q2 — Current Unresolved Problem / Failure Isolation

Explain the newly exposed mismatch between the core enrichment authority order and Issue #1321's later effective-value order. Identify the existing regression that demonstrates current source-over-accepted-override behavior and explain why #1504 intentionally does not change it.

#### Q3 — Authority / Invariant

For a source-explicit `UNIT_PIPE_WEIGHT` plus a Product default, and for a Project configured `ELASTIC_MODULUS` plus a Product default, name the required selected authorities and the evidence that must remain visible for the shadowed Product candidate. Also identify which support fields must still reject Product authority.

#### Q4 — Independent Validation

Run or independently inspect the new focused falsifier. What exact output or assertion failure would prove that Product authority was inserted above Project authority, that Product support authority leaked into `RESTRAINT_TYPE`, or that a second ordinary resolver path was introduced?

#### Q5 — Next Contribution / Minimal Patch

Assuming #1504 source logic is accepted, propose the smallest separately bounded plan to reconcile—or deliberately preserve—the higher-authority hierarchy discrepancy. Include required migration compatibility falsifiers and state why this must not be combined with populating generic Product engineering values.