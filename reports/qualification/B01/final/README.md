# B01 final integrated qualification evidence

This directory indexes the final exact-head qualification for issue #1100.

The authoritative final execution receipt is generated by `.github/workflows/lafea-b01-final.yml` using `scripts/lafea-b01-final-qualification.mjs`. The workflow checks out the pull-request **head SHA itself** (not the synthetic merge commit), requires a clean source tree, reruns the independent oracle and deterministic mesh generator, reruns prior continuum controls, then executes the frozen B01 matrices:

- 54 registered-route base runs (six affine cases × T3/T6/Q8 × M0/M1/M2),
- 270 frozen metamorphic runs (five variants across the same 54 identities),
- 16 governed fail-closed negative controls.

The receipt retains SHA-256 and Git blob custody for the frozen source/oracle/probe/mesh/acceptance files, current production owner files, runner files, and committed repair receipts. It also retains stdout/stderr hashes and exit codes for every integrated command, the frozen parent commit/tree ancestry, normalized mesh semantic identities, and the base/metamorphic/fail-closed master evidence hashes.

B01 remains code-verification authority for the affine linear-continuum foundation only. It does **not** grant nonlinear, contact, shell, weld, design-code, or release authority. `releaseAuthorityGrantedByProgram=false` is mandatory in the final receipt.

The research report references a sandbox ZIP that is not present in the repository. Final evidence must not claim that archive hash was independently verified; the repository-native frozen benchmark and independent oracle are the governed source used for B01 execution.
