# EP-0009 — MITC assembly/solve composition implemented; recovery leg pre-work

CHAIN_STATE_VERSION: 3
CHAIN_ID: ADV-LAFEA-1536-SHELL-PRODUCTION
ENDPOINT_ID: EP-0009
PARENT_ENDPOINT: EP-0008
CUSTODY_EPOCH: 9
MATERIAL_BATCH_HEAD: 8cab9260b4873dee4203f192a55e153f6512400a
COMMON_PROTOCOL: engineering-pr-delivery-v2
COMMON_PROTOCOL_BASIS: 9573ec2bba234cc7cd6abcf73c4c3f3e2bc0892c
COMMON_PROTOCOL_STATUS: CURRENT
ROADMAPS: docs/conceptcumroadmapLAFEA.md@088f4cebfd954e5d1e37da855c95142712463a31
ROADMAP_REVIEW_STATUS: COMPLETE
HANDOVER_READY: TRUE

## Material result

The third experimental MITC adoption batch adds production-shaped global assembly and linear-solve composition without creating new numerical authority.

`src/core/local-shell/mitc-adoption-solve.js`:

- keeps geometry/formulation input in the separate experimental MITC model contract;
- canonicalizes execution-time constraints, nodal force/tangent-moment loads and pressure loads without rewriting `local-shell-model/v1`;
- passes retained MITC element `globalStiffness` and `globalDofOrdering` directly into the generalized parent `assembleGlobalSystem()`;
- consumes the already-qualified experimental MITC pressure vector rather than recomputing pressure;
- maps nodal `[FX,FY,FZ,M1,M2]` into the existing `[UX,UY,UZ,R1,R2]` generalized load convention;
- calls the parent `solveLoadCase()` so prescribed-DOF partition, dense Cholesky, sparse PCG, residuals, reactions and force/moment equilibrium retain their existing owners;
- exposes only experimental/nonproduction execution evidence and does not perform stress recovery.

Focused `scripts/lafea.4-mitc-adoption-solve-check.mjs` includes:

- fully fixed pressure `q=0` -> independent exact oracle `R=-F` plus global force/moment equilibrium;
- fully fixed nodal load/tangent moments -> exact generalized component reversal in reactions;
- prescribed global rigid motion with no load -> assembled `Kq` near zero;
- a real free-DOF MITC4 cantilever pressure solve -> parent deterministic dense Cholesky and retained residual/equilibrium qualifications;
- under-constrained model -> existing shell singular-system failure, with no stabilization/drilling penalty/regularization/fallback.

## Validation truth

Executable Node checks remain `NOT_RUN` in this environment. No software or engineering-validation PASS is claimed. The focused checks are committed but not executed here. Parent #1541 exact-head runner remains the dependency for final numerical qualification.

No recovery, result schema, stage registry, presenter, UI, roadmap or workflow file changed.

## Next bounded material batch — formulation-aware recovery only

Add experimental MITC recovery from retained displacement/element evidence without changing solve mechanics or publishing a production result contract.

Required semantics:

1. transform solved global generalized displacement to each element's local `[u,v,w,betaX,betaY]` ordering using retained adoption transformation evidence;
2. recover membrane strain, curvature and tied transverse-shear strain at formulation integration points;
3. recover in-plane stress at `BOTTOM/MIDSURFACE/TOP` from `epsilon_m + z*kappa` using the existing plane-stress D matrix;
4. retain transverse shear separately as `gamma_xz/gamma_yz`, effective average shear stress `kG*gamma`, and shear resultant `kGt*gamma`;
5. preserve current in-plane plane-stress von Mises meaning; do not silently combine transverse shear into a new 3D invariant;
6. reconstruct membrane/bending/shear energies independently and compare their sum with element `0.5*q^T*K*q`;
7. no stage registry/presenter/UI or production result-schema promotion.

### Handover snapshot

Repo: `reallaksh19/Advanced_Analysis`
Task: #1536 — MITC adoption package
Chain / endpoint: `ADV-LAFEA-1536-SHELL-PRODUCTION` / `EP-0009`
PR: #1542 Draft stacked on #1541
Branch / material head: `codex/lafea-1536-mitc-adoption` / `8cab9260…`
Merge authority: OWNER_ONLY
State: IN_PROGRESS / HELD / NOT_REQUIRED / WRITE_ALLOWED / AUTO RUNNING
Roadmap: `docs/conceptcumroadmapLAFEA.md@088f4ceb…`
Inputs: experimental solved displacement + retained MITC element evidence
Benchmarks: adoption element/pressure/solve checks + existing MITC component gates
Blocker: executable qualification NOT_RUN
Exact next action: implement experimental membrane/bending/transverse-shear recovery and energy parity only; no result/UI promotion.

Q1: Trace global solved DOFs through the retained MITC beta transformation to Gauss-point membrane, curvature and shear strains for both MITC4 and MITC3.
Q2: Hand-reconstruct one plane-stress top/bottom surface state and one `Q=kGt*gamma` transverse-shear resultant with units/signs.
Q3: Prove in-plane von Mises remains the existing plane-stress invariant and transverse shear cannot be silently folded into it.
Q4: Independently reconstruct membrane+bending+shear strain energy and compare with `0.5*q^T*K*q`; include zero rigid-motion and pure membrane controls.
Q5: Define minimal recovery patch and NO-PATCH boundary before any result-contract/registry/UI work.

### Takeover qualification pack

#### Q1 — Recovery production trace
Anchors: `mitc-adoption-element.js`, `mitc-adoption-solve.js`, `mitc4-element.js`, `mitc3-element.js`, current CST/DKT `recovery.js` as sign/plane-stress reference.
Required reconstruction: select one element's global DOF identities, extract solved values, apply retained local beta transformation, then evaluate formulation-owned membrane/bending/shear B matrices at each retained Gauss point. Identify exact local order and surface `z` coordinates.
Fail if: recovery reinterprets physical rotations as beta slopes or bypasses the retained transformation.

#### Q2 — Numerical stress/shear reconstruction
Given `E,nu,t`, one membrane strain vector, curvature vector and shear strain pair, derive plane-stress D, top/bottom `epsilon_m+z*kappa`, in-plane stress/von Mises, `tau_bar=kG gamma`, and `Q=kGt gamma`. State units explicitly: stress MPa, shear resultant N/mm for canonical mm/N units.
Falsifier: missing 5/6 factor in retained effective shear stress/resultant when matching stiffness energy.

#### Q3 — Authority invariant
Existing `vonMises` in LAFEA.4 is an in-plane plane-stress invariant. MITC adoption may retain transverse shear evidence but may not redefine that field as a 3D equivalent stress. Any later 3D invariant needs an independently authorized method/result-contract change.
Invalid shortcut: adding `3*tau_xz^2+3*tau_yz^2` to the current field and keeping the same identity.

#### Q4 — Independent energy oracle
At each Gauss point integrate `0.5*eps_m^T D eps_m*t`, `0.5*kappa^T D kappa*t^3/12`, and `0.5*gamma^T gamma*kGt`. Sum over the element and compare against `0.5*q_local^T*K_local*q_local`. Rigid motion must recover zero strain/energy; an affine membrane field must recover its closed-form constant membrane strain/stress.
Fail if: stiffness-derived total is used as the only recovery expected value.

#### Q5 — Minimal patch
Safe boundary: new experimental recovery module + focused checks. Consume existing B/evidence/formulation owners; do not alter solver, stiffness, pressure, v1 CST/DKT recovery, result schema, registry, presenter, UI, roadmap or workflow. Roll back if energy parity fails, shear-factor semantics diverge from stiffness, or surface sign conventions cannot match current LAFEA.4. NO-PATCH if a new stress invariant/publication meaning is required to make the recovery appear complete.
