# EP-0011 — CI run-creation cessation isolated; LEG-002 remains unmeasured

CHAIN_STATE_VERSION: 3
CHAIN_ID: ADV-PROD-1634-RECOVERY
ENDPOINT_ID: EP-0011
PARENT_ENDPOINT: EP-0010
CUSTODY_EPOCH: 11
WORK_ITEM_KEY: github:reallaksh19/Advanced_Analysis#1634
WORK_ITEM_MODE: EXCLUSIVE
AGENT_INSTANCE_ID: chatgpt:449bf49a-6d6f-47c6-8a31-848fe113b1a1
COMMON_PROTOCOL: engineering-pr-delivery-v2
COMMON_PROTOCOL_BASIS: 293a3db7993a6945c01adc592a7ff14a339c504a
COMMON_PROTOCOL_STATUS: CURRENT
QUALIFICATION_PROFILE: FEA
QUALIFICATION_PROFILE_VERSION: 2
OWNER_PROGRESSION_COMMAND: proceed next
WRITE_AUTHORITY: READ_ONLY
MERGE_AUTHORITY: OWNER_ONLY
MERGE_AUTHORIZED: FALSE
RELEASE_QUALIFIED: FALSE

## Current repository / PR basis

- live `main`: `ed01bc8b43ee219a0c69c0d2114ccac351096148`;
- PR #1639: OPEN / DRAFT / mergeable at observation;
- exact observed PR head before this custody endpoint: `d73b71a9af8aaca71901cedf378375f6bce081d6`;
- LEG-002 material head: `f85f6b9262fe8ae878eb30c49355af750d50f43b`;
- LEG-002 state: `IMPLEMENTED_NOT_ACCEPTED`;
- no material engineering source changed in this endpoint.

## New executable-infrastructure diagnosis

The prior generic `BLOCKED_INFRASTRUCTURE` classification is now narrowed.

### 1. #1639 exact head still has no execution

At exact head `d73b71a9af8aaca71901cedf378375f6bce081d6`:

- commit-associated pull-request workflow runs: `0`;
- combined commit statuses: `0`;
- branch-specific Actions query for `fix/1634-bundle-size-recovery` + `event=pull_request`: `total_count=0`.

This is not an observation gap caused by querying only one SHA; the branch has no pull-request workflow run at all.

### 2. Repository-wide run creation ceased after 2026-09-02

Repository-wide Actions query for runs created at or after `2026-09-03T00:00:00Z` returns `total_count=0` despite ongoing PR synchronize/merge activity on September 3–4.

Historical Actions do exist. The latest visible repository-wide run returned by the API is the EMP.1 gamma5 workflow run `33582556445`, created `2026-09-02T02:15:35Z` for PR #1628. Therefore the repository is not an Actions-never-configured case; run creation has ceased across the observed repository window.

The connected GitHub surface does not expose workflow dispatch or Actions permission/settings mutation, and direct Actions permission/workflow-state endpoints are unavailable through the connector. The exact administrative cause — disabled Actions, policy, account/billing, repository setting, or another GitHub-side condition — is therefore **NOT DETERMINED** here and must not be guessed.

### 3. Last same-workflow runner was healthy enough to execute repository code

For historical run `33582556445`, job `100099718493`:

- hosted Ubuntu runner allocated successfully;
- `actions/checkout@v4`: PASS;
- `actions/setup-node@v4`: PASS;
- repository script execution began;
- the job later failed on an old EMP.1 exact-head oracle semantic-hash drift (`POST_AUTHORITY_ORACLE_SEMANTIC_HASH_DRIFT`), not on runner provisioning.

That old engineering failure belongs to PR #1628 and is **not** projected onto current PR #1639. Its relevance is only infrastructural: the same workflow/runner route was executable on September 2.

## Validation-route coverage diagnosis

### Unconditional EMP.1 workflow

`.github/workflows/emp1-gamma5-main-route.yml` exists unchanged on current `main` and #1639 and declares both unconditional `pull_request:` and `workflow_dispatch:` triggers. It runs EMP.1 oracle/product qualification, including `scripts/emp1-workbench-product-run-qualification.mjs`.

However, that workflow does **not** run:

- `node scripts/bundle-lazy-emp1-product-run-check.mjs`;
- `npm run check:imports` as the production recovery aggregate;
- `npm run build` / Rollup chunk measurement;
- served-`dist/` Chromium boot.

Therefore restoring this workflow is necessary evidence for EMP.1 product custody but is insufficient by itself to accept LEG-002.

### Existing LAFEA.4 shell workflow

`.github/workflows/lafea4-shell-pr-validation.yml` does execute:

- focused shell load/solver gate;
- `npm run check:lafea-solver`;
- `npm run check:lafea-core`;
- `npm run check:imports`;
- `npm run build`.

But it is path-filtered to LAFEA shell/solver/package/chain-1536 files. #1639 changes only its recovery-chain custody, the lazy EMP.1 structural guard, and two `src/workspace/emp1-workbench-*` files, so #1639 does not qualify for that workflow's path trigger. It also has no served-production browser step.

Separately, the active #1536 shell-production chain has already recorded that `check:lafea-core` does not transitively execute the six integrated LAFEA.4 MITC-adoption/production-route scripts. That validation-policy gap remains governed by chain #1536 and is not mutated here.

## Local execution route

A fresh local GitHub checkout probe still fails before materialization with DNS resolution failure (`Could not resolve host: github.com`). Partial connector reconstruction is not accepted as faithful full-repository executable evidence.

## Engineering disposition

LEG-002 remains fail-closed:

| Gate | Status |
|---|---|
| material/source scope | PASS_SOURCE_INSPECTION |
| route-authority owner/source custody | PASS_REOBSERVED_SOURCE |
| lazy-boundary source inspection | PASS_SOURCE_INSPECTION |
| structural guard execution | NOT_RUN |
| EMP.1 product qualification | NOT_RUN |
| imports | NOT_RUN |
| production Rollup/chunk bytes | NOT_RUN |
| served production browser boot | NOT_RUN |
| current exact-head CI | BLOCKED_ON_RUN_CREATION |
| LEG-002 acceptance | IMPLEMENTED_NOT_ACCEPTED |
| release | HOLD / FALSE |

No evidence authorizes the EP-0008 benchmark-runtime packaging seam. Stacking another optimization before measuring LEG-002 would destroy causal attribution of bundle-size and runtime effects.

## Exact next action

1. Keep PR #1639 Draft and unmerged.
2. Restore a faithful execution route by either:
   - restoring repository GitHub Actions run creation, or
   - providing a faithful full checkout/dependency/browser runtime for exact PR head execution.
3. On that exact head, execute in order:
   - `node scripts/bundle-lazy-emp1-product-run-check.mjs`;
   - `node scripts/emp1-workbench-product-run-qualification.mjs` and governed EMP.1 authority checks;
   - `npm run check:imports`;
   - `npm run build` and retain exact generated chunk byte measurements against the unchanged 1.125 MiB ceiling;
   - serve generated `dist/` and run Chromium smoke capturing `pageerror`, console errors and unhandled rejections.
4. PASS with sufficient chunk margin and clean browser boot -> accept LEG-002 and advance to UI/layout and numerical qualification.
5. PASS structurally/browser but still above ceiling -> open a fresh write-ahead material endpoint for the EP-0008 `FeaBenchmarkPanel.run()` lazy-runtime candidate.
6. Any executable failure -> isolate the first wrong owner from the measured failure before changing source.
7. Do not mutate workflow YAML, Actions settings, numerical/oracle/tolerance/source/route/release authority, or the bundle ceiling under this chain's current authority.
