# Endpoint — EP-0023

CHAIN: ADV-EMP1-HUMAN-UI-1651
WORK_ITEM_KEY: github:reallaksh19/Advanced_Analysis#1651
PARENT_ISSUE: 1651
RECOVERY_CHILD_ISSUE: 1664
PREDECESSOR_ENDPOINT: EP-0022
COMMON_PROTOCOL: engineering-pr-delivery-v2
COMMON_PROTOCOL_BASIS: 3e21f0054ab8d80b7fe045e7c105a81643fcbbf7
COMMON_PROTOCOL_STATUS: CURRENT
MATERIAL_LEG: LEG-011
MATERIAL_RECEIPT: agents/chains/ADV-EMP1-HUMAN-UI-1651/material-legs/LEG-011.md
FIXED_MATERIAL_HEAD: dfc7e0f90fdd15dc1bf8e48b462b612b27bfbf5f
MATERIAL_PR: 1696
MATERIAL_PR_STATUS: OPEN_DRAFT_NOT_MERGE_AUTHORIZED
OWNER_PROGRESSION_COMMAND: proceed next
OWNER_PROGRESSION_STATUS: CONSUMED_BY_LEG_011
OWNER_MERGE_COMMAND: NONE
QUALIFICATION_SCOPE_ID: QSCOPE-1651-EMP-TASK-SHELL-RECOVERY
QUESTION_SET_ID: QS-ADV-EMP1-HUMAN-UI-1651-0006
QUESTION_SET_STATUS: CURRENT
QUESTION_PACK_ACTION: REUSED
QUESTION_DISPLAY: HIDE
CUSTODY_STATE: HELD
QUALIFICATION_STATE: NOT_REQUIRED
WRITE_AUTHORITY: READ_ONLY_PENDING_FRESH_OWNER_PROGRESSION
AUTO_STATE: NOT_APPLICABLE
MERGE_AUTHORITY: OWNER_ONLY
MERGE_AUTHORIZED: FALSE
HANDOVER_VALIDATION_STATUS: FAIL
HANDOVER_READY: FALSE
ISSUE_HANDOVER_SYNC_STATUS_AT_ENDPOINT_CREATION: PENDING_EP0023_COMMENT_SYNC

## LEG-011 result

The bounded controller repair and dependent falsifiers were frozen at:

```text
dfc7e0f90fdd15dc1bf8e48b462b612b27bfbf5f
```

Static contract checks passed, but real Chromium execution disproved the repair. The focused qualification-sample spec ran under a successfully installed/launched browser and failed 2/2. The complete-sample action still returns `EMP1_A_CURRENT_QUALIFIED_RESULT_REQUIRED` with no A/B/runInput retained.

This is executable product/test evidence, not an environment blocker and not PASS.

## Refined root cause

The immediate failure occurs before `LafeaWorkbenchController.loadEmp1QualificationSample()` receives a sample.

`createEmp1WorkbenchQualificationSample()` currently:

1. creates a raw LAFEA.1 fixture;
2. calls `executeLafeaStage('LAFEA.1', rawA)`;
3. receives an execution whose `source` is the workbench-normalized A document;
4. calls `refreshEmp1BSourceEvidence()` with the original raw A document plus that execution.

The refresh seam deliberately requires `semanticHash(execution.source) === semanticHash(aDocument)`. LAFEA.1 normalization canonicalizes the retained source representation, so the raw fixture and normalized execution source are not guaranteed semantically identical. On mismatch the factory throws `EMP1_A_CURRENT_QUALIFIED_RESULT_REQUIRED` before returning. That matches the browser observation that A/B/runInput all remain absent.

The next authority-safe source fix is to normalize A through the same public LAFEA.1 document normalizer first, use that normalized document both for execution and for A-to-B refresh, and continue returning source documents only. The factory must not return or inject `aExecution`.

## Stage-17 carrier failure classification

`node scripts/lafea-stage17-browser-run.mjs` also failed, but earlier at `scripts/emp1-public-product-check.mjs:67`:

```text
actual   BOUNDED_LOCAL_CORRELATION_AVAILABLE
expected BLOCKED_LOCAL_CORRELATION
```

This is independent baseline test drift. At the same fixed head the bounded gamma5 registry is already `registered=true`, `engineeringUseAuthorized=true`, `suspensionReasons=[]`; the public-product script still asserts its historical pre-promotion blocked state. LEG-011 changes neither the product contract nor route registry. Future alignment may update the stale test expectation but must not mutate route authority.

## Validation ledger

PASS at fixed head:

- npm clean install;
- `emp1-qualification-sample-orchestration-check.mjs`;
- `emp1-analytical-layout-check.mjs`;
- `emp1-manual-browser-audit-check.mjs`;
- `emp1-issue1651-acceptance-check.mjs`.

FAIL at fixed head:

- focused Playwright qualification-sample orchestration: 2/2 failed;
- Stage-17 carrier: failed on stale public-product assertion before reaching the new focused browser family.

NOT_RUN / not closable because prerequisite still fails:

- correctly prepared desktop structured audit;
- correctly prepared narrow structured audit;
- passing desktop default/readiness-open/narrow screenshots;
- valid CAUx Enter/Space acceptance observation.

## Main drift

After LEG-011 was based on `8eefaf0d1b9effdcd395dbd54a0b027bc43f308c`, `main` advanced to `65a5aa6c29ef533f358e5c4c296318025dfafe9f`. Compare shows 18 additional commits limited to separate LAFEA-UQ reference-validation material and scripts; no EMP.1 controller/sample/falsifier or protected authority file overlaps this chain. The diagnosis remains valid. A future source leg should base on the then-current main rather than rebasing the frozen failed material receipt.

## Exact next action

No further source mutation is authorized by validation evidence alone. Await a fresh recognized Owner command:

```text
proceed next
```

On that command, current custodian should open one bounded continuation from current main to:

1. normalize the qualification A source before factory execution/refresh;
2. retain the no-private-execution-injection boundary;
3. align the stale `emp1-public-product-check.mjs` bounded-route expectations to current registered authority without changing authority;
4. run/freeze static checks plus focused Playwright and Stage-17 gates;
5. only after executable success, resume the desktop/narrow/keyboard human-factor observation path;
6. record receipt/endpoint and synchronize #1651/#1664.

PR #1696 must not be merged in its current failed-validation state.