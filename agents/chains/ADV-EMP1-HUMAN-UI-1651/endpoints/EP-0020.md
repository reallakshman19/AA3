# Endpoint — EP-0020

CHAIN: ADV-EMP1-HUMAN-UI-1651
WORK_ITEM_KEY: github:reallaksh19/Advanced_Analysis#1651
PARENT_ISSUE: 1651
RECOVERY_CHILD_ISSUE: 1664
PREDECESSOR_ENDPOINT: EP-0019
COMMON_PROTOCOL: engineering-pr-delivery-v2
COMMON_PROTOCOL_BASIS: 3e21f0054ab8d80b7fe045e7c105a81643fcbbf7
COMMON_PROTOCOL_STATUS: CURRENT
MATERIAL_LEG: NONE
LATEST_MATERIAL_LEG: LEG-010
FIXED_MATERIAL_HEAD: dc2df7a8a8b26feea26d29dbf0741794f788cfac
MERGED_IMPLEMENTATION_PR: 1675
PR_HEAD_AT_MERGE: 661b2b0c0eacb3243e037e5356b6f2188596a3d6
PR_MERGE_COMMIT: 11f655e71a81b0d7ebef42e99792482b434e60db
MAIN_HEAD_OBSERVED_POST_MERGE: 11f655e71a81b0d7ebef42e99792482b434e60db
POST_MERGE_RECONCILIATION_BRANCH: agent/emp1-1651-postmerge-reconciliation-0020
POST_MERGE_RECONCILIATION_PR: PENDING
OWNER_TEXT_OBSERVED: `merge. clearly say what that novice agent should do step by step!`
OWNER_MERGE_COMMAND: MERGE_PR_1675
OWNER_MERGE_COMMAND_STATUS: CONSUMED
OWNER_PROGRESSION_COMMAND: NONE
QUALIFICATION_SCOPE_ID: QSCOPE-1651-EMP-TASK-SHELL-RECOVERY
QUESTION_SET_ID: QS-ADV-EMP1-HUMAN-UI-1651-0006
QUESTION_SET_STATUS: CURRENT
QUESTION_PACK_ACTION: REUSED
QUESTION_DISPLAY: HIDE
TAKEOVER_QUALIFICATION_READY: TRUE
ENGINEERING_STATE: LEG_010_MERGED_SAMPLE_LOADED_POST_MERGE_REOBSERVATION_PENDING
CUSTODY_STATE: HELD
QUALIFICATION_STATE: NOT_REQUIRED
WRITE_AUTHORITY: READ_ONLY_AWAITING_EXTERNAL_VALIDATION
AUTO_STATE: NOT_APPLICABLE
MERGE_AUTHORITY: OWNER_ONLY
MERGE_AUTHORIZED: FALSE
HANDOVER_CONTENT_READY: TRUE
HANDOVER_VALIDATION_STATUS: FAIL
CHAIN_HANDOVER_READY: TRUE
HANDOVER_READY: FALSE
ISSUE_HANDOVER_SYNC_STATUS_AT_ENDPOINT_CREATION: PENDING_EP0020_COMMENT_SYNC

## Merge result

Owner explicitly authorized merge of implementation PR #1675. The PR was marked ready for review only as a necessary merge transition and merged with exact-head protection:

- exact PR head: `661b2b0c0eacb3243e037e5356b6f2188596a3d6`;
- merge commit: `11f655e71a81b0d7ebef42e99792482b434e60db`;
- `main` observed at exactly the merge commit after merge;
- PR #1675 is closed/merged;
- the merge authorization is consumed and does not transfer to any successor PR.

Merge does not alter validation truth and does not convert NOT_RUN/FAIL evidence to PASS.

## Validation truth after merge

Retained accepted evidence:

- three focused Node/static checks PASS at fixed LEG-010 material head `dc2df7a8a8b26feea26d29dbf0741794f788cfac` from supporting PR #1681;
- Section Screening -> Review / LAFEA.2 presentation coherence is supporting PASS evidence;
- protected authority remains unchanged.

Still open:

- previous desktop structured audit own status = `FAIL_CURRENT_VIEWPORT_DOM_OBSERVATION`;
- previous narrow structured audit own status = `FAIL_CURRENT_VIEWPORT_DOM_OBSERVATION`;
- correctly prepared complete-qualification-sample desktop audit on merged main = NOT_RUN;
- correctly prepared complete-qualification-sample narrow audit on merged main = NOT_RUN;
- actual CAUx Enter/Space disclosure observation on merged main = NOT_RUN;
- passing-run desktop default/readiness-open/narrow screenshots = NOT_RUN;
- focused Playwright = `BLOCKED_ENVIRONMENT / NOT_RUN` unless a compatible project-local browser actually executes it;
- zero status contexts/workflow runs are not PASS.

## Exact validation target and prerequisite

Post-merge human-factor validation must use the actual merged application state:

```text
TARGET COMMIT: 11f655e71a81b0d7ebef42e99792482b434e60db
```

In EMPIRICAL -> EMP.1, the tester must click exactly:

```text
[SIMULATED] Load complete EMP.1 qualification sample
```

The following is NOT a substitute:

```text
[SIMULATED] Load EMP.1.A demonstration source
```

`runEmp1ManualBrowserAudit({ seedQualificationPressure: true })` does not load the qualification sample. It only supplements Pressure when a suitable LAFEA.1 document is already present.

## Novice-agent role boundary

The novice agent is a validation runner only. It must not:

- claim takeover/custody;
- create a new handover PR;
- change product/source/test code;
- open LEG-011 unless the current custodian later does so under a valid Owner progression command;
- merge or authorize any PR;
- call a FAIL object PASS;
- treat screenshots, 50% zoom, zero CI contexts, or `.click()` as substitutes for the required structured/keyboard observations.

## Exact next action

Run the post-merge validation against `11f655e71a81b0d7ebef42e99792482b434e60db`:

1. checkout the exact merge commit and verify `git rev-parse HEAD` equals it;
2. run the three static Node checks and preserve their exact output;
3. start Vite with `npm run dev`;
4. open EMPIRICAL -> EMP.1;
5. click `[SIMULATED] Load complete EMP.1 qualification sample` and verify the qualification sample is visibly loaded before invoking the audit helper;
6. import `/scripts/emp1-manual-browser-audit.js?manual-audit=4`;
7. desktop: run `runEmp1ManualBrowserAudit({ seedQualificationPressure: true })` and preserve the entire returned object;
8. accept desktop only if its own `status` is `PASS_CURRENT_VIEWPORT_DOM_OBSERVATION` and `failures=[]`;
9. capture desktop default screenshot;
10. open Readiness/review/technical custody detail and capture desktop readiness-open screenshot, confirming the primary console is not displaced;
11. resize to 720x900;
12. run `runEmp1ManualBrowserAudit()` and preserve the entire returned object;
13. accept narrow only if its own `status` is `PASS_CURRENT_VIEWPORT_DOM_OBSERVATION` and `failures=[]`;
14. capture narrow screenshot showing Work/Basis/Evidence and exactly one selected pane;
15. navigate to Evidence -> Benchmark Evidence -> CAUx retained audit disclosure;
16. use actual keyboard input: focus the disclosure, press Enter and observe it opens, then press Space and observe it closes;
17. report `keyboard PASS` only if those two physical keyboard observations succeed;
18. return exact command outputs, both full audit objects, keyboard result, and all three screenshots to the current custodian;
19. make no repository mutation.

If any returned audit object is FAIL, preserve the exact `failures` entries verbatim. Those concrete findings may become the basis for a future LEG-011 only under a new valid Owner progression command.