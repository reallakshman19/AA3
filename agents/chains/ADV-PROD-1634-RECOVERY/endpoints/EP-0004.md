# EP-0004 — LEG-002 bundle-size recovery prework

CHAIN_ID: ADV-PROD-1634-RECOVERY
ENDPOINT_ID: EP-0004
ENDPOINT_KIND: MATERIAL_LEG_PREWORK
CUSTODY_EPOCH: 4
COMMON_PROTOCOL: engineering-pr-delivery-v2
COMMON_PROTOCOL_BASIS: 293a3db7993a6945c01adc592a7ff14a339c504a
OWNER_PROGRESSION_COMMAND: fix, merge open PR (only yours in this Chat) and proceed next

## Repository state

- Prior recovery PR #1637: MERGED by explicit Owner authorization only.
- Merge commit / new main basis: `6e6c4062fffbd173aa9c4d2a2b34c2586df47f4e`.
- New branch: `fix/1634-bundle-size-recovery`.
- Release qualification remains `FALSE / HOLD`.
- Merge authority for any new PR remains Owner-only and is **not** pre-authorized by the prior merge command.
- GitHub Actions: zero runs observed on merge head and preceding current-main heads; workflow mutation remains forbidden.

## Why LEG-002 is now permitted

The Owner explicitly directed the recovery chain to merge #1637 and proceed next. LEG-001 remains only partially executable because exact-head production build/browser infrastructure is absent, but the first production recovery patch is now on main. P1 work therefore proceeds fail-closed: it may reduce the known oversize eager bundle, but it may not claim production boot/build PASS without faithful exact-head execution.

## Measured P1 basis

The last available executable Pages build produced a main JavaScript chunk of approximately `1,834,272 B` against the unchanged hard ceiling `1,179,648 B` (1.125 MiB). That build also emitted Rollup evidence that `src/workspace/emp1-workbench-product-run.js` was both dynamically and statically imported, so its dynamic import could not establish a lazy chunk boundary.

Current source inspection on merged main confirms:

1. `lafea-workbench-controller.js` statically imports only `currentEmp1WorkbenchRouteAuthority()` from `emp1-workbench-product-run.js` for synchronous UI/readiness projection.
2. `runEmp1Product()` separately dynamically imports `executeEmp1WorkbenchProduct()` from the same module.
3. `emp1-workbench-product-run.js` currently imports the complete EMP.1 A/B/C execution stack and `lafea-workbench-model.js`, making the static authority projection pull the heavy transaction graph eagerly.

## Chosen repair seam

Preserve `emp1-workbench-product-run.js` as the **single owner** of `currentEmp1WorkbenchRouteAuthority()` and leave that authority function/payload/hash semantics unchanged. Do not create a second authority projection and do not force stateful workspace modules into manual chunks.

Split only the heavy product transaction implementation into a new lazy implementation module. Keep the public async `executeEmp1WorkbenchProduct(options)` API in `emp1-workbench-product-run.js` as a thin dynamic loader that delegates after loading the implementation. Pass the already-owned current route-authority snapshot into the implementation; the new implementation must not import the owner module, avoiding a generated lazy-chunk back-edge.

## Planned material paths

- `src/workspace/emp1-workbench-product-run.js`
- new `src/workspace/emp1-workbench-product-execution.js`
- new `scripts/bundle-lazy-emp1-product-run-check.mjs`

No planned changes to:

- `src/core/emp1/**` numerical, registry, applicability or route-authority sources;
- `currentEmp1WorkbenchRouteAuthority()` decision logic, payload, semantic hash or suspension reasons;
- LAFEA shell element/load/recovery/frame mechanics;
- Issue #1551 spring-rate conversion/withholding;
- benchmark expected values/tolerances;
- `vite.config.js` manual chunk ownership in this leg;
- `.github/workflows/**`;
- bundle hard ceiling;
- release/deployment authority.

## Validation contract

Source/structural checks must prove:

- the authority owner remains `emp1-workbench-product-run.js`;
- the authority owner no longer statically imports `../core/emp1/index.js` or `./lafea-workbench-model.js`;
- `executeEmp1WorkbenchProduct()` dynamically loads the new execution implementation;
- the implementation imports the existing qualified execution dependencies;
- the implementation does **not** import `emp1-workbench-product-run.js`;
- the public execution API remains async and retains the same exported name;
- no manual chunk ceiling/config mutation occurs.

Faithful acceptance still requires, when infrastructure exists:

1. focused EMP.1 product-run qualification;
2. method-authority projection check;
3. import checker;
4. production build and chunk report;
5. served `dist/` Chromium boot with zero startup exceptions;
6. exact-head Actions evidence.

## Protected-domain disposition

This leg is packaging/dependency-graph work. If the split requires changing engineering route decisions, numerical formulas, registry contents, source authority, applicability authority, benchmark or release semantics, STOP and re-scope rather than proceed.

QUESTION_SET_ID: QS-ADV-PROD-1634-RECOVERY-0002
QUESTION_SET_STATUS: CURRENT
QUESTION_PACK_ACTION: REUSED
QUESTION_DISPLAY: HIDE
RELEASE_QUALIFIED: FALSE
MERGE_AUTHORITY: OWNER_ONLY
MERGE_AUTHORIZED: FALSE

EXACT_NEXT_ACTION: implement only the bounded lazy execution split plus structural regression guard; then inspect the branch diff and record LEG-002 as IMPLEMENTED_NOT_ACCEPTED unless faithful production execution becomes available.
