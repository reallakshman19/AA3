MATERIAL_LEG_ID: LEG-001
CHAIN_ID: ADV-PROD-1634-RECOVERY
WORK_ITEM_KEY: github:reallaksh19/Advanced_Analysis#1634
PREWORK_ENDPOINT: EP-0001
BASE_HEAD: 9824efabf604e3e2e47f8b8493d978f2a09ff069
MATERIAL_HEAD: 62b5bd88d0d2b8997254215210124cfd32317043
LEG_STATUS: IMPLEMENTED_NOT_VALIDATED

# LEG-001 — production core chunk back-edge recovery

## Scope

Production/build ownership only. No engineering formula, solver, LAFEA/LFEA stiffness/load/recovery, benchmark expected value/tolerance, workflow, source authority, roadmap, or release mutation.

## Demonstrated first wrong boundary

Current forced chunking maps generic core/geometry to `core-application` and `/src/core/linear-piping-*` to `core-linear-piping`. Production source includes:

```text
geometry/adapters/inputxml-unit-system.js
  -> linear-piping-analysis-consumer/restraint-spring-rate.js

linear-piping-analysis-consumer/**
  -> geometry/**
```

This creates a generated chunk back-edge. Untouched-main retained evidence reproduced a TDZ browser-startup exception in `core-linear-piping-*`.

The spring-rate file is also an active Issue #1551 engineering authority. Its arithmetic/withholding semantics were therefore not edited or relocated.

## Changed production/configuration

### `vite.config.js`

Added one explicit exception before the generic `linear-piping-*` rule:

```text
src/core/linear-piping-analysis-consumer/restraint-spring-rate.js
  -> core-application
```

The file is import-free, so co-locating this leaf with the generic core dependency removes the demonstrated generated ESM back-edge while preserving the source owner and existing import API.

This follows the repository's existing `lafea-canonical-sha256.js` chunk-placement pattern: resolve generated evaluation-order cycles by routing a pure leaf with its dependency rather than moving authority or forcing stateful controllers.

### `scripts/bundle-chunk-ownership-check.mjs`

- asserts the spring-rate leaf maps to `core-application`;
- asserts the geometry adapter still consumes the same spring-rate source;
- asserts the spring-rate authority remains import-free, so future dependency growth forces requalification of this exception;
- corrects a stale test-harness assertion from a historical 1 MiB ceiling to the repository's already-existing 1.125 MiB production ceiling. The production ceiling itself was not changed.

## Protected unchanged domains

- `src/core/linear-piping-analysis-consumer/restraint-spring-rate.js` — byte/source semantics unchanged by this leg;
- LAFEA.4 shell element/formulation/loads/recovery/result-frame contract;
- all benchmark expected values and tolerances;
- `.github/workflows/**`;
- source/applicability authority;
- roadmap authority;
- release/deployment state.

## Validation ledger

| Check | Status | Observation | Oracle |
|---|---|---|---|
| Source diff / protected-domain review | PASS | execution not required; diff contains only `vite.config.js` + ownership check for material files | source inspection |
| `manualChunk()` spring-rate owner assertion | NOT_RUN | checker updated but not executed in a faithful repo runtime in this session | code verification |
| spring-rate focused resolver check | NOT_RUN | required because Issue #1551 owns semantics | independent/focused |
| `check:imports` | NOT_RUN | no executable run attached to current head | code verification |
| production Vite/Rollup build | NOT_RUN | no executable run/status attached to current head | execution |
| production built-`dist/` browser smoke | NOT_RUN | mandatory acceptance gate | execution |
| chunk-size gate | NOT_RUN | P1 may remain red after boot fix | execution |
| current-head GitHub Actions | NOT_RUN | workflow/status list empty on `62b5bd88...` | external execution |

## Falsifier / rollback

LEG-001 is rejected/reverted if any of the following occurs:

- built `dist/` still throws the same/new startup TDZ exception;
- spring-rate resolved/withheld values change;
- `check:imports` or focused LFEA support checks expose an invalid chunk/import assumption;
- the spring-rate leaf gains source imports and the explicit chunk exception creates a new back-edge;
- generated chunk inspection shows the true SCC is elsewhere.

## Code-quality review

### Pre-change

- existing owner searched: PASS;
- patch boundary limited to build/chunk ownership: PASS;
- active #1551 engineering authority identified before mutation: PASS;
- speculative production abstraction: NONE;
- new unused production modules: 0.

### Post-change

- production calculation/source duplication introduced: NONE;
- new hidden state/singleton: NONE;
- protected engineering source changed: NONE;
- material production/config diff size: 9 added lines in `vite.config.js`;
- regression seam: strengthened existing bundle ownership checker;
- required next proof: real build + built-browser boot, not source inspection.

## Exact next action

Run the focused ownership/spring-rate/import checks and a production build served from `dist/`. If browser boot is zero-error, accept LEG-001 and proceed to P1 chunk-size profiling. If not, freeze the generated import/chunk graph from the failing build and patch only the newly demonstrated first wrong boundary.
