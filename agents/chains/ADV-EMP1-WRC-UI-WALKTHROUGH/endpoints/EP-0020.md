# EP-0020 — post-mutation validation: viewport evidence repaired; Chromium/human gate remains open

CHAIN_ID: ADV-EMP1-WRC-UI-WALKTHROUGH
PREVIOUS_ENDPOINT: agents/chains/ADV-EMP1-WRC-UI-WALKTHROUGH/endpoints/EP-0019.md
LEG_ID: LEG-007
REASON: POST_MUTATION_VALIDATION
ISSUE: #1559 OPEN
PR: #1571 DRAFT
BRANCH: codex/emp1-wrc-ui-1559-postmerge
BASE_MAIN: 541e5ad6078e811c55e1c426f1e8ca7a34356a61
MATERIAL_HEAD: 72b76061e333bba992a214f57df62ef0d652dd1c

## Current truth

The evidence harness now distinguishes actual viewport landing from full-page context. This closes the source-level acceptance-evidence defect identified after faithful checkout again failed due local DNS resolution.

No production WRC UI or engineering authority changed.

## Validation matrix

PASS:
- exact committed evidence spec `node --check`
- physical lines: 165 / <300
- 12 checkpoint calls
- each checkpoint captures viewport evidence plus full-page context
- checkpoint manifest records scrollX / scrollY / viewport width / viewport height
- full-page capture explicitly restores recorded scroll coordinates
- checkpoint 12 scrolls the stale notice into the viewport before capture
- branch containment: chain/docs/e2e only

NOT_RUN:
- faithful Git checkout: local `github.com` DNS resolution failed
- Playwright Chromium execution
- generated viewport/full-page PNG evidence
- human-observed/recorded review

NOT_APPLICABLE:
- numerical WRC comparison for this validation-support leg

## Human acceptance rule

The reviewer must use each `*-viewport` attachment to judge the actual target landing and scroll position. The matching `*-full-page` attachment is supporting context only. A passing test or a full-page screenshot by itself is insufficient for #1559 acceptance.

## Exact next action

In a faithful checkout/browser run:

```bash
node scripts/run-playwright.mjs e2e/emp1-professional-walkthrough-evidence.spec.js --workers=1
```

Then review all 12 viewport + 12 full-page attachments using `docs/emp1/EMP1_WRC_UI_WALKTHROUGH.md` and record PASS/PARTIAL/FAIL.

If execution exposes a concrete product defect, create a new PRE_MUTATION endpoint before touching production UI. If it passes, create a validation-only acceptance endpoint. No UI-complete, code-compliance or release claim is authorized before the human gate passes.

HANDOVER_READY: TRUE
