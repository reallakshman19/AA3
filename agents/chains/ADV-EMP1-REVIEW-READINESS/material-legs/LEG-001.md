# LEG-001 — Review State → Readiness Composition

STATUS: MATERIAL_COMPLETE_VALIDATION_NOT_RUN
DATE_UTC: 2026-09-01

## Stack basis

MAIN: 93d208dc1298570f20ce170edbd4f881039d22f0
STACK_BASE_PR: 1620
STACK_BASE_BRANCH: agent/emp1-engineering-review-v1
STACK_BASE_HEAD: a9a43258cb499fcf838341f183fd1d76c4894ced
MATERIAL_HEAD: e51d8c7f2c43f36a000df5d00b9bc10aa51581d8
BRANCH: agent/emp1-review-readiness-integration-v1

## Material changes relative to stack base

```text
src/core/emp1/emp1-readiness-projection.js
scripts/emp1-review-readiness-integration-check.mjs
```

Custody files are under `agents/chains/ADV-EMP1-REVIEW-READINESS/**`.

## Implemented contract

`projectEmp1Readiness(productProjection, options)` now accepts:

```js
{ reviewState: emp1EngineeringReviewState }
```

The previous call remains valid and unchanged in meaning:

```js
projectEmp1Readiness(productProjection)
```

No supplied review state still yields:

```text
review = NOT_REVIEWED
current calculation overall = READY_FOR_ENGINEERING_REVIEW
```

Exact supplied review states compose to:

```text
REVIEW_ACCEPTED -> overall REVIEW_ACCEPTED when calculation is current
REVIEW_REJECTED -> overall REVIEW_REJECTED when calculation is current
REVIEW_STALE    -> overall REVIEW_STALE when calculation is current
```

Source, method, and calculation failure/currentness states retain precedence over review state.

## Fail-closed review-state shape

The readiness projection accepts only schema:

```text
emp1-engineering-review-state/v1
```

and rejects inconsistent state tuples such as `REVIEW_ACCEPTED` with `current=false` or `REVIEW_STALE` without changed bindings.

This is schema-consistency validation only. Readiness does not compare source/A/B/C/assessment/route hashes and does not recreate stale/current review classification.

## Authority boundary

Source inspection confirms readiness does not call or import:

```text
semanticHash(
projectEmp1EngineeringReviewState(
bindEmp1EngineeringReviewEvidence(
emp1-engineering-review-record.js
```

It does not create review, code-compliance, release, method, applicability, source, or numerical authority.

## Compatibility / UI boundary

The existing professional workflow still calls:

```js
projectEmp1Readiness(projection)
```

exactly once. Therefore this PR does not inject review state into the UI and does not add reviewer controls/storage. That remains a separate downstream slice.

Existing readiness and dashboard checkers should retain their prior expected default behavior; they are not modified by this stack.

## Source-level diff audit

Comparison against `agent/emp1-engineering-review-v1` at `a9a43258...`:

```text
status     ahead
ahead      6
behind     0
merge base a9a43258cb499fcf838341f183fd1d76c4894ced
```

Material production change to readiness is +49/-6 lines relative to stack base; no WRC/source/applicability/route/oracle/release/workflow owner is modified.

## Validation truth

Focused executable checker:

```bash
node scripts/emp1-review-readiness-integration-check.mjs
```

STATUS: NOT_RUN

Source inspection and compare cleanliness are not executable PASS.

## Exact next action

Record endpoint custody and open a stacked Draft PR against `agent/emp1-engineering-review-v1`. Keep `MERGE_AUTHORIZED = FALSE`. If PR #1620 merges first, rebase/retarget this stack to current main before review readiness.
