# LEG-001 — Read-Only Method-Authority Projection

CHAIN_ID: ADV-EMP1-GOVERNANCE-METHOD-AUTHORITY
LEG_ID: LEG-001
WORK_ITEM_KEY: owner-direct:advanced-analysis:emp1-governance-method-authority-v1
OWNER_INSTRUCTION: Skip the remaining readiness validation step and proceed coding.

## Authorized material scope

- Add `src/core/emp1/emp1-method-authority-projection.js`.
- Add a focused static/runtime contract checker for the projection.
- Modify `src/core/emp1/emp1-readiness-projection.js` only enough to consume the method-authority projection while preserving readiness behavior.
- Add/update chain custody and a Draft stacked PR.

## Forbidden scope

- WRC equations, tables, datasets, stresses or load/sign mechanics.
- Source authority or applicability authority mutation.
- `emp1-c-bounded-route-registry.js` route values or registration/authorization state.
- Qualification hashes, independent oracle values or tolerances.
- release/deployment authority.
- `.github/workflows/**`.
- UI-side qualification logic.

## Validation plan

Focused checks are authored in this leg but execution through this connector is expected to remain `NOT_RUN` unless an existing execution surface runs them. Source inspection must not be promoted to PASS.

Expected local commands after the material patch:

```bash
node scripts/emp1-method-authority-projection-check.mjs
node scripts/emp1-readiness-projection-check.mjs
```

The previously skipped `node scripts/emp1-professional-workflow-check.mjs` remains part of the parent readiness PR validation story and is not silently reclassified.
