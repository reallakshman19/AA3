# EP-0002 — Calculation Trace Projection Material Complete

CHAIN_ID: ADV-EMP1-CALCULATION-TRACE
ENDPOINT_ID: EP-0002
PREVIOUS_ENDPOINT: EP-0001
CUSTODY_EPOCH: 2
WORK_ITEM_KEY: owner-direct:advanced-analysis:emp1-calculation-trace-v1

## State

MATERIAL_COMPLETE_VALIDATION_NOT_RUN

## Material head

`0cddcf804e73f949d62e447c8822902dc8def131`

Receipt: `agents/chains/ADV-EMP1-CALCULATION-TRACE/material-legs/LEG-001.md`

## Implemented contract

Added `emp1-calculation-trace/v1` as a deterministic read-only projection over already-retained EMP.1 transaction evidence.

It exposes:

- source hash;
- A load-transfer retained result hash/schema/qualification;
- B section-screening retained result hash/schema/qualification/decision;
- C retained evidence hash/state and whether the retained artifact is numerical, prepared/blocked, gate/blocked, or absent;
- immutable assessment parent hashes and decision;
- retained invocation counts;
- existing execution/C currentness;
- retained execution/current route-authority hashes and existing reportability flags.

The ancestry view compares only retained identifiers. `CONSISTENT_WITH_RETAINED_PARENTS` means the available retained identifiers agree with the immutable assessment parent links; it is not an engineering PASS, code-compliance statement, or release qualification.

## Static authority audit

The projection contains no import and no call/reference to:

- `runEmp1(`;
- `semanticHash(`;
- `reconstructResultHashes`;
- `evaluateEmp1Wrc537CylindricalApplicability`;
- `requireEmp1Wrc537QualifiedCylindricalApplicability`;
- `runEmp1Wrc537Gamma5ZeroDpRoute`.

It also contains no WRC limit constants/ratio mechanics.

## Concurrency disposition

Draft PR #1617 concurrently modifies `src/workspace/emp1-professional-result-presentation.js`. To preserve independent review and avoid conflict, this phase stops at the core projection API plus checker. The Owner's selected phase was Calculation Trace Projection; UI rendering is a downstream presentation activity, not silently folded into this PR.

## Validation

Encoded focused command:

`node scripts/emp1-calculation-trace-projection-check.mjs`

Status: **NOT_RUN**.

This remains distinct from PASS/FAIL. The Owner previously directed Draft-PR progression without blocking on local execution.

## Protected domains unchanged

- WRC numerics: unchanged
- source authority: unchanged
- applicability authority: unchanged
- route registry/executor: unchanged
- oracle/tolerances: unchanged
- code compliance: unchanged
- release/deployment: unchanged
- `.github/workflows/*`: unchanged

## Exact next safe action

Open/maintain the Draft PR, preserve `NOT_RUN`, perform latest-main drift review before readiness-for-review/merge, and only add UI rendering after active presentation-PR overlap is resolved.

## Next-agent questions

1. Does every ancestry relation compare retained identifiers only?
2. Does `emp1-assessment/v1.parents` remain the ancestry owner rather than a recomputed hash chain?
3. Can blocked/prepared C evidence be distinguished from a numerical C result without solving anything?
4. Are currentness and route-authority states copied from existing owners rather than inferred in the trace?
5. Is any future UI wiring prevented from treating trace consistency as engineering/code/release approval?
