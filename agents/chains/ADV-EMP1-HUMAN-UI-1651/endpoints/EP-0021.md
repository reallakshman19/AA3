# Endpoint — EP-0021

CHAIN: ADV-EMP1-HUMAN-UI-1651
WORK_ITEM_KEY: github:reallaksh19/Advanced_Analysis#1651
PARENT_ISSUE: 1651
RECOVERY_CHILD_ISSUE: 1664
PREDECESSOR_ENDPOINT: EP-0020
COMMON_PROTOCOL: engineering-pr-delivery-v2
COMMON_PROTOCOL_BASIS: 3e21f0054ab8d80b7fe045e7c105a81643fcbbf7
COMMON_PROTOCOL_STATUS: CURRENT
MATERIAL_LEG: NONE
LATEST_MATERIAL_LEG: LEG-010
MERGED_IMPLEMENTATION_PR: 1675
PR_1675_MERGE_COMMIT: 11f655e71a81b0d7ebef42e99792482b434e60db
POST_MERGE_RECONCILIATION_PR: 1690
POST_MERGE_RECONCILIATION_PR_STATUS: OPEN_DRAFT_OWNER_ONLY_NOT_AUTHORIZED
OWNER_PROGRESSION_COMMAND: NONE
OWNER_MERGE_COMMAND: MERGE_PR_1675_CONSUMED
QUALIFICATION_SCOPE_ID: QSCOPE-1651-EMP-TASK-SHELL-RECOVERY
QUESTION_SET_ID: QS-ADV-EMP1-HUMAN-UI-1651-0006
QUESTION_SET_STATUS: CURRENT
QUESTION_PACK_ACTION: REUSED
QUESTION_DISPLAY: HIDE
TAKEOVER_QUALIFICATION_READY: TRUE
ENGINEERING_STATE: LEG_010_MERGED_POST_MERGE_VALIDATION_PREREQUISITE_FAILED_RERUN_REQUIRED
CUSTODY_STATE: HELD
QUALIFICATION_STATE: NOT_REQUIRED
WRITE_AUTHORITY: READ_ONLY_AWAITING_EXTERNAL_VALIDATION
AUTO_STATE: NOT_APPLICABLE
MERGE_AUTHORITY: OWNER_ONLY
MERGE_AUTHORIZED: FALSE
HANDOVER_VALIDATION_STATUS: FAIL
HANDOVER_READY: FALSE
ISSUE_HANDOVER_SYNC_STATUS_AT_ENDPOINT_CREATION: PENDING_EP0021_COMMENT_SYNC

## Evidence intake

Returned runner evidence identifies the intended integrated target:

```text
11f655e71a81b0d7ebef42e99792482b434e60db
```

Accepted PASS evidence on that reported target:

- `node scripts/emp1-analytical-layout-check.mjs` -> `EMP1_SPLIT_CONSOLE_LAYOUT_CHECK_PASS`;
- `node scripts/emp1-manual-browser-audit-check.mjs` -> `PASS_STATIC_SPLIT_CONSOLE_MANUAL_BROWSER_AUDIT_CONTRACT`;
- `node scripts/emp1-issue1651-acceptance-check.mjs` -> `PASS_STATIC_ROBUST_SPLIT_CONSOLE_ACCEPTANCE_MANIFEST_EXECUTABLE_BROWSER_GATES_RETAINED`.

Browser evidence is NOT acceptance evidence because the required application prerequisite was not established.

Desktop result:

```text
status = AUDIT_ERROR
EMP1_MANUAL_AUDIT_LAFEA1_DOCUMENT_UNAVAILABLE
```

This proves the complete qualification sample was not available to the audit helper. It does not establish a product defect.

Narrow result explicitly reports:

```text
status = FAIL_CURRENT_VIEWPORT_DOM_OBSERVATION
app.empirical.active = false
activeViewId = WORKSPACE
```

Therefore the subsequent missing-pane, missing-shell, route, benchmark and CAUx failures are downstream observations against the wrong application state. They are not accepted as 18 independent split-console defects.

Keyboard result `CAUx disclosure not found` is likewise not accepted as a CAUx accessibility defect because the runner was not in the required EMP.1/Evidence state.

Screenshots were referenced in the returned text but were not supplied as accessible chat attachments and were not added by the latest validation-branch mutation; no visual PASS/FAIL is inferred from them at this endpoint.

## Runner defect / process violation

The validation runner created/pushed `emp1-validation-audit.mjs` on branch:

```text
claude/emp1-issue-1651-validation-yqgedz
```

latest observed head:

```text
1dead34196fe781c43b20657377a1cd239309503
```

That branch is the stale PR #1681 branch, not the merged-main target branch. Compared with merged main it is diverged, ahead 3 / behind 158, and contains validation/report artifacts only. The mutation was not authorized by the current custodian.

Inspection of `emp1-validation-audit.mjs` shows it:

1. guesses direct query-parameter navigation with `?view=EMPIRICAL&entity=EMP.1` instead of proving navigation through the application;
2. attempts to click the complete-sample button but catches failure and continues;
3. repeats that reset/catch/continue pattern in narrow mode;
4. runs the structured audits even when the prerequisite sample/application state is absent.

This explains the returned invalid-state cascade.

PR #1681 has therefore been closed unmerged. Its latest branch head must not be treated as product source or acceptance authority.

## Authority boundary

No LEG-011 is opened. No product/UI/engineering/test/workflow/roadmap/benchmark/route mechanics mutation is authorized or justified from this invalidly prepared browser run.

PR #1690 remains control-plane-only and has no inherited merge authorization.

## Exact next action

A validation runner must rerun against the integrated merged application without creating or modifying repository files:

1. checkout `11f655e71a81b0d7ebef42e99792482b434e60db` and verify exact HEAD;
2. start the application normally;
3. navigate through the rendered UI to EMPIRICAL -> EMP.1;
4. verify the EMP.1 view is active before continuing;
5. click `[SIMULATED] Load complete EMP.1 qualification sample`;
6. verify the complete qualification sample is visibly loaded and a LAFEA.1 document is present;
7. only then import `manual-audit=4` and run the desktop audit;
8. require the desktop object's own PASS status / empty failures;
9. resize the same prepared session to 720x900 rather than reloading into an unproven query-param state;
10. run the narrow audit and require its own PASS status / empty failures;
11. perform actual keyboard Enter-open / Space-close on the CAUx disclosure after navigating to Evidence -> Benchmark Evidence;
12. return exact outputs and actual screenshots without any git commit, branch push, PR, source edit or new audit script.

If that correctly prepared run produces a concrete product failure, preserve the exact failure entries for current-custodian diagnosis. A future LEG-011 would still require a valid Owner progression command.