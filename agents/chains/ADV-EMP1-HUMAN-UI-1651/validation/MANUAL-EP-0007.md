# Manual validation note — EP-0007

Manual localhost verification is accepted as human-observed UI evidence for the current checkpoint while Playwright remains environment-blocked by a missing project-local Chromium executable.

This does not convert the automated Playwright regression gate to PASS. The gate remains present in source and its execution remains BLOCKED_ENVIRONMENT until a compatible browser is installed and the suite runs.

Manual validation should cover:
- EMPIRICAL -> EMP.1 loaded sample;
- no raw machine-state tokens visible outside explicit technical/raw disclosures;
- Pressure table reads one row per identity with Internal / External columns;
- Primary work and Engineering basis are side-by-side at desktop width and collapse without horizontal overflow at narrow width;
- Benchmark Evidence is outside the workflow card in full-width detail;
- CAUx status visibly scopes qualification to comparison and says engineering use is not authorized;
- CAUx retained detail opens/closes without changing the eight retained rows or authority wording;
- PV Elite remains Reference not available / source not retained / no invented values or tolerance.

Separately, `scripts/emp1-analytical-layout-check.mjs` currently has a deterministic test-harness initialization-order defect and must be repaired under a new Owner progression before executable closure can be complete.
