# EP-0003 — real Load case → Run → Output → Export custody

CHAIN_STATE_VERSION: 3
CHAIN_ID: ADV-LFEA-UI-CONTINUITY
STATE: ACTIVE
ENGINEERING_STATE: IN_PROGRESS
CUSTODY_STATE: HELD
QUALIFICATION_STATE: NOT_REQUIRED
WRITE_AUTHORITY: WRITE_ALLOWED
AUTO_STATE: RUNNING
MERGE_AUTHORITY: OWNER_ONLY
COMMON_PROTOCOL: engineering-pr-delivery-v2
COMMON_PROTOCOL_BASIS: 9573ec2bba234cc7cd6abcf73c4c3f3e2bc0892c
COMMON_PROTOCOL_STATUS: CURRENT
HANDOVER_READY: TRUE

## Completed material batch

The pipeline steps are now real UI tasks rather than aliases:
- Load case owns case choice + Apply selection only; no Analyze action.
- Run owns the single Analyze action and forwards only `preFlight.preparation.requestedCaseIds` to the existing `onAnalyze` callback.
- Shell UI custody keeps Output and Export unavailable until a successful retained analysis event exists; existing main/solver code is unchanged.
- Output owns result review + authority panel.
- Export owns a dedicated export summary/action and consumes the existing results panel `csvFor(active)` output without recalculating values.
- The legacy inline Output CSV action is hidden to avoid duplicate export affordances.
- Active completed steps remain visibly CURRENT while completion is retained as a separate overlay.
- Focused static continuity guard now checks the rail, task ownership, fail-closed downstream gating, CSV custody, and unchanged native execution call.

Validation truth:
- source/contract audit: PASS_STATIC;
- base→head: ahead-only, 0 behind;
- production solver/pre-flight/numerical/source/WRC files changed: NO;
- local focused/runtime execution: NOT_RUN_LOCAL because checkout failed with `Could not resolve host: github.com`;
- GitHub Actions: intentionally not used per owner instruction.

## Known test migration gap

`e2e/lfea-pipeline-accdb-real-model.spec.js` still encodes the old journey by clicking Analyze on Load case. It must be updated before this UI PR is ready. Do not restore the old conflated action just to satisfy the stale test.

## Next bounded batch

1. migrate existing LFEA E2E journeys to Load case → Error check if required → Run → Analyze → Output → Export;
2. add focused DOM assertions for six stable icons, five connectors, one Analyze on Run, no visible legacy Output export action and a distinct Export surface;
3. continue stale-icon audit inside the pipeline results/source panels (sort triangles, generic button glyphs, old affordances) and replace only shared presentation artifacts;
4. audit empty/blocked transitions for continuity and plain-language next actions;
5. open/update Draft PR and keep executable validations truthful.

## Q1–Q5

Q1: Trace the exact UI path from case checkbox → Apply selection → regenerated pre-flight → Run panel → existing `runLfeaPipelineAnalysis()` → `analysisController.analyze()` → Output result state. Where is engineering authority unchanged?

Q2: Reconstruct UI state for no applied cases, applied but unauthorized cases, authorized Run, completed analysis, and completed export. Why must Output/Export be blocked before retained analysis state?

Q3: Prove the new shell `flow` object is presentation custody only: it can suppress/reveal tasks but cannot authorize or create a pre-flight, analysis value, recovery or CSV value.

Q4: Validate that Export calls `resultsPanel.csvFor(active)` and that the native execution call remains `executionAuthority.run(authorized, { requestedCaseIds })`.

Q5: Migrate stale E2E expectations rather than reintroducing Analyze on Load case. Any proposed patch to solver/pre-flight/result numerical content is NO-PATCH for this chain.

### Handover snapshot
Repo `reallaksh19/Advanced_Analysis`; branch `codex/lfea-ui-continuity-icons`; base main `7d350c269217f3133e778c09a61b69d2b3909a64`; material head `55f4b4fa422dcc9f17965a2dd8a8d5dd4b4edf5d`; chain `ADV-LFEA-UI-CONTINUITY`; endpoint EP-0003; PR pending; merge OWNER_ONLY; AUTO RUNNING. Exact next action: migrate stale E2E journey and continue secondary icon/empty-state audit without altering engineering authority.