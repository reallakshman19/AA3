schema: engineering-pr-claims/v1
work_item: PR1291
repository: reallaksh19/Advanced_Analysis
issue: 1261
pr: 1291
claims:
  - id: AUTH-BOUNDARY-1291-001
    state: PASS_BOUNDED
    statement: Cylindrical WRC537 coefficient selection is engineering-qualified only when runtime gamma matches a source-tabulated gamma within machine round-off; this does not qualify interpolation.
  - id: IMP-SELECTOR-1291-001
    state: PASS
    statement: The selector parses 28 cylindrical coefficient tables into 321 selectable source-qualified curves and preserves one unresolved blank-gamma curve as unselectable.
  - id: IMP-SELECTOR-1291-002
    state: PASS
    statement: Original and Extrapolated variants require explicit selection and the selector never falls back across variants.
  - id: IMP-SELECTOR-1291-003
    state: PASS
    statement: A non-tabulated midpoint gamma is rejected with interpolationUsed=false and extrapolationFallbackUsed=false.
  - id: AUTH-ORACLE-1291-001
    state: PASS_REOBSERVED
    statement: WRC537 PDF page 95 independently supplies the 1A Original gamma=5 coefficient row; at beta=0.155 its frozen rational result is Y=0.10525347243142809 with semantic hash 809178dc3ef3ed4f81443777918d91048d0008fa6eeca074c93fc315691bc41e and zero production imports.
  - id: IMP-EVAL-1291-001
    state: PASS_PRODUCTION_COMPARISON
    statement: The production core exact-gamma rational evaluator reproduces the frozen independent WRC PDF oracle to 2e-15 relative-scale tolerance and rejects non-tabulated gamma, interpolation, fallback, unresolved parameter identity, source substitution, legacy polynomial semantics, and interpolation-policy substitution.
  - id: IMP-TABLE5-1291-001
    state: PASS_PRODUCTION_COMPARISON
    statement: The production cylindrical WRC Table 5 kernel reproduces all 32 qualified CAUx sustained stress outputs within source-resolution tolerances derived before production observation.
  - id: IMP-TABLE5-1291-002
    state: PASS_METAMORPHIC
    statement: Reversing all six WRC external loads reverses circumferential, longitudinal, and shear stresses while leaving Tresca stress intensity invariant.
  - id: IMP-GATE-1291-001
    state: PASS
    statement: The local-correlation gate now enforces an optional bounded exact-gamma scope against runtime source custody; non-tabulated gamma, interpolation, cross-variant fallback, unresolved source parameter identity, invalid variant, SHA substitution, and widened roundoff tolerance are blocked while unrelated unscoped gate behavior is preserved.
  - id: RISK-WRC-1291-001
    state: OPEN_FULL_DOMAIN
    statement: WRC537 does not provide a uniquely qualified programmable rule for non-tabulated cylindrical gamma selection; full-domain interpolation remains blocked.
  - id: RISK-WRC-1291-002
    state: OPEN_AFFECTED_ROW_ONLY
    statement: WRC Figure 1B Original contains one coefficient row with unresolved gamma identity; assigning gamma=10 by sequence remains prohibited.
  - id: BLOCK-DATASET-1291-001
    state: OPEN
    statement: The production route is not registered because a deterministic source-qualified 321-curve production coefficient package and dataset hash have not yet been frozen; arbitrary runtime coefficient injection must not become the production data path.
  - id: AUTH-ROUTE-1291-001
    state: BLOCKED
    statement: Global EMP.1.C route registration remains false and release is not qualified; bounded dispatch may only be considered after production dataset packaging, hash binding, and real orchestrator fail-closed proof.
