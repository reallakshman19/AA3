# EP-0002 — connected chrome complete; split Run/Output/Export next

CHAIN_STATE_VERSION: 3
CHAIN_ID: ADV-LFEA-UI-CONTINUITY
STATE: ACTIVE
ENGINEERING_STATE: IN_PROGRESS
CUSTODY_STATE: HELD
QUALIFICATION_STATE: NOT_REQUIRED
WRITE_AUTHORITY: WRITE_ALLOWED
AUTO_STATE: RUNNING
MERGE_AUTHORITY: OWNER_ONLY
COMMON_PROTOCOL: engineering-pr-delivery-v2
COMMON_PROTOCOL_BASIS: 9573ec2bba234cc7cd6abcf73c4c3f3e2bc0892c
COMMON_PROTOCOL_STATUS: CURRENT
HANDOVER_READY: TRUE

## Completed material batch

Presentation-only shared-shell continuity patch:
- six stable manifest-backed step icons;
- toolbar actions migrated from mixed inline SVGs to one icon language;
- connected step rail with COMPLETE / ACTIVE / BLOCKED / PENDING connector states;
- stable ordinal retained after completion; completion is an overlay icon, not identity replacement;
- persistent active-step context header with stage purpose and state;
- all host groups stamped with current step identity;
- focused `scripts/lfea-ui-continuity-check.mjs` added.

Validation truth: source/contract inspection PASS_STATIC. Executable Node/browser validation in this leg: NOT_RUN. Engineering authority/numerics changed: NO.

## Newly isolated continuity defect

`refreshLfeaStepGuidance()` calls `setLfeaDownstreamStepAvailability(cleared, ...)`, which makes RUN, OUTPUT and EXPORT simultaneously available as soon as pre-flight clears. `runLfeaPipelineAnalysis()` then performs analysis from the Load case panel and jumps directly to OUTPUT. `lfea-pipeline-results-panel.js` includes the CSV export inside the same surface. Therefore Run is not a real task and Export is not a distinct task.

## Exact next action

Second bounded UI batch:
1. split case selection from execution so Load case owns selection/apply only and Run owns Analyze;
2. gate RUN on an applied non-empty case selection;
3. keep OUTPUT/EXPORT blocked until an analysis result exists;
4. show result review on OUTPUT and a dedicated export summary/action on EXPORT, both reading the same retained result state;
5. mark OUTPUT complete when an analysis result is present/reviewable and EXPORT complete only after successful export custody event;
6. preserve existing solver callback, pre-flight, selected-case semantics, result tables and CSV generation.

Safe patch boundary: pipeline UI panels/controller/main presentation status only. No solver, pre-flight rules, numerical result contract, source authority, benchmark/oracle, WRC/code or workflow files.

## Q1–Q5

**Q1 — production trace.** Trace case selection from `lfea-pipeline-case-selection-panel.js` through `applyLfeaCaseSelection()` and `runLfeaPipelineAnalysis()`. Identify exactly why the current Analyze button collapses Load case and Run into one task.

**Q2 — state reconstruction.** Reconstruct step availability/completion for: pre-flight cleared with no applied cases; applied cases but not run; successful run; output review; successful CSV export. OUTPUT and EXPORT must never be READY before a retained analysis exists.

**Q3 — authority invariant.** Prove that moving the Analyze control and changing step availability is UI/session custody only: the same `analysisController.analyze(preFlight, caseIds)` call and same result state must remain untouched.

**Q4 — validation.** Define deterministic checks for one Analyze control on RUN, no Analyze control on LOAD_CASE, OUTPUT/EXPORT gating, active-step-specific result/export surfaces, and successful export completion state.

**Q5 — minimal patch.** If splitting the steps requires changing solver inputs, pre-flight authorization, result values, CSV numerical contents, source authority or code/WRC semantics, stop. The safe patch must reuse existing callbacks/state and alter only ownership/presentation of actions.

### Handover snapshot

Repo `reallaksh19/Advanced_Analysis`; chain `ADV-LFEA-UI-CONTINUITY`; endpoint EP-0002; branch `codex/lfea-ui-continuity-icons`; base main `7d350c269217f3133e778c09a61b69d2b3909a64`; PR pending; merge OWNER_ONLY. Chrome batch PASS_STATIC / runtime NOT_RUN. Exact next action: split Load case → Run → Output → Export into real UI tasks using existing analysis/result/export callbacks only.