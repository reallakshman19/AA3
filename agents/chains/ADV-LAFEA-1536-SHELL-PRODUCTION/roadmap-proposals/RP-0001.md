# RP-0001 — LAFEA roadmap observed-status refresh for shell sparse-direct path

PROPOSAL_ID: RP-0001
CHAIN_ID: ADV-LAFEA-1536-SHELL-PRODUCTION
PROPOSAL_TYPE: STATUS_REFRESH
PROPOSAL_STATUS: PROPOSED
ROADMAP_PATH: docs/conceptcumroadmapLAFEA.md
ROADMAP_BASIS_BLOB: 088f4cebfd954e5d1e37da855c95142712463a31
ROADMAP_WRITE_AUTHORITY: NONE

## Evidence

Issue #1536 identifies the active LAFEA.4 production route as dense-Cholesky-only and requires a sparse/scalable solve prerequisite. PR #1541 now contains a first bounded implementation that reuses `src/core/lafea-linear-solve/**` to provide sparse lower-symmetric global stiffness storage, prescribed-DOF elimination and sparse Cholesky for the existing CST/DKT production formulation.

The implementation deliberately leaves MITC3/MITC4, shell kinematics, recovery semantics, tolerances, registry authority and UI authority unchanged.

## Proposed roadmap status change

After executable qualification passes, refresh only the observed LAFEA.4 solver/scalability status to distinguish:

- dense small-system route retained;
- sparse direct assembly/solve route implemented;
- very-large-free-DOF production scalability still incomplete because the reused sparse Cholesky has no fill-reducing ordering and remains a local/patch-scale direct solver;
- iterative/fill-reduced shell solve remains an open follow-on prerequisite;
- MITC3/MITC4 remain benchmark-only/nonproduction.

## Expected value

Prevents the roadmap from incorrectly remaining “dense-only” after the code changes, while also preventing the new sparse-direct path from being overclaimed as full production-scale closure.

## Impact / risks

No engineering equations, source authority, benchmark oracle, release authority or owner intent change is proposed. The risk is descriptive status drift if the implementation is merged without the corresponding observed-status refresh.

## Alternatives

1. Leave roadmap unchanged: rejected as stale if PR #1541 later qualifies and merges.
2. Claim production-scale solver closure: rejected; unsupported by the current sparse Cholesky scope.

## Validation implication

Do not adopt this status refresh unless PR #1541 focused/aggregate executable checks pass on the exact head.

## Can coding continue without roadmap mutation?

YES for bounded implementation and diagnosis. NO for final merge/completion if the governing roadmap’s mandatory same-PR update rule remains applicable and no owner authorization to refresh the roadmap has been recorded.
