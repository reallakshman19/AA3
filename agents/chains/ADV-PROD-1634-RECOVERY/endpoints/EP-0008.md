# EP-0008 — latest-main reconciliation and P1 benchmark-runtime prequalification

CHAIN_ID: ADV-PROD-1634-RECOVERY
ENDPOINT_ID: EP-0008
ENDPOINT_KIND: BASE_DRIFT_RECONCILIATION_AND_READ_ONLY_PREQUALIFICATION
CUSTODY_EPOCH: 8
COMMON_PROTOCOL: engineering-pr-delivery-v2
COMMON_PROTOCOL_BASIS: 293a3db7993a6945c01adc592a7ff14a339c504a
MATERIAL_LEG_ID: LEG-002
MATERIAL_LEG_RECEIPT: agents/chains/ADV-PROD-1634-RECOVERY/material-legs/LEG-002.md
MATERIAL_HEAD: f85f6b9262fe8ae878eb30c49355af750d50f43b
MATERIAL_STATUS: IMPLEMENTED_NOT_ACCEPTED

## Owner progression

Owner command `proceed next` was received after EP-0007. This does not authorize merge of Draft PR #1639 and does not relax the fail-closed LEG-002 acceptance gate.

## Latest-main drift and bounded reconciliation

Live `main` advanced from `1aab8842759e63fe94f80438630647c166866034` to `fe57071e69b056c65ad866548056b8a097416081`, merge commit for PR #1638 (`EMP.1: add benchmark comparison custody admission contract`). The new-main delta was reviewed and does not modify the LEG-002 material paths:

- `src/workspace/emp1-workbench-product-run.js`
- `src/workspace/emp1-workbench-product-execution.js`
- `scripts/bundle-lazy-emp1-product-run-check.mjs`

The recovery branch was reconciled without force/history rewrite by creating a tree from exact latest-main tree `9c098821bca449276a7ba68598d99ead65881cef` and overlaying exactly the ten paths already present in the #1639 PR diff. Two-parent reconciliation commit:

`c86bfe2dc7cc7a0aa6562e47a295a0881e953370`

Parents:

1. prior #1639 head `2d249a36e7d6ff2666f29ebcdece0c06ee776dae`;
2. latest main `fe57071e69b056c65ad866548056b8a097416081`.

After reconciliation, GitHub reports #1639 open, Draft, `mergeable=true`, clean, with exactly the same ten changed paths. No additional engineering/material path was introduced.

## Exact-head execution observation

On reconciliation head `c86bfe2dc7cc7a0aa6562e47a295a0881e953370`:

- pull-request workflow runs: 0;
- combined commit statuses: 0;
- prior opened/reopened/synchronize observations remain zero as recorded in EP-0007.

The connected GitHub surface still cannot dispatch `workflow_dispatch`, and direct Actions settings/workflow-list endpoints are not exposed by the allowed connector route. Protected workflow YAML remains unchanged.

## LEG-002 structural source re-observation

The repository structural checker remains NOT_RUN because faithful full-file materialization/execution is unavailable. Do not label source inspection as executable checker evidence.

Read-only source inspection still confirms:

- `currentEmp1WorkbenchRouteAuthority()` remains in `emp1-workbench-product-run.js`;
- frozen function SHA-256 remains `74f6ebdacfff23d14dd12262b23535f49dc901538c4cbfa013cc458666c3d197` from EP-0007 re-observation;
- `executeEmp1WorkbenchProduct()` remains async and dynamically imports `emp1-workbench-product-execution.js`;
- lazy execution module imports the execution-only EMP.1/LAFEA dependencies and has no import back to product-run;
- `lafea-workbench-controller.js` still reads the route authority synchronously and dynamically imports the product transaction for execution;
- `vite.config.js` does not add a manual-chunk rule for `emp1-workbench-product-execution.js`;
- the production hard ceiling remains `1.125 * 1024 * 1024` bytes.

These are source observations only. EMP.1 product qualification, import checker, Rollup build/chunk bytes, served-dist Chromium boot, and exact-head CI remain NOT_RUN/BLOCKED.

## Read-only P1 candidate — lazy FEA benchmark runtime

A safer subsequent packaging seam has been prequalified for consideration **only after LEG-002 receives faithful measured execution**.

Current `src/workspace/fea-benchmark-panel.js` statically imports `CASE_STATUS`, `benchmarkCasesForSurface`, and `runBenchmarks` from `../core/fea-benchmarks/index.js`, although `FeaBenchmarkPanel.run()` is already an async API and no benchmark case is needed for the initial NOT_RUN panel render.

Call-site review establishes important compatibility constraints:

- LAFEA/LFEA controllers and the LFEA verification drawer construct the real `FeaBenchmarkPanel` synchronously.
- `runBenchmark()` delegates to `panel.run()` and must remain callable regardless of panel composition mode.
- `getBenchmarkReport()` is synchronous and must keep returning the current report/null without triggering a load.
- existing LFEA anti-drift checks explicitly require panel construction to remain available even when inline composition is disabled.
- the panel owns presentation only; expected values, tolerances, benchmark case definitions, runner semantics and numerical authority remain in `src/core/fea-benchmarks/**`.

If a later material leg is opened, the admissible seam is limited to loading benchmark execution/runtime dependencies inside the already-async `FeaBenchmarkPanel.run()` path. It must preserve:

1. synchronous panel construction, `render()`, `getReport()`, `download()` and caller APIs;
2. initial status `NOT_RUN` and existing status/report semantics;
3. exact existing benchmark case selection, ordering, expected values, tolerances, report hashes and runner code;
4. the single real `FeaBenchmarkPanel` class used by all callers — no duplicate/re-derived benchmark implementation;
5. no changes under `src/core/fea-benchmarks/**` for packaging convenience;
6. no manual-chunk forcing in `vite.config.js`;
7. no workflow, bundle-ceiling, numerical, oracle, source, route or release-authority mutation;
8. deterministic handling of concurrent `run()` calls and module-load failure without fabricating benchmark PASS/FAIL evidence;
9. measured Rollup proof that an eager dependency actually leaves the entry graph;
10. served-production browser boot and benchmark UI qualification before acceptance.

No material code change for this candidate is made in EP-0008. Its expected byte benefit is UNKNOWN until a real build measures it.

## Current disposition

- LEG-002: IMPLEMENTED_NOT_ACCEPTED.
- PR #1639: OPEN_DRAFT / MERGEABLE_TRUE_AT_RECONCILIATION / MERGE_NOT_AUTHORIZED.
- WRITE_AUTHORITY: READ_ONLY.
- AUTO_STATE: PAUSED.
- RELEASE_QUALIFIED: FALSE.
- QUESTION_SET_ID: QS-ADV-PROD-1634-RECOVERY-0002.
- QUESTION_SET_STATUS: CURRENT.
- QUESTION_PACK_ACTION: REUSED.
- QUESTION_DISPLAY: HIDE.

EXACT_NEXT_ACTION: keep #1639 Draft and unmerged. Obtain faithful exact-head LEG-002 structural/product/import/build/chunk/served-browser execution. Only after measured LEG-002 disposition may a new material leg be opened; if size remains above the unchanged ceiling, use this prequalified benchmark-runtime seam as the next packaging candidate subject to write-ahead custody and exact-head validation. UI/layout/manual/published-benchmark obligations remain open and release remains HOLD.
