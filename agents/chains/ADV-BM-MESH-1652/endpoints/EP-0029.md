# EP-0029 — M4 physics/response fixture accepted / solver execution protected

STATUS: ACCEPTED_LEG_016_M4_FIXTURE_FROZEN_SOLVER_EXECUTION_PROTECTED
DATE_UTC: 2026-09-07
PREDECESSOR_ENDPOINT: EP-0028
PREWORK_ENDPOINT: EP-0028
MATERIAL_LEG: LEG-016
MATERIAL_HEAD: b0943ae5dc605af745d60eea0cd955e569c1cae8

CHAIN_ID: ADV-BM-MESH-1652
WORK_ITEM_KEY: github:reallaksh19/Advanced_Analysis#1652
REPO: reallaksh19/Advanced_Analysis
PR: 1663
BRANCH: engineering/bm-mesh-1652-m2-data

ENGINEERING_STATE: M4_PHYSICS_RESPONSE_FROZEN_SOLVER_CONVERGENCE_EXECUTION_PROTECTED
WRITE_AUTHORITY: READ_ONLY_AT_M4_EXECUTION_BOUNDARY
MERGE_AUTHORITY: OWNER_ONLY
MERGE_AUTHORIZED: FALSE

## Accepted LEG-016 result

BM-MESH now has a frozen, source-cited M4 fixture for both production mesh families:

- LAFEA.3 reuses the independently frozen B01 plane-stress affine field/material and the existing continuum physical-probe contract;
- LAFEA.4 reuses the existing prescribed membrane patch field/material and a bounded CST in-plane physical-point UX recovery rule;
- only `STRAIN_ENERGY` and `SELECTED_DISPLACEMENT` are admitted as M4 convergence quantities;
- existing production default limits are retained with no overrides;
- all selected-displacement identities remain physical coordinates, never node IDs;
- raw singular peak stress, moving maxima and cross-element stress averaging remain forbidden.

Source custody is pinned through S-030, including both solver public contracts and the frozen M4 fixture.

Analytical reference values in the fixture are diagnostic closed-form references. M4 acceptance is not redefined by those values; it remains the existing `requireSufficientMeshLevels` + `qualifyConvergenceSet` behavior.

## Execution status

No solver/compiler was executed in LEG-016. The staged runner was not changed. No M4 response history exists yet and no M4 PASS is claimed.

M0-M3 also remain static-not-run in this PR lineage, so M4 cannot execute until the exact execution head first establishes a contiguous M0-M3 PASS prefix.

## Remaining protected work

The next material batch would require explicit Owner progression for:

- wiring the frozen M4 fixture into `scripts/lafea-mesh-benchmark-run.mjs`;
- constructing canonical continuum/shell solver models from each production mesh level;
- executing `calculateLocalContinuum` / `calculateLocalShell`;
- retaining physical-point recovery evidence and three-level response histories;
- calling `requireSufficientMeshLevels` and `qualifyConvergenceSet`;
- performing a clean exact-head M0-M4 run if an eligible execution environment is available.

PR #1663 rebase/conflict resolution and merge remain separately Owner-only. Release and temperature authority remain false.

CURRENT_BLOCKER: M4 runner wiring plus solver/convergence execution authority; PR #1663 rebase/merge remains separately protected.
EXACT_NEXT_ACTION: await explicit Owner progression for the M4 execution batch, or separately explicit PR rebase/merge authority. Do not infer either from the LEG-016 fixture-freeze authorization.
