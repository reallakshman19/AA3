# EP-0011 — Shared CST/DKT↔MITC qualification authored; production-route baseline pre-work

CHAIN_STATE_VERSION: 3
CHAIN_ID: ADV-LAFEA-1536-SHELL-PRODUCTION
ENDPOINT_ID: EP-0011
PARENT_ENDPOINT: EP-0010
CUSTODY_EPOCH: 11
MATERIAL_BATCH_HEAD: 72f1ae50ac16a4784270ea95d27d854cba93ac7a
COMMON_PROTOCOL: engineering-pr-delivery-v2
COMMON_PROTOCOL_BASIS: 9573ec2bba234cc7cd6abcf73c4c3f3e2bc0892c
COMMON_PROTOCOL_STATUS: CURRENT
ROADMAPS: docs/conceptcumroadmapLAFEA.md@088f4cebfd954e5d1e37da855c95142712463a31
ROADMAP_REVIEW_STATUS: COMPLETE
HANDOVER_READY: TRUE

## Material result

The experimental MITC adoption package now has an end-to-end shared-physics qualification script without changing either formulation to force agreement.

`scripts/lafea.4-mitc-adoption-shared-qualification-check.mjs` runs:

- the actual registered legacy route through `createCanonicalLocalShellModel()` -> `calculateLocalShell()` on TRI3 CST/DKT meshes;
- the explicit experimental route through MITC adoption model -> solve -> recovery on QUAD4 meshes;
- a common 32-cell cantilever strip with `E=200000 MPa`, `nu=0`, `L=10`, `b=1`, `P=1`;
- thin `t/L=0.01`: each route is compared independently against its analytical limit, with negligible Timoshenko shear fraction and common thin-limit convergence required;
- moderate `t/L=0.2`: CST/DKT is checked against Euler-Bernoulli/Kirchhoff bending, MITC against Timoshenko/Reissner-Mindlin total deflection, and MITC must remain measurably more flexible because the analytical shear fraction exceeds 2%;
- an affine plane-stress membrane state shared by both routes;
- a fully-fixed pressure patch where formulation-specific nodal shares may differ but total traction force, first moment and support equilibrium must be identical.

The benchmark never uses one FE route as the other's sole oracle and does not tune shear correction, tolerances or mesh per formulation.

## Validation truth

All new adoption checks remain `NOT_RUN` in the current environment. No PASS is claimed. The parent exact-head runner has historically failed before step 1, and no current executable artifact supersedes that truth.

No production mechanics/result contract, registry, presenter, UI, roadmap or workflow file changed.

## Next bounded material batch — issue-required production-route baselines

Add named CST/DKT production-route benchmarks required by Issue #1536. These are anti-regression evidence for the route that the application actually runs today and must call `calculateLocalShell()` directly:

- `LAFEA4-CYL-01` — curved/cylindrical shell patch with retained local/global basis and pressure/equilibrium evidence;
- `LAFEA4-PRESS-01` — planar uniform-pressure patch with exact applied force/first-moment/reaction oracle;
- `LAFEA4-COMB-01` — combined membrane/bending/pressure or force-moment case exercising solve, surface recovery, equilibrium and energy in one retained result.

Do not copy expected values from current production output without an independent derivation. Prefer analytical invariants/free-body values and existing independently frozen benchmark quantities. No MITC production authority follows from these CST/DKT baselines.

### Handover snapshot

Repo: `reallaksh19/Advanced_Analysis`
Task: #1536 — LAFEA.4 production/adoption qualification
Chain / endpoint: `ADV-LAFEA-1536-SHELL-PRODUCTION` / `EP-0011`
PR: #1542 Draft stacked on #1541
Branch / material head: `codex/lafea-1536-mitc-adoption` / `72f1ae50…`
Merge authority: OWNER_ONLY
State: IN_PROGRESS / HELD / NOT_REQUIRED / WRITE_ALLOWED / AUTO RUNNING
Roadmap: `docs/conceptcumroadmapLAFEA.md@088f4ceb…`
Inputs: current `calculateLocalShell()` production route + independent analytical/free-body oracles
Benchmarks: shared CST/DKT↔MITC physics script; existing shell checks
Blocker: executable qualification NOT_RUN
Exact next action: add `LAFEA4-CYL-01`, `LAFEA4-PRESS-01`, `LAFEA4-COMB-01` direct production-route checks only.

Q1: Trace each named benchmark through `calculateLocalShell()` and identify exactly which production element/load/solver/recovery owners it exercises.
Q2: Hand-reconstruct at least two expected force/moment/stress/energy values before encoding them.
Q3: Prove benchmark fixtures are independent evidence and do not become runtime engineering defaults or formulation authority.
Q4: Define failure controls for basis reversal, pressure sense, equilibrium and non-finite/unqualified output without weakening tolerances.
Q5: Define the minimal benchmark-only patch and the stop condition after which no more mechanics coding is permitted before executable qualification.

### Takeover qualification pack

#### Q1 — Production trace
Anchors: `calculate.js`, `element.js`, `loads.js`, `solver.js`, `recovery.js`, production shell fixtures/checks.
Required reconstruction: for each of the three case IDs, trace canonicalization, element construction, load assembly, global assembly/solve, reaction/equilibrium and recovered surface evidence. Show that the case calls `calculateLocalShell()` rather than `mitc*StiffnessMatrix()` or another benchmark-only helper.
Fail if: a benchmark bypasses the registered kernel calculation boundary.

#### Q2 — Numerical hand oracles
`PRESS-01`: derive `F=pA n`, centroid first moment and fully-fixed `R=-F`. `CYL-01`: derive pressure resultant/equilibrium and at least one geometry/basis invariant independent of solver output. `COMB-01`: derive applied force/moment totals plus one closed-form membrane or prescribed-curvature surface stress/energy component.
Fail if: frozen production output is the only expected value.

#### Q3 — Authority invariant
Benchmark fixtures/checks are independent test evidence only. They may not supply runtime material, loads, tolerances, registry state, result authority or release authorization. Existing CST/DKT formulation remains unchanged; MITC remains experimental.
Invalid shortcut: import benchmark expected values into production or change production to satisfy a fixture-specific number.

#### Q4 — Independent falsifiers
Reverse pressure sense -> force/moment sign reversal; reverse director/orientation coherently -> physical response transforms consistently; fully-fixed case -> exact reaction balance; rejected/non-finite result must never be admitted as a benchmark PASS. Existing tolerance owners remain unchanged.

#### Q5 — Minimal patch / stop boundary
Safe boundary: one focused benchmark script and, if useful, script-only fixture helpers. No production, schema, registry, presenter, UI, roadmap or workflow changes. After these cases are authored, mechanics implementation stops until executable adoption and parent solver gates run. NO-PATCH if an expected numerical value cannot be derived independently enough to serve as an oracle; record it as comparison-only evidence instead of freezing production output.
