# EP-0019 — pre-mutation: walkthrough evidence must prove viewport landing

CHAIN_ID: ADV-EMP1-WRC-UI-WALKTHROUGH
PREVIOUS_ENDPOINT: agents/chains/ADV-EMP1-WRC-UI-WALKTHROUGH/endpoints/EP-0018.md
REASON: PRE_MUTATION_VALIDATION_SUPPORT_DEFECT
ISSUE: #1559 OPEN
PR: #1571 DRAFT
BRANCH: codex/emp1-wrc-ui-1559-postmerge
BASE_MAIN: 541e5ad6078e811c55e1c426f1e8ca7a34356a61

## Observed defect

The current evidence harness captures every checkpoint with `page.screenshot({ fullPage: true })` only.

Issue #1559 explicitly requires inspection of scroll position and transitions. A full-page screenshot is useful for hierarchy/context but does not preserve the user's actual viewport landing after navigation. Therefore the current evidence bundle cannot independently demonstrate whether a professional-step click landed the engineer on the intended screen region.

This is an acceptance-evidence defect only. No production WRC UI or numerical behavior is implicated.

## Bounded repair

- Keep the same 12 user-flow checkpoints.
- At each checkpoint attach a viewport screenshot that preserves the actual user-visible landing.
- Also attach a full-page context screenshot for hierarchy/duplicate-control/terminology review.
- Record current `scrollX`, `scrollY`, viewport width and viewport height in the walkthrough manifest.
- Update the human-review rubric so scroll-position review uses the viewport attachment; full-page attachment is supporting context only.

## Falsifiers / protected authority

- Do not change `src/workspace/**`, `src/core/emp1/**`, WRC source/dataset/oracle/tolerances, bounded-route authorization, code compliance, release/deployment, or `.github/workflows/**`.
- Do not alter the 12-step workflow behavior merely to make screenshots easier.
- Chromium execution and human observation remain NOT_RUN unless actually executed.

## Validation target

- exact committed spec syntax PASS
- 12 viewport checkpoint attachments + 12 full-page context attachments
- manifest records viewport/scroll metadata for each checkpoint
- branch remains relay/docs/e2e only

HANDOVER_READY: TRUE
