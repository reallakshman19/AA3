# EP-0005 — LEG-002 implemented, executable acceptance blocked

CHAIN_ID: ADV-PROD-1634-RECOVERY
ENDPOINT_ID: EP-0005
ENDPOINT_KIND: POST_MATERIAL_RELAY
CUSTODY_EPOCH: 5
COMMON_PROTOCOL: engineering-pr-delivery-v2
COMMON_PROTOCOL_BASIS: 293a3db7993a6945c01adc592a7ff14a339c504a
MATERIAL_LEG_ID: LEG-002
MATERIAL_LEG_RECEIPT: agents/chains/ADV-PROD-1634-RECOVERY/material-legs/LEG-002.md
MATERIAL_HEAD: f85f6b9262fe8ae878eb30c49355af750d50f43b
MATERIAL_STATUS: IMPLEMENTED_NOT_ACCEPTED

## Current engineering state

LEG-002 implements a source-level lazy boundary for the heavy EMP.1 product transaction without moving or re-deciding the existing `currentEmp1WorkbenchRouteAuthority()` owner. The public `executeEmp1WorkbenchProduct()` API remains async and now dynamically loads `emp1-workbench-product-execution.js`. The transaction implementation receives the existing live authority resolver by callback, so the lazy module has no import back to the eager owner.

No core EMP.1 numerical/registry/applicability/route authority, LAFEA shell mechanics, spring-rate authority, Vite manual chunk rule, workflow, bundle ceiling, benchmark oracle/tolerance or release authority changed.

## Validation disposition

Source inspection supports the intended boundary and the authority function is frozen by a pre-LEG source hash guard. However the exact-head repository runtime is unavailable and GitHub reports zero Actions runs on the material head. Therefore:

- structural checker execution: NOT_RUN;
- EMP.1 product qualification: NOT_RUN;
- method-authority projection check: NOT_RUN;
- import checker: NOT_RUN;
- production build/chunk measurement: NOT_RUN;
- served-dist browser boot: NOT_RUN;
- exact-head CI: NOT_RUN.

The last executable historical build remains only the profiling basis: ~1,834,272 B entry JS vs the unchanged 1,179,648 B ceiling. The new split's actual byte reduction is not yet measured.

## Risk / stop condition

The eager authority owner still imports the authorized WRC route module, which itself has qualified WRC execution dependencies. If a faithful build shows insufficient byte reduction, the next step must identify another packaging-only seam; it must not duplicate route authority, change numerical authority or raise the bundle ceiling.

WRITE_AUTHORITY: READ_ONLY
AUTO_STATE: PAUSED
MERGE_AUTHORITY: OWNER_ONLY
MERGE_AUTHORIZED: FALSE
RELEASE_QUALIFIED: FALSE
QUESTION_SET_ID: QS-ADV-PROD-1634-RECOVERY-0002
QUESTION_SET_STATUS: CURRENT
QUESTION_PACK_ACTION: REUSED
QUESTION_DISPLAY: HIDE

EXACT_NEXT_ACTION: open a Draft PR for LEG-002, keep merge unauthorized, and seek/consume faithful exact-head execution. If measured build + browser boot pass and size improvement is sufficient, accept LEG-002 and continue P1 profiling if needed. If the lazy boundary fails or regresses EMP.1 behavior, revert/re-isolate before any new material leg.
