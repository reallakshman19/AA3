# LEG-006 — analytical-layout checker declaration-order repair

CHAIN: ADV-EMP1-HUMAN-UI-1651
WORK_ITEM_KEY: github:reallaksh19/Advanced_Analysis#1651
PREWORK_ENDPOINT: EP-0007
QUALIFICATION_SCOPE_ID: QSCOPE-1651-EMP-EQUIVALENCE-CLOSURE
QUESTION_SET_ID: QS-ADV-EMP1-HUMAN-UI-1651-0005
QUESTION_PACK_ACTION: REUSED
QUESTION_DISPLAY: HIDE
OWNER_TEXT_OBSERVED: `fix, proceed next`
OWNER_PROGRESSION_INTERPRETATION: explicit repair authorization plus one bounded proceed-next progression

MATERIAL_BASE: dec0a757f41346f0609551dddd97c8df5b4029cc
MATERIAL_HEAD: 3a8b4e7241ed341deb4f54175ad14f893a7af241
MATERIAL_FILES:
- scripts/emp1-analytical-layout-check.mjs

## Trigger

Owner/local validation at LEG-005 material head `9910d2f9...` proved the layout checker itself was invalid: top-level `new FakeDocument()` executed before the class declaration was initialized, producing `ReferenceError: Cannot access 'FakeDocument' before initialization` before any layout assertion ran.

## Repair

Move the existing `FakeDocument` and `FakeNode` class declarations above the first construction of `FakeDocument`.

No assertion text, expected surface order, region count, duplicate-node falsifier, missing-key falsifier, production renderer, layout registry, CSS or engineering projection is changed.

Material compare `dec0a757... -> 3a8b4e72...` is exactly one file with 34 lines moved up and 34 lines removed from the former trailing location.

## Main drift classification

Live `main` advanced from the PR's historical base `b39f7673...` to `80f335b7...` before LEG-006. The drift is 66 commits but its changed-file set is confined to `ADV-BM-MESH-1652` custody artifacts and meshing-check scripts. There is no overlap with EMP.1/layout/benchmark paths or the repaired checker. Classification: `NON_MATERIAL_TO_LEG006_SCOPE`.

## Validation truth

PASS:
- source/diff inspection of the declaration-order repair;
- no production `src/**` material change;
- PR #1660 returned mergeable after GitHub recomputation.

PENDING_EXTERNAL_REVALIDATION:
- `node scripts/emp1-analytical-layout-check.mjs` at `3a8b4e72...`.

Retained external PASS from prior target:
- plain-language labels;
- governed vector table;
- professional workflow;
- benchmark evidence static contract;
- issue-1651 static acceptance manifest;
- import check;
- `git diff --check`.

Retained blockers:
- Playwright `BLOCKED_ENVIRONMENT` because project-local Chromium is absent;
- `npm run build` Vite phase completed but bundle budget failed; causality requires a current-main comparator and must not be inferred.

## No-patch authority boundary

No production source, WRC/Pressure mechanics, benchmark source/value/tolerance, route authority, code/release state, roadmap text or `.github/workflows/**` change.
