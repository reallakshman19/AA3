# EP-0011 — HANGER default/applied case identity pre-work

CHAIN_STATE_VERSION: 3
CHAIN_ID: ADV-LAFEA-SUPPORT-REPRESENTABILITY
ENDPOINT_ID: EP-0011
PARENT_ENDPOINT: EP-0010
CUSTODY_EPOCH: 11
PR: #1553 DRAFT
BRANCH: codex/lfea-support-representability-1551
BASIS_HEAD: 8def0c142df4976a2854ce2b79669853daada02f
LIVE_MAIN_AT_REGROUND: 94b766d0deb8afb25451d9cf0d5c7d61d63d2b4d
COMMON_PROTOCOL: engineering-pr-delivery-v2
COMMON_PROTOCOL_BASIS: 9573ec2bba234cc7cd6abcf73c4c3f3e2bc0892c
COMMON_PROTOCOL_STATUS: CURRENT
ENGINEERING_STATE: IN_PROGRESS
CUSTODY_STATE: HELD
QUALIFICATION_STATE: NOT_REQUIRED
WRITE_AUTHORITY: WRITE_ALLOWED
AUTO_STATE: RUNNING
MERGE_AUTHORITY: EXPLICIT_APPROVED_MERGE_ONLY
MERGE_TRIGGER: APPROVED MERGE
HANDOVER_READY: TRUE
QUESTION_SET_ID: QS-ADV-LAFEA-SUPPORT-REPRESENTABILITY-0011
QUALIFICATION_BASIS_HEAD: 8def0c142df4976a2854ce2b79669853daada02f

## EP-0010 batch result

Load-case presentation state is now source-bound by the sealed source semantic hash. Explicit checkbox custody and stale status/error prose are cleared on source replacement, while same-source case application/regeneration preserves the engineer's explicit selection. Hanger Run custody fails closed if source identity is unavailable. The focused aggregate includes a same-case-ID/different-source transfer test and deliberate-break mode.

Executable status remains `NOT_RUN_RUNTIME`. Current Actions evidence again shows the deterministic LFEA promotion job concluded failure with an empty step list and BM4_L skipped; no execution PASS/FAIL is inferred.

## Newly isolated default/applied identity defect

For a hanger model with no explicit checkbox edit, the controller displays the governed H-bearing default family (`WH/WPH/WTH/WPTH`). The current Run custody predicate accepts the untouched state if `appliedCaseIds.some(caseId => hangerIds.includes(caseId))`.

That predicate is too weak. Example after controller remount/reconstruction:

- displayed selected default: `WH/WPH/WTH/WPTH`;
- sealed applied set: `WH` only;
- `selectionExplicit=false`;
- current predicate: ready because `WH` is one H-bearing case.

The engineer would see four selected cases but Run would execute one. This is custody mismatch, not mechanics.

## Safe next batch

For hanger models, untouched/default state must be Run-ready only when the complete selected default case set equals the complete applied set, order-insensitively. Reuse the existing `sameCaseIds()` comparison.

Required behavior:

- native applied `W` vs default H family: blocked;
- applied full H default family vs displayed full H default family: ready;
- applied subset/superset of displayed default: blocked;
- explicit selection path remains exact selected-vs-applied comparison as already implemented;
- no-H legacy behavior remains unchanged.

## Validation design

Extend `scripts/lfea-hanger-case-selection-check.mjs` with untouched/default hanger cases:

- `WH` only applied while four H defaults display -> blocked;
- full four H defaults applied -> ready;
- deliberate-break restoring `some(H)` acceptance must turn the check red.

## Protected exclusions

No source/intake case default change, physical-case compilation, HANGER source classification, rate/preload arithmetic, Run numerical execution, solver, BM4 parity/reference/tolerance/sign/rows, reducer mechanics, code allowables, workflows or owner roadmap.

## Exact next action

Tighten the untouched HANGER Run-custody predicate to exact set identity and extend the existing aggregate-focused check. Source-audit, then freeze a successor endpoint. PR remains DRAFT and must not merge without exact `APPROVED MERGE`.
