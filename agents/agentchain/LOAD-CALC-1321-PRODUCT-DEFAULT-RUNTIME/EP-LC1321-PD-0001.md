# EP-LC1321-PD-0001 — Product engineering defaults ordinary-Run runtime gap

CHAIN_ID: LOAD-CALC-1321-PRODUCT-DEFAULT-RUNTIME
LEG_ID: LEG-A
ENDPOINT_ID: EP-LC1321-PD-0001
PREVIOUS_ENDPOINT: NONE — chain start

CREATED_AT: 2026-08-28T00:50:45Z
ENDPOINT_REASON: PRE_MUTATION

TASK / ISSUE: Advanced_Analysis #1321 — zero-blocker configurable-default architecture with unified effective-value resolution
PR: PENDING
BRANCH: agent/issue-1321-product-engineering-default-runtime

CHECKPOINT_HEAD: 81c2e780a03d565345457b07609fe19d8d1421e4
MAIN_HEAD_OBSERVED: 81c2e780a03d565345457b07609fe19d8d1421e4
MERGE_BASE: 81c2e780a03d565345457b07609fe19d8d1421e4

STATE: ACTIVE

### Mission

Connect the already-existing versioned target-level `PRODUCT_DEFAULT` engineering provider to the ordinary current Common Input field-resolution path required by Issue #1321, without inventing any OD, wall, density, insulation, fluid, or component-mass values and without creating a second resolver.

### This leg completed

- Re-grounded current `main` and the live Issue #1321 Definition of Done.
- Read current root `AGENTS.md`, repo-wide `agents/agentchain.md`, and live Common engineering-pr-delivery-v2.
- Confirmed there is no open #1321 PR.
- Confirmed current `buildCurrentPreFeaRequestInput()` composes accepted enrichment plus `PROJECT_CONFIGURED_DEFAULT` records only.
- Confirmed `createNonFeaProductEngineeringDefaultProvider()` exists but is not consumed by the ordinary Common Input runtime.
- Confirmed the core `NON_FEA_ENRICHMENT_AUTHORITIES` and field authority rules omit `PRODUCT_DEFAULT`, despite the workspace field registry already defining `PRODUCT_DEFAULT` as the lowest default authority for eligible fields.
- Confirmed the shipped target-level engineering Product-default profile is intentionally empty and must remain so in this leg.
- Coordination classification: engineering paths SAFE relative to open EMP.1 PR #1502; `agents/agentchain.md` is COORDINATION_REQUIRED because both v2 chains update the compact index.

### Currently in progress

No engineering production mutation has occurred yet. The branch contains only this durable pre-mutation relay endpoint and the associated repo-wide index update.

### Remaining work

- Add `PRODUCT_DEFAULT` as the lowest target-level default authority in the existing core enrichment resolver for fields already permitted by Issue #1321 / the field registry.
- Inject the existing Product engineering-default provider into the ordinary Common Input ephemeral resolver evidence bundle after Project configured defaults.
- Preserve the empty shipped engineering Product-default table; no new engineering values are authorized by this leg.
- Add a focused regression proving source/master/project-default precedence over Product default and Product-default selection only when higher authority is absent.
- Register the focused check without dropping newer Non-FEA checks.
- Re-ground against live `main` and preserve any concurrent v2 relay-index rows before PR review.

### Exact next action

Patch `src/core/non-fea-enrichment/index.js` and `src/workspace/non-fea-common-input-runtime.js` so the existing versioned Product engineering-default records enter the same field-resolution ledger strictly below `PROJECT_CONFIGURED_DEFAULT`, then add a focused precedence/runtime-wiring regression.

### Known / proven

- Issue #1321 explicitly requires first-class target-level `PRODUCT_DEFAULT` authority below `PROJECT_CONFIGURED_DEFAULT`.
- Workspace `non-fea-field-registry.js` already permits `PRODUCT_DEFAULT` for the default-eligible engineering fields.
- Core `non-fea-enrichment/index.js` currently omits `PRODUCT_DEFAULT` from `NON_FEA_ENRICHMENT_AUTHORITIES` and from default authority arrays.
- Ordinary `buildCurrentPreFeaRequestInput()` currently bundles only accepted enrichment and Project configured-default records before `resolveNonFeaEnrichment()`.
- `LOAD_CALC_ENGINEERING_PRODUCT_DEFAULTS_EMPTY_V1` contains zero target-level engineering defaults by design.
- Open PR #1502 touches EMP.1 engineering files and `agents/agentchain.md`, but none of this chain's planned engineering production/test paths.

### Not proven

- Executable correctness of the planned resolver/runtime changes.
- Aggregate Non-FEA regression status after the planned changes.
- Import/build validity on the future implementation head.
- Any authorization for actual generic OD/wall/density/component-mass Product values.

### NOT_RUN

- Focused Product-default resolver/runtime regression — NOT_RUN, not yet authored.
- `node scripts/run-non-fea-checks.mjs` — NOT_RUN.
- `npm run check:imports` — NOT_RUN.
- `node scripts/advanced-shell-contract-check.mjs` — NOT_RUN.
- `npm run build` — NOT_RUN.
- `git diff --check` — NOT_RUN.

### Active hypothesis

Issue #1321 target-level Product engineering defaults are structurally implemented but dead on the ordinary Run path because the core resolution contract rejects `PRODUCT_DEFAULT` records and the current Common Input runtime never adds the Product provider records to its ephemeral resolver sidecar.

### Falsifier

A live current-main trace showing `buildCurrentPreFeaRequestInput()` already passes `createNonFeaProductEngineeringDefaultProvider(...).records` into the same `resolveNonFeaEnrichment()` sidecar, with core `createNonFeaEnrichmentRecord()` accepting `PRODUCT_DEFAULT` and candidate precedence placing it below `PROJECT_CONFIGURED_DEFAULT`, would falsify this hypothesis.

### Protected invariants

- One effective-value / field-resolution authority path only; do not add another resolver.
- `SOURCE_EXPLICIT`, `SOURCE_INHERITED`, `EXACT_APPROVED_MASTER`, `ACCEPTED_OVERRIDE`, and `CONFIGURED_DERIVATION` precedence must not be weakened.
- `PROJECT_CONFIGURED_DEFAULT` must outrank `PRODUCT_DEFAULT`.
- Invalid explicit values must not silently fall through to a Product default.
- The shipped engineering Product-default table remains empty in this leg.
- No support-load statics, route allocation, CoG mechanics, source-axis mechanics, equilibrium formulas, solver/tolerance, WRC/EMP.1/LAFEA authority, or workflow files may change.
- `NOT_RUN` never becomes PASS by source inspection.

### Do not redo

- Do not recreate D3 effective-value inspection, D4 defaults observability, one-click Run routing, explicit-moment retention, gravity/load-factor binding, hand-calculation parity, active-load-case disclosure, or X/Y/Z scalar-gravity qualification already merged for #1321.
- Do not create generic engineering defaults to make a test green.

### Do not change

- `.github/workflows/**`
- `src/workspace/engineering-loads/support-load-distribution-v3.js`
- WRC/EMP.1/LAFEA production/source/validation domains
- benchmark expected values unrelated to this authority integration
- `LOAD_CALC_ENGINEERING_PRODUCT_DEFAULTS_EMPTY_V1` contents

### Expected next-leg files / domains

- `src/core/non-fea-enrichment/index.js`
- `src/workspace/non-fea-common-input-runtime.js`
- `scripts/non-fea-product-engineering-default-runtime-resolution-check.mjs` (new)
- `scripts/run-non-fea-checks.mjs`
- relay endpoint/index files only

### Inputs

- `reallaksh19/Advanced_Analysis` Issue #1321, live owner-authored requirements.
- `Advanced_Analysis@81c2e780a03d565345457b07609fe19d8d1421e4`.
- `AGENTS.md` at the same main head.
- `agents/agentchain.md` at the same main head.
- Open PR #1502 changed-file inventory for coordination.

### Benchmarks

- `scripts/non-fea-product-engineering-default-profile-check.mjs` — existing provider/profile contract.
- `scripts/non-fea-configured-default-resolution-check.mjs` — existing common configured-default resolution behavior.
- `scripts/non-fea-common-enriched-effective-default-authoring-check.mjs` — existing authoring seam showing Project-default then Product-default composition before publication.
- NONE independent numerical benchmark is required for this authority-plumbing leg; no numerical mechanics are changed.

### Common / governing documents

- `reallaksh19/Common@10d667ce715bb52e1f73035c6fa326db77d0f9dd/skills/engineering-pr-delivery-v2/SKILL.md`.
- `reallaksh19/Common@10d667ce715bb52e1f73035c6fa326db77d0f9dd/skills/engineering-pr-delivery-v2/references/agentchain-schema.md`.
- Advanced_Analysis root `AGENTS.md` @ `81c2e780a03d565345457b07609fe19d8d1421e4`.

### Authoritative sources

- Advanced_Analysis Issue #1321 owner-authored authority order and Definition of Done.
- NONE external code/standard source — this leg changes software authority plumbing only and introduces no engineering numeric assumptions.

### Production paths

- `src/core/non-fea-enrichment/index.js`
- `src/workspace/non-fea-common-input-runtime.js`
- `src/workspace/project-data/non-fea-field-registry.js`
- `src/workspace/project-data/non-fea-product-engineering-default-profile.js`
- `src/workspace/enrichment/non-fea-common-enriched-effective-default-authoring.js`

### Validation / test paths

- `scripts/non-fea-product-engineering-default-profile-check.mjs`
- `scripts/non-fea-configured-default-resolution-check.mjs`
- `scripts/non-fea-common-enriched-effective-default-authoring-check.mjs`
- planned `scripts/non-fea-product-engineering-default-runtime-resolution-check.mjs`
- `scripts/run-non-fea-checks.mjs`

### Changed during this leg

- Created branch `agent/issue-1321-product-engineering-default-runtime` from exact main `81c2e780a03d565345457b07609fe19d8d1421e4`.
- Created this immutable pre-mutation endpoint.
- Updated the compact relay index to register the active chain; no engineering production/test file has yet changed.

### Validation summary

- Live source trace of the missing ordinary-Run Product-default seam: PASS by source inspection.
- Issue-authority comparison against field registry/core resolver/runtime: PASS by source inspection.
- Open-PR engineering-path overlap with #1502: SAFE; relay-index overlap: COORDINATION_REQUIRED.
- Executable checks: NOT_RUN.

### Open risks / questions

- A concurrent merge of PR #1502 will move `main` and `agents/agentchain.md`; later reconciliation must preserve its chain row/log exactly.
- The core enrichment field list is narrower than the workspace registry; this leg must not opportunistically expand unrelated field support.
- Actual Product engineering values require a separately qualified/authorized product table and are outside this leg.

### Next-agent qualification

QUALIFICATION_BASIS_HEAD: 81c2e780a03d565345457b07609fe19d8d1421e4
QUESTION_SET_ID: QS-LC1321-PD-RUNTIME-0001
QUESTION_SET_STATUS: CURRENT

#### Q1 — Production Trace

Trace the live ordinary path from `buildCurrentPreFeaRequestInput()` through `createNonFeaEnrichmentSidecar()` and `resolveNonFeaEnrichment()`, and identify exactly why a record emitted by `createNonFeaProductEngineeringDefaultProvider()` cannot currently become a selected target-level resolver candidate.

#### Q2 — Current Unresolved Problem / Failure Isolation

Using `PIPE_OUTER_DIAMETER` as the concrete field, identify the minimum two production seams that must change for a versioned Product default to participate in ordinary Run, and identify the first current function that would reject a `PRODUCT_DEFAULT` enrichment record before the patch.

#### Q3 — Authority / Invariant

Reconstruct the required authority order for a target-level defaultable field and prove that `PROJECT_CONFIGURED_DEFAULT` must beat `PRODUCT_DEFAULT` while invalid higher-authority explicit evidence must remain fail-closed rather than falling through.

#### Q4 — Independent Validation

Design a falsifier with three candidate sets for one entity/field: source + Project default + Product default; Project default + Product default; Product default only. Predict the selected authority/value in each case and identify which output would prove the patch reversed precedence.

#### Q5 — Next Contribution / Minimal Patch

Name the smallest production/test file set that connects the existing Product engineering provider to ordinary Common Input without adding numeric defaults, changing statics, or creating another resolver; explain why each proposed file is necessary.