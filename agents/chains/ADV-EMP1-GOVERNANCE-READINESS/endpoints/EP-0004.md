# EP-0004 — Engineer-Facing Readiness Dashboard Integration

STATUS: MATERIAL_COMPLETE_FOCUSED_CHECKS_NOT_RUN_EXISTING_WRC_GATES_FAIL
DATE_UTC: 2026-09-01

## Exact state

BASE_MAIN: 70dd23a4fb36533f00d818586d1b753fa4f12276
PR: 1614
PR_STATUS: OPEN_DRAFT
BRANCH: agent/emp1-governance-readiness-v1
MATERIAL_HEAD: 52de0672aaeb244471538b851ff263ef21ee0841
MERGEABILITY: TRUE_AT_LAST_READ
MERGE_AUTHORITY: OWNER_ONLY
MERGE_AUTHORIZED: FALSE

## New material slice

```text
src/workspace/emp1-professional-workflow-view.js
scripts/emp1-readiness-dashboard-check.mjs
```

The previously added core projection and focused projection checker remain:

```text
src/core/emp1/emp1-readiness-projection.js
scripts/emp1-readiness-projection-check.mjs
```

## Integration contract

`renderEmp1ProfessionalWorkflow(...)` now invokes the existing core-owned:

```text
projectEmp1Readiness(projection)
```

exactly once per render and presents the resulting states as an engineer-facing `Engineering readiness` dashboard.

Displayed dimensions are:

1. Source & input
2. Bounded method
3. Applicability
4. Calculation
5. Engineering review
6. Code compliance
7. Release qualification

The dashboard also presents existing blocker codes when the core projection supplies them.

## Authority boundary

The DOM/view layer does not:

- evaluate WRC geometry or method-domain applicability;
- decide bounded-route authorization;
- execute `runEmp1()`;
- calculate or reconstruct WRC stress/intensity values;
- create semantic/currentness/authority hashes;
- assess engineering review completion;
- assess code compliance;
- decide release qualification.

Those states remain owned by the existing governed contracts and the new core readiness projection merely composes their evidence.

The view explicitly states that it does not establish method authority, applicability authority, engineering review, code compliance, or release qualification.

## Focused dashboard checker

Encoded command:

```bash
node scripts/emp1-readiness-dashboard-check.mjs
```

The checker contains state falsifiers for:

- current bounded-C result;
- ready-to-calculate pre-execution state;
- stale retained C calculation;
- suspended bounded route;
- missing source/input state.

It also statically guards against moving route authorization, release predicates, hash creation, numerical execution or stress semantics into the UI file.

STATUS: NOT_RUN

## Hosted CI truth on material head

Three existing PR-triggered WRC workflows ran against the updated head. They fail before either new focused readiness checker is invoked:

- `EMP.1 gamma5 bounded route on current main`: fails at the existing independent-oracle/import-firewall subordinate gate;
- `EMP.1 current-main independent baseline`: fails at the same existing independent-oracle boundary;
- `EMP.1 runEmp1 bounded gamma5 orchestration`: passes independent gamma5 hand calculation and cylindrical load-axis polarity, then fails the historical `remaining fail-closed authority` expectation.

No WRC source, numerical, route, oracle or workflow authority is changed in response.

## Validation truth

```text
node scripts/emp1-readiness-projection-check.mjs   NOT_RUN
node scripts/emp1-readiness-dashboard-check.mjs    NOT_RUN
```

Static source inspection and hosted failures that occur before these checks are not represented as executable PASS.

## Exact next action

From a faithful checkout of `agent/emp1-governance-readiness-v1`, execute:

```bash
node scripts/emp1-readiness-projection-check.mjs
node scripts/emp1-readiness-dashboard-check.mjs
node scripts/emp1-professional-workflow-check.mjs
```

Then run applicable repository import/build checks without modifying WRC authority, oracle custody or GitHub workflow files. Keep PR #1614 Draft until explicit Owner merge authorization.
