# PLAN-0001 — EMP.1 Benchmark Evidence UI

## Objective

Surface already-retained CAUx benchmark evidence in EMP.1 Review & Evidence without recalculation or authority expansion, and render PV Elite truthfully as `REFERENCE_NOT_AVAILABLE` until exact retained source evidence exists.

## Stage 0 — protocol, custody, overlap

- Pin live `Advanced_Analysis/main` and live Common `engineering-pr-delivery-v2` basis.
- Establish exclusive chain for issue #1645.
- Preserve Issue Basis, current Q1–Q5 pack, Owner admission and immutable pre-work endpoint before material code.
- Reconcile exact overlap with open Draft #1622 and adjacent #1624 before touching their UI seams.

Exit gate: pre-work endpoint exists in Git history strictly before first material commit.

## Stage 1 — read-only benchmark workspace composition

Preferred new file: `src/workspace/emp1-benchmark-evidence-workspace.js`.

Responsibilities:
- consume retained CAUx benchmark/custody/qualification V3;
- call existing `projectEmp1BenchmarkEvidence()` rather than duplicate its calculations;
- reconcile historical execution custody with superseding source qualification without rewriting retained history;
- assert benchmark/source/route/quantity identity agreement and fail closed on mismatch;
- project PV Elite with `referenceAvailable=false`, zero quantities and no invented tolerance/value/version.

No WRC execution, interpolation, tolerance selection, hash authority or route mutation.

## Stage 2 — pure renderer

Preferred new file: `src/workspace/emp1-benchmark-view.js`.

CAUx panel must show comparator/case/source verification/freeze state; exact route identity and state; all eight points with retained reference and EMP values, absolute/relative difference, frozen tolerance and status; summary with worst relative difference at Cu and governing Du agreement; and explicit authority caveat.

Never use a generic affirmative PASS that can be mistaken for engineering-route authorization. The interpolated route must visibly state `engineeringUseAuthorized=false`.

PV Elite panel must show `REFERENCE_NOT_AVAILABLE` and no fabricated rows.

## Stage 3 — Review & Evidence integration

Only after deliberate reconciliation with #1622/#1624 ownership.

Preferred existing seams:
- `src/workspace/lafea-analytical-calc-content.js`
- `src/workspace/emp1-professional-workflow-view.js`
- `scripts/emp1-professional-workflow-check.mjs`

Preferred options contract after #1622 reconciliation:

```js
{
  runFailure,
  reviewWorkspace,
  onReview,
  benchmarkEvidence,
}
```

Benchmark Evidence should be a sibling of governed Engineering Review inside Review & Evidence, not a new calculator or engineering-authority owner.

## Stage 4 — focused static/checker coverage

Preferred new checker: `scripts/emp1-benchmark-evidence-ui-check.mjs`.

Verify:
- CAUx comparator/case/source qualification V3;
- exactly eight Au/Al/Bu/Bl/Cu/Cl/Du/Dl rows;
- Cu relative delta `2.0355862430856293%`;
- Du absolute delta `26.786740343133943 kPa`;
- all eight inside frozen 3%; governing Du agreement;
- route registered/comparison-qualified/engineering-use unauthorized;
- all authority-creation flags false;
- PV Elite `REFERENCE_NOT_AVAILABLE` with zero quantities;
- renderer has no WRC evaluator/Table-5/interpolation/tolerance/semantic-hash authority imports.

## Stage 5 — browser evidence

Preferred new spec: `e2e/emp1-benchmark-evidence.spec.js`.

Required assertions:
- panel appears on EMP.1 Review & Evidence only;
- all eight CAUx rows and retained values render;
- source re-verification PASS/current qualification is visible;
- interpolated route authorization reads NO/comparison-only;
- required authority caveat is visible;
- PV Elite shows unavailable without invented values;
- keyboard/table semantics remain usable;
- LAFEA.3+/FEA presentation remains unchanged.

## Validation ladder

1. `node scripts/emp1-benchmark-evidence-projection-check.mjs`
2. focused #1645 benchmark UI checker
3. `node scripts/emp1-caux-pp24-31-benchmark-check.mjs`
4. `node scripts/emp1-wrc537-caux-interpolated-comparison-check.mjs`
5. `node scripts/emp1-pvelite-benchmark-programme-check.mjs`
6. inherited #1622 review/workflow regressions after integration
7. focused Playwright #1645 spec
8. retained EMP/LAFEA.3+ navigation regression
9. `npm run check:imports`
10. `npm run build`
11. `git diff --check`

Preserve exact PASS/FAIL/NOT_RUN.

## Explicit NO-PATCH

- WRC mechanics, coefficient/table/sign/axis/source/applicability owners;
- route registry/executor authorization;
- CAUx frozen expected values, tolerance or retained comparison history;
- PV Elite invented source/value/tolerance;
- generic FEA benchmark behavior;
- LAFEA.3+;
- Load Calc/project-data;
- code/release/deployment authority;
- `.github/workflows/**`;
- unrelated UI redesign.

## Abort conditions

Stop material work if:
- #1622/#1624 exact seam ownership is still unreconciled;
- benchmark/source/route identities disagree;
- current qualification requires rewriting historical custody;
- UI composition requires WRC execution, tolerance selection or route authorization;
- PV Elite values would need invention;
- scope spills outside EMP.1.
