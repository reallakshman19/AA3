# EP-0001 — Release Qualification Integration Prework

STATUS: PREWORK_COMPLETE_MATERIAL_NOT_STARTED

## Owner authorization

The owner instructed `proceed` immediately after the prior automated sequence stopped at the explicit protected release/deployment boundary. This endpoint records that instruction as authorization for one bounded release-qualification integration slice only.

It does **not** authorize merge, deployment, code-compliance authority, WRC/source/oracle mutation, or cryptographic signing/sealing.

## Stack basis

- Base PR: #1626
- Base branch: `agent/emp1-engineering-record-release-handoff-v1`
- Base head: `e2ed96d471c999907999ab1b90cf3fd6233732f9`
- New branch: `agent/emp1-engineering-record-release-qualification-v1`

## Existing release authority observed

Authoritative retained artifact:

`validation/emp1/release/emp1-professional-release-current-state-v1.json`

Identity observed on the stack base:

- Git blob SHA-1: `8d108c6f7850e2a240fc15fdd51318ce1a70a29f`
- `currentStateSemanticHash`: `f9a205509b0da61014655bdb7271c5f4d5716a36a40ab3ea11e0a6cbab286d81`
- schema: `emp1-professional-release-current-state/v1`
- release profile: `EMP1_WRC537_2013_CYLINDRICAL_GAMMA5_ZERO_DP_V1`
- professional release ready: false
- release qualified: false
- deployment authorized: false

The retained current-state checker recomputes `currentStateSemanticHash` from the canonical payload while excluding only `currentStateSemanticHash` and `status`.

## Material contract

Create an immutable qualification binding that:

1. validates the Engineering Record package;
2. validates release handoff/profile compatibility;
3. independently recomputes the release-current-state semantic hash;
4. requires the exact authorized release-state path, Git blob SHA-1 and semantic hash frozen above;
5. rejects any caller-substituted release-state artifact, including a self-consistent object with `releaseQualified=true`;
6. may report `releaseQualified=true` only when a separately authorized future release-state artifact is explicitly admitted by a protected code change;
7. creates only qualification-record identity; it does not mutate the underlying release artifact or deployment authority.

## Current expected result

Because the exact authorized artifact remains fail-closed:

`RELEASE_BLOCKED_EXISTING_AUTHORITY`

No current Engineering Record may become release-qualified through this slice.

## Validation truth

Focused checker will be encoded but remains `NOT_RUN` until actually executed.

## Merge

`MERGE_AUTHORITY = OWNER_ONLY`
`MERGE_AUTHORIZED = FALSE`
