# EP-0004 — E2E continuity + result-control icon cleanup complete

CHAIN_STATE_VERSION: 3
CHAIN_ID: ADV-LFEA-UI-CONTINUITY
STATE: ACTIVE
ENGINEERING_STATE: IN_PROGRESS
CUSTODY_STATE: HELD
QUALIFICATION_STATE: NOT_REQUIRED
WRITE_AUTHORITY: WRITE_ALLOWED
AUTO_STATE: RUNNING
MERGE_AUTHORITY: OWNER_ONLY
COMMON_PROTOCOL: engineering-pr-delivery-v2
COMMON_PROTOCOL_BASIS: 9573ec2bba234cc7cd6abcf73c4c3f3e2bc0892c
COMMON_PROTOCOL_STATUS: CURRENT
HANDOVER_READY: TRUE

## Completed material batch

- Migrated `e2e/lfea-pipeline-accdb-real-model.spec.js` from stale Load-case Analyze to Load case → Error check if re-authorization is required → Run → Analyze → Output → Export.
- Added fixture-free `e2e/lfea-pipeline-continuity.spec.js` for six stable steps, five connectors, active context, blocked empty-state continuity, one Analyze owner and one Export owner.
- Added shared sort direction symbols and a read-only result-sort presentation adapter; visible sort state now uses sprite icons + `aria-sort` instead of raw `▲/▼` label text.
- Kept legacy results-controller sort logic untouched.
- Kept legacy inline CSV producer hidden after every results rerender; the dedicated Export panel remains the only visible export owner.
- Expanded focused static continuity guard to cover E2E migration, sort adapter, task ownership and native execution authority sentinel.

Validation truth:
- base→head comparison: ahead-only, 0 behind;
- changed engineering solver/pre-flight/core result/WRC files: none;
- source/contract audit: PASS_STATIC;
- local checkout/runtime: NOT_RUN_LOCAL (`github.com` DNS resolution unavailable);
- GitHub Actions: intentionally not used per owner instruction.

## Next bounded batch

Audit Input/Error-check continuity and control hierarchy inside the LFEA workflow:
1. inventory visible source acquisition/import actions and Error-check controls for duplicated/stale button language;
2. distinguish primary workflow actions from advanced/optional actions (`Authority supplement`, optional `Code checks`, verification drawer) so optional tools no longer compete visually with Input→Run continuity;
3. improve empty/blocked copy where it still points at implementation concepts instead of the next engineer task;
4. preserve source acceptance/pre-flight authority exactly;
5. update fixture-free UI checks for the resulting hierarchy.

Do not redesign global application navigation in this batch. Any broader nav/icon overhaul needs a successor endpoint after the LFEA workflow itself is coherent.

## Q1–Q5

Q1: Trace Input source acquisition → source owner selection → Error check acceptance → Load case release. Which visible controls are primary workflow controls versus optional engineering supplements?

Q2: Reconstruct the UI with no source, loaded-unreviewed source, BLOCK pre-flight, conditional accepted pre-flight and PASS pre-flight. What should the next-action copy say in each state without changing the underlying verdict?

Q3: Prove that moving optional toolbar actions into an advanced-tools cluster or changing their labels/icons cannot change source/pre-flight/code authority. Identify the callbacks that must remain untouched.

Q4: Validate one visible primary action per current workflow state, no duplicated import/code-check affordance, preserved `data-action` contracts and unchanged source/pre-flight statuses.

Q5: If continuity requires bypassing source acceptance, auto-accepting limitations, changing pre-flight verdicts or changing optional code-check authority, stop: NO-PATCH. Only presentation hierarchy/copy is in scope.

### Handover snapshot
Repo `reallaksh19/Advanced_Analysis`; PR #1552 Draft; branch `codex/lfea-ui-continuity-icons`; base `7d350c269217f3133e778c09a61b69d2b3909a64`; material head before this checkpoint `34b00fe48d0bfae1d703f6a197c3a9ed7b6db048`; chain EP-0004; AUTO RUNNING; merge OWNER_ONLY. Exact next action: source/acquisition + Error-check hierarchy audit and presentation-only cleanup.