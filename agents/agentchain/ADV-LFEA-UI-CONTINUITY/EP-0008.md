# EP-0008 — issue #1559 executed walkthrough leg start

CHAIN_ID: ADV-LFEA-UI-CONTINUITY
LEG_ID: LEG-ISSUE-1559-EXECUTED-WALKTHROUGH
ENDPOINT_ID: EP-0008
PREVIOUS_ENDPOINT: EP-0007 — legacy locator `agents/chains/ADV-LFEA-UI-CONTINUITY/endpoints/EP-0007.md`

CREATED_AT: 2026-08-30T02:54:36Z
ENDPOINT_REASON: LEGACY_TO_RELAY_MIGRATION + PRE_MUTATION

TASK / ISSUE: #1559 — Executed User Walkthrough for the LFEA engineer workflow
PR: #1561 DRAFT
BRANCH: codex/lfea-ui-continuity-consolidation

CHECKPOINT_HEAD: 9dde8e0ed391784a3f66837feb42c055087e9e86
MAIN_HEAD_OBSERVED: 4266249515db1a3cf1f1881292248f477d5214f8
MERGE_BASE: 4266249515db1a3cf1f1881292248f477d5214f8

STATE: ACTIVE

### Mission

Execute and evidence the real engineer journey required by issue #1559 against the native six-stage LFEA workflow, repair only concrete UI/presentation defects exposed by that acceptance contract, and preserve all solver, pre-flight, numerical-result, source, WRC/code, benchmark/oracle, workflow and release authority.

This endpoint migrates the live `ADV-LFEA-UI-CONTINUITY` mission forward from the historical `agents/chains/...` relay layout into the pinned `engineering-pr-delivery-v2` split relay layout. Historical EP-0001 through EP-0007 remain immutable.

### This leg completed

- Re-grounded `main` at `4266249515db1a3cf1f1881292248f477d5214f8` and PR #1561 branch at material/relay head `9dde8e0ed391784a3f66837feb42c055087e9e86`.
- Reconciled the legacy EP-0007 recorded head `da0529d...` with live branch history: `9dde8e0...` is a later relay-only descendant, so the observed difference is metadata drift, not a newly inferred engineering-authority change.
- Confirmed issue #1559 is an execution/UX qualification gate; authored tests alone do not satisfy it.
- Performed active-chain coordination check. Other active PRs touch EMP.1 WRC source governance, Load Calc policy/projection, and B02D relay metadata; no current production-file overlap exists with the LFEA pipeline UI surface. `agents/agentchain.md` is a shared relay index and must be reconciled carefully.
- Confirmed two concrete stale task-custody strings in `src/main.js`: Load case guidance still says `then Analyze`, and optional code-check guidance still says `use Analyze on the Load case step`, while the native architecture assigns Analyze exclusively to RUN.
- Confirmed downstream invalidation mechanics exist: `invalidateLfeaDownstreamPresentation()` clears retained analysis/results and drives LOAD_CASE/RUN/OUTPUT/EXPORT incomplete; shell flow then fail-closes Output/Export. Human-observed clarity remains NOT_RUN.

### Currently in progress

Bounded presentation repair for the proven Load-case/Run wording drift, plus a focused static guard so the old task-custody wording cannot regress silently.

### Remaining work

1. Patch only the stale task-custody copy in its current owning production path.
2. Extend the focused continuity guard to reject the old Load-case Analyze wording and require Apply-selection → Run wording.
3. Execute the focused static continuity check where tooling permits.
4. Execute Playwright continuity and ACCDB journeys where Chromium/tooling permits.
5. Perform the issue #1559 human-observed 12-stage walkthrough, including upstream-change stale-state behavior, with stage evidence.
6. If the walkthrough reveals a concrete material UI defect, create a fresh PRE_MUTATION endpoint before changing its owning component.
7. Preserve PR #1561 as Draft until the issue #1559 acceptance evidence is complete.

### Exact next action

Replace the two stale Load-case/Analyze guidance strings in `src/main.js` with explicit `Apply selection` then `Run` custody, add focused source assertions in `scripts/lfea-ui-continuity-check.mjs`, and stop before any broader UI change unless another concrete #1559 defect is demonstrated.

### Known / proven

- Native workflow IDs remain `INPUT, ERROR_CHECK, LOAD_CASE, RUN, OUTPUT, EXPORT`.
- RUN owns the single Analyze action; LOAD_CASE owns case selection and `Apply selection`.
- OUTPUT owns result review; EXPORT owns the visible CSV action and consumes the existing `resultsPanel.csvFor(active)` producer.
- `src/main.js` currently contains stale guidance that contradicts that custody.
- `invalidateLfeaDownstreamPresentation()` clears analysis controller/results state and marks downstream stages incomplete on source/pre-flight change.
- PR #1561 does not intentionally change solver, pre-flight, result, source, WRC/code, benchmark/oracle or release authority.

### Not proven

- Browser runtime behavior at the current head.
- Human comprehension of icons, hierarchy, blocked reasons, transitions and stale-state messaging.
- Chromium version/environment compatibility.
- Whether issue #1559's full 12-stage walkthrough passes without further UI defects.

### NOT_RUN

- `node scripts/lfea-ui-continuity-check.mjs` at this leg head: NOT_RUN at endpoint creation.
- Playwright `e2e/lfea-pipeline-continuity.spec.js`: NOT_RUN at endpoint creation.
- Playwright `e2e/lfea-pipeline-accdb-real-model.spec.js`: NOT_RUN at endpoint creation.
- Human-observed 12-stage Chromium walkthrough: NOT_RUN.
- GitHub Actions: NOT_USED by Owner instruction inherited from the chain.

### Active hypothesis

The first demonstrable #1559 defect is presentation-custody drift rather than engineering-state drift: stale copy in `src/main.js` tells users to Analyze from Load case even though execution was moved to Run. A minimal copy + anti-drift guard patch should remove that contradiction without changing analysis authority.

### Falsifier

The hypothesis is falsified if the current production branch does not actually render those strings, or if changing them requires altering case-selection/pre-flight/solver/result authority. Any browser-observed stale-result publication, duplicate execution authority, or numerical/result mutation becomes a separate concrete defect and must be isolated before mutation.

### Protected invariants

- `executionAuthority.run(authorized, { requestedCaseIds })` remains the solve authority.
- Requested case IDs remain sealed by the current authorized pre-flight.
- Retained analysis publication state remains `EMPTY | CURRENT` under the existing controller contract.
- CSV numerical production remains `resultsPanel.csvFor(active)`.
- No solver, element, assembly, recovery, pre-flight rule, source authority, WRC/B31 mechanics, benchmark/oracle, tolerance, workflow/security or release-authority change in this leg.
- Output/Export must fail closed after upstream invalidation.

### Do not redo

- Do not reintroduce the deleted observer/presentation adapters.
- Do not move Analyze back to Load case.
- Do not add a second CSV/export owner.
- Do not infer runtime PASS from static/source inspection.

### Do not change

- Numerical solver/recovery code.
- Engineering pre-flight authorization semantics.
- Source/canonical engineering authority.
- WRC/code mechanics and licensed-source boundaries.
- Independent benchmark/oracle values or tolerances.
- CI/workflow/security configuration.

### Expected next-leg files / domains

Expected for the immediate bounded patch:
- `src/main.js`
- `scripts/lfea-ui-continuity-check.mjs`

Potential only after independently observed UX defect:
- native owner among `src/workspace/lfea-pipeline-shell-view.js`, `lfea-pipeline-shell-controller.js`, `lfea-pipeline-run-panel.js`, `lfea-pipeline-results-panel.js`, `lfea-pipeline-export-panel.js`, `lfea-source-acquisition.js`
- focused E2E evidence paths

### Inputs

- GitHub issue #1559, current body as observed 2026-08-30.
- PR #1561 branch `codex/lfea-ui-continuity-consolidation` at `9dde8e0ed391784a3f66837feb42c055087e9e86`.
- Merged functional continuity base `4266249515db1a3cf1f1881292248f477d5214f8` from PR #1560.
- Historical chain endpoint `agents/chains/ADV-LFEA-UI-CONTINUITY/endpoints/EP-0007.md`.

### Benchmarks

- IMPLEMENTATION_COUPLED: `scripts/lfea-ui-continuity-check.mjs` structural/task-custody assertions.
- IMPLEMENTATION_COUPLED: `e2e/lfea-pipeline-continuity.spec.js` fixture-free workflow contract.
- IMPLEMENTATION_COUPLED: `e2e/lfea-pipeline-accdb-real-model.spec.js` real-model workflow journey.
- INDEPENDENT USER-OBSERVATION ORACLE FOR #1559: 12-stage human-observed walkthrough against the exact tested head; NOT_RUN at endpoint creation.

### Common / governing documents

- `reallaksh19/Common@10d667ce715bb52e1f73035c6fa326db77d0f9dd/skills/engineering-pr-delivery-v2/SKILL.md`
- same revision: `references/relay-model.md`, `agentchain-schema.md`, `source-indexing.md`, `qualification.md`, `engineering-validation.md`, `anti-gaming-rules.md`, `multi-agent-coordination.md`, `git-pr-policy.md`

### Authoritative sources

- For this UI leg, repository production state and issue #1559 are authoritative for workflow/presentation acceptance.
- Engineering numerical/source authority is explicitly outside this leg and remains with its existing qualified sources/controllers.

### Production paths

- `src/main.js` — LFEA engineering-session synchronization, step guidance, invalidation and Run callback wiring.
- `src/workspace/lfea-pipeline-case-selection-panel.js` — LOAD_CASE selection/apply owner.
- `src/workspace/lfea-pipeline-run-panel.js` — RUN Analyze owner.
- `src/workspace/lfea-pipeline-shell-controller.js` — presentation sequencing and downstream fail-closed state.
- `src/workspace/lfea-pipeline-shell-view.js` — native task hosts and visible step state.
- `src/workspace/lfea-pipeline-results-panel.js` / `lfea-pipeline-export-panel.js` — Output/Export publication custody.

### Validation / test paths

- `scripts/lfea-ui-continuity-check.mjs`
- `e2e/lfea-pipeline-continuity.spec.js`
- `e2e/lfea-pipeline-accdb-real-model.spec.js`
- issue #1559 human-observed 12-stage browser evidence

### Changed during this leg

At endpoint creation: only this new relay endpoint is being added. No production mutation has yet been made under EP-0008.

### Validation summary

- Repository/source reconciliation: PASS_STATIC.
- Multi-agent production-file coordination: SAFE for the bounded LFEA UI patch; shared `agents/agentchain.md` requires ordinary index reconciliation.
- Browser/runtime acceptance: NOT_RUN.
- Human-observed #1559 acceptance: NOT_RUN.

### Open risks / questions

- The current environment may still prevent faithful local Chromium execution; if so, preserve NOT_RUN rather than promoting authored tests.
- Issue #1559 title says CORE WRC while its body/linked work is the LFEA/EMP.1 six-stage workflow; this naming inconsistency must not silently widen this leg into WRC mechanics.
- Stale-state mechanics are statically present, but their user-facing clarity is not proven until the browser walkthrough.

### Next-agent qualification

QUALIFICATION_BASIS_HEAD: 9dde8e0ed391784a3f66837feb42c055087e9e86
QUESTION_SET_ID: QS-ADV-LFEA-UI-1559-EXECUTION-0001
QUESTION_SET_STATUS: CURRENT

#### Q1 — Production Trace

Trace one applied case from the LOAD_CASE `Apply selection` action through pre-flight requested-case retention, RUN's sole Analyze action, retained CURRENT analysis state, OUTPUT review and EXPORT CSV production. Name the current files/functions and identify the first boundary that could wrongly re-authorize or alter case identity.

#### Q2 — Current Unresolved Problem / Failure Isolation

Reproduce the stale task-custody wording in `src/main.js`, identify every currently reachable string that tells the engineer to Analyze from Load case, predict the corrected visible wording after the minimal patch, and state the source/browser observation that would falsify the patch.

#### Q3 — Authority / Invariant

Show why changing Load-case/Run instructional copy and its static guard cannot authorize a solve, mutate requested case IDs/results, or expand WRC/B31 scope. Identify one plausible but invalid shortcut, such as moving Analyze back to Load case or deriving a second export value in the UI.

#### Q4 — Independent Validation

Against the exact tested head, distinguish source/static PASS, automated browser PASS, and human-observed walkthrough PASS. Reconstruct the required negative controls: 0 Analyze on LOAD_CASE, 1 Analyze on RUN, 0 CSV on OUTPUT, 1 CSV on EXPORT, and Output/Export fail-closed after upstream invalidation. Do not represent NOT_RUN as PASS.

#### Q5 — Next Contribution / Minimal Patch

If the wording defect is confirmed, identify the smallest changed-file set and exact assertions needed to prevent regression. If the human walkthrough instead reveals stale results remaining publishable, identify the first native owner to inspect and list the protected numerical/source/WRC/workflow domains that must remain unchanged.