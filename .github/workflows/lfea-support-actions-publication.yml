name: lfea-support-actions-publication

on:
  pull_request:
    paths:
      - 'src/workspace/linear-piping-support-actions-publication.js'
      - 'scripts/linear-piping-support-actions-publication-check.mjs'
      - 'scripts/linear-piping-support-actions-publication-source-guard.mjs'
      - '.github/workflows/lfea-support-actions-publication.yml'

permissions:
  contents: read

jobs:
  qualify-support-actions-publication:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact pull-request head
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Prove exact-head execution
        run: |
          test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.head.sha }}"
          git diff --exit-code

      - name: Qualify governed recovery publication
        run: node scripts/linear-piping-support-actions-publication-check.mjs

      - name: Enforce global-force and explicit-up custody
        run: node scripts/linear-piping-support-actions-publication-source-guard.mjs
