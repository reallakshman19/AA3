# LEG-008 — retain accessibility evidence for WRC walkthrough checkpoints

CHAIN_STATE_VERSION: 3
CHAIN_ID: ADV-EMP1-WRC-UI-WALKTHROUGH
MATERIAL_LEG_ID: LEG-008
PRE_MUTATION_ENDPOINT: agents/chains/ADV-EMP1-WRC-UI-WALKTHROUGH/endpoints/EP-0025.md
ISSUE: #1559
PR: #1579 DRAFT
BASE_MAIN: fad372eaf55487b10a2cbcad5ff8438b71e031ff

## Defect repaired

The #1559 human-review rubric requires icon/control meaning to be accessible and consistent, but the merged evidence harness retained only PNGs plus scroll metadata. Artifact-only review therefore could not prove a non-visible accessible name.

## Material changes

Exactly two evidence-layer files changed:

1. `e2e/emp1-professional-walkthrough-evidence.spec.js`
   - preserves the same 12 checkpoint sequence;
   - preserves the same viewport and full-page PNG captures;
   - adds one `Locator.ariaSnapshot()` capture in the shared checkpoint helper;
   - attaches one `*-aria` text artifact per checkpoint;
   - advances manifest schema to `emp1-wrc-ui-walkthrough-evidence/v3`;
   - declares `ariaSnapshots: 12`;
   - records that hover-only tooltip meaning still requires live observation.

2. `docs/emp1/EMP1_WRC_UI_WALKTHROUGH.md`
   - artifact-only icon/control PASS now requires a self-evident visible label or stable retained accessible name;
   - hover-only meaning not represented in ARIA evidence requires live/recorded observation, otherwise PARTIAL;
   - execution evidence expectation is now 12 viewport + 12 full-page + 12 ARIA attachments plus manifest.

## Exact material identities

- evidence spec blob: `8e3f22ec5cdcc66debab0e094d073c07edcc58cf`
- walkthrough document blob: `446310833bd5f1ba5fb0b7dbeb03a8f916094dea`

## Validation

Exact committed evidence-spec bytes were matched to the locally validated file by Git blob identity:

```text
local git hash-object = 8e3f22ec5cdcc66debab0e094d073c07edcc58cf
committed blob        = 8e3f22ec5cdcc66debab0e094d073c07edcc58cf
```

Executed on those exact bytes:

- `node --check`: PASS;
- physical lines: 174 (<300): PASS;
- checkpoint calls: 12: PASS;
- shared ARIA snapshot operation present: PASS;
- manifest `ariaSnapshots: 12`: PASS;
- manifest schema v3: PASS;
- hover-only tooltip live-observation flag: PASS.

Document source inspection confirms:

- 12 ARIA artifact policy is stated;
- artifact-only accessible-name rule is explicit;
- hover-only tooltip evidence cannot be inferred from screenshots and remains PARTIAL without live/recorded observation.

## Containment

Current main -> branch compare after material patch:

- 13 ahead / 0 behind;
- no `src/workspace/**` production change;
- no `src/core/emp1/**` change;
- no WRC numerical/source/dataset/oracle/tolerance change;
- no route registry/authorization/currentness change;
- no code-compliance or release/deployment authority change.

Non-relay changed material is exactly the two files above.

## NOT_RUN / not claimed

- faithful project-local Playwright execution: NOT_RUN because source/dependency materialization remains blocked in this runtime;
- generated 12 viewport + 12 full-page + 12 ARIA artifacts: NOT_RUN;
- qualified human review: NOT_RUN;
- #1559 acceptance: NOT_COMPLETE;
- UI COMPLETE: NOT_PROVEN.

## Authority boundary

This leg improves acceptance evidence only. It does not alter engineering calculations, WRC method authority, route authorization, currentness logic, code compliance, release or deployment authority.
