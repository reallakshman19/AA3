# LEG-001 — Engineering Record Package Material

Status: MATERIAL_COMPLETE_VALIDATION_NOT_RUN

Material head before completion custody: `e5ced5fc74676c681e6d15d2d5ff324f07a37c96`

## Material files

- `src/core/emp1/emp1-engineering-record-package.js`
- `src/workspace/emp1-engineering-record-package-workspace.js`
- `scripts/emp1-engineering-record-package-check.mjs`

## Core package

Schema: `emp1-engineering-record-package/v1`.

The package retains only hash-bound/governed custody:

```text
sourceHash
loadTransferResultHash
sectionScreeningResultHash
localCorrelationResultHash
assessmentSemanticHash
routeAuthorityHash
reviewId
reviewSemanticHash
```

It embeds the validated review record and exact retained route-authority snapshot. Method/scope/limitations are copied from that snapshot only.

The package computes one new `semanticHash` / `packageId` for package custody. This is not an engineering result hash and creates no numerical/source/method/applicability/review/code/release authority.

## Current-review gate

Creation requires the existing review-state owner to classify the retained review as current. Both current `REVIEW_ACCEPTED` and current `REVIEW_REJECTED` are permitted truthful audit outcomes. `NOT_REVIEWED` and `REVIEW_STALE` fail closed.

## Workspace adapter

`emp1-engineering-record-package-workspace.js`:

- derives package evidence through `emp1EngineeringReviewEvidenceFromExecution(execution)`;
- requires current workbench execution/C custody through the existing review workspace projection;
- exposes `canExport` + blocker codes;
- returns a validated immutable package;
- provides deterministic JSON serialization and a hash-suffixed `.json` filename;
- retains no package in controller/session state in this slice.

## Authority boundary

Explicitly false:

```text
createsEngineeringCalculationAuthority
createsSourceAuthority
createsMethodAuthority
createsApplicabilityAuthority
createsReviewAuthority
createsCodeCompliance
createsReleaseAuthority
createsDeploymentAuthority
createsCryptographicSeal
```

## Validation

Focused checker encoded:

```bash
node scripts/emp1-engineering-record-package-check.mjs
```

Execution status: `NOT_RUN`.

Source inspection / branch comparison are not executable PASS.
