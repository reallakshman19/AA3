# LEG-002 — spring-rate resolver exact-runtime falsifiers

CHAIN_ID: ADV-LFEA-SUPPORT-REPRESENTABILITY
LEG_ID: LEG-002-SPRING-RATE-RESOLVER-EXACT-RUNTIME
PREWORK_ENDPOINT: EP-0025
WORK_ITEM_KEY: github:reallaksh19/Advanced_Analysis#1551
AGENT_INSTANCE_ID: chatgpt:7a26369e-efea-4bc5-b582-f36fa511a93c
OWNER_PROGRESSION_COMMAND: CONTINUE_INTERRUPTED_BOUNDED_PROGRESS
BASE_ENDPOINT_HEAD: 614d274ebf0c2d1c1d716f9cf7c7b5d67b484e12
TEST_HEAD: ef275dfea07000b2dd3acaba4c462443c231cade
LIVE_MAIN_AT_REGROUND: 75c670424ef46888f0a1c02188a9ea8d42b5209f
COMMON_PROTOCOL_BASIS: 4b3a7a9c7ca2fac4a9182ef0028135d17eafaf02
COMMON_LIVE_MAIN_AT_REGROUND: 293a3db7993a6945c01adc592a7ff14a339c504a

## Material change

Two test-side files changed; production code is untouched.

1. `scripts/lfea-spring-rate-units-check.mjs`
   - added explicit deliberate-break modes for raw N/mm pass-through and unresolved-unit factor-1 fallback;
   - this broader structural-compilation check remains `NOT_RUN` because its full import closure has not yet been reconstructed.
2. `scripts/lfea-spring-rate-resolver-check.mjs`
   - new two-file resolver micro-gate for the named production authority `restraint-spring-rate.js`;
   - exercises conversion factors, resolved value, unresolved withholding and two independent falsifiers.

`614d274e... -> ef275dfe...` changes only these two test files.

## Exact source closure

The resolver micro-gate has exactly one production import:

- `scripts/lfea-spring-rate-resolver-check.mjs` = `126d25ce81921ef1e39ac9ede68ac3629562b577`
- `src/core/linear-piping-analysis-consumer/restraint-spring-rate.js` = `50094a2bd8b2fd54374b06e33f82484e27cc774b`

Both local files reproduced their current PR Git blob SHA with `git hash-object` before execution.

Runtime: Node v22.16.0 with `--experimental-default-type=module` so the isolated reconstruction uses the same ESM interpretation as the repository's declared module mode without depending on an unverified local package file.

## Observed normal execution

Command:

```text
node --experimental-default-type=module scripts/lfea-spring-rate-resolver-check.mjs
```

Observed exit `0` / `PASS`.

Exact conversion factors:

- N/m → `1`
- N/cm → `100`
- N/mm → `1000`
- lbf/in → `175.12683524647636`

A declared `400 N/mm` resolves to `400000 N/m`.

For unresolvable units, `resolveSpringRate(400, null)` retains `stiffnessDeclared = 400`, sets `stiffnessValue = null`, and reports `stiffnessUnitsResolvable = false`.

## Observed deliberate breaks

### Raw-rate break

```text
node --experimental-default-type=module scripts/lfea-spring-rate-resolver-check.mjs --deliberate-break-rate
```

Observed exit `1` at:

```text
AssertionError: 400 N/mm must resolve to 400000 N/m, never raw 400 N/m
400 !== 400000
```

This reproduces the historical 1000x-too-soft defect by deliberately substituting factor `1` at the resolver boundary.

### Unresolved fallback break

```text
node --experimental-default-type=module scripts/lfea-spring-rate-resolver-check.mjs --deliberate-break-unresolved
```

Observed exit `1` at:

```text
AssertionError: unresolved units must withhold the solver stiffness rather than assume factor 1
400 !== null
```

This reproduces the forbidden unresolvable-unit fallback.

Classification:

- normal: `PASS_EXACT_HEAD_GIT_BLOB_LOCAL`
- raw-rate break: `RED_OBSERVED_EXACT_HEAD_GIT_BLOB_LOCAL`
- unresolved fallback break: `RED_OBSERVED_EXACT_HEAD_GIT_BLOB_LOCAL`

## Engineering meaning / authority boundary

This closes the resolver-level part of issue #1551 Q4:

`k_SI = k_declared × forceScale / lengthScale`, with no factor-1 fallback when the quotient cannot be resolved.

It does **not** yet prove the broader structural fail-closed path `INPUTXML_STRUCTURAL_SPRING_RATE_UNRESOLVED`, because `lfea-spring-rate-units-check.mjs` imports the structural compiler and its complete closure remains unexecuted. It also does not qualify any support's stiffness assembly, production solve, reaction share, BM4 parity or CAESAR reference.

## Drift / authority

Live Common and Advanced_Analysis main are unchanged from the EP-0025 re-ground. No production file in this resolver closure moved on live main during this leg.

Protected boundaries preserved: no solver formulation, stiffness/load assembly, recovery, tolerance, benchmark/oracle, sign convention, row selection, reducer or parity-residual change.

## Validation truth

PASS_EXACT_HEAD_GIT_BLOB_LOCAL:
- spring-rate resolver factor/withholding micro-gate;
- raw-rate deliberate break red;
- unresolved-factor-1 deliberate break red.

PASS_SOURCE / NOT_RUN_RUNTIME:
- broader `lfea-spring-rate-units-check.mjs` deliberate-break wiring is authored but its structural-compilation closure has not been executed.

NOT_RUN remains:
- dedicated refusal fixture;
- finite skew/CNODE/HANGER production mechanics;
- mixed fixed+skew production solve;
- spring/full aggregates;
- imports/lint/diff;
- BM4_L before/after;
- external CAESAR feature references.

AGENT_LEG_COMPLETE: TRUE
PR_COMPLETE: FALSE
CHAIN_COMPLETE: FALSE
