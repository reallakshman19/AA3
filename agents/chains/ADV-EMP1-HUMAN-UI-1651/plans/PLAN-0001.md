# PLAN-0001 — Issue #1651 EMP human-engineering UI programme

PLAN_STATUS: ISSUE_EXECUTION_PLAN
WORK_ITEM_KEY: github:reallaksh19/Advanced_Analysis#1651
BASE_MAIN: b4eb0cea9a7a73ddaec86210373ed6f3acb714eb
PROJECT_ROADMAP: EMP_TAB_UI_RECOMMENDATIONS.md@d616a4ea014d583c9709a872c3af3896dea9011e
ROADMAP_WRITE_AUTHORITY: NONE

## Programme invariant

UI/presentation changes may improve human engineering readability and regression safety but may not change WRC numerics, source/applicability/sign/axis authority, route authorization, benchmark expected values/tolerances, code compliance or release/deployment authority.

## LEG-001 — strict presentation and live-DOM regression gate

Purpose:
- replace normal-UI silent machine-code fallback with an explicit mapped presentation contract;
- preserve raw exact identifiers only in semantically designated technical/raw regions;
- add a focused EMP Playwright DOM sweep rejecting raw enum/dotted identifiers outside those regions;
- keep legitimate engineering abbreviations (WRC, ASME, PDF, SHA-256, CAUx, EMP) valid.

Primary owners expected:
- `src/workspace/emp1-plain-language-labels.js`;
- normal EMP call sites that currently render raw machine values;
- `scripts/emp1-plain-language-labels-check.mjs`;
- focused new `e2e/` EMP raw-token spec;
- minimal existing EMP test orchestration/package script only if required.

No layout/table-generalization work in this leg.

## LEG-002 — governed identity × value-column matrix primitive

Purpose:
- generalize current conservative X/Y/Z grouping into declared matrix-family metadata;
- preserve descriptor/entity IDs, batch edit, Undo/Redo, units and per-cell source custody;
- route Pressure to one row per identity with `Internal | External` columns;
- keep Reference points / Load force / Load moment behavior unchanged.

Negative controls:
- partial family, duplicate column, mismatched collection path/identity key/unit family must fall back rather than guess.

## LEG-003 — anti-waterfall assessment layout

Purpose:
- introduce a semantic assessment shell with primary engineering-input/result region and contextual engineering-basis/authority region;
- keep Review & Evidence separate from primary data-entry height;
- responsive one-column fallback at narrow widths;
- establish default assessment-height and evidence-isolation browser regression checks.

## LEG-004 — benchmark presentation hierarchy hardening

Purpose:
- consume the already-formal benchmark workspace/view merged by PR #1648;
- compact default CAUx summary with detailed comparison behind progressive disclosure;
- clearly separate current source qualification, historical comparison custody and route engineering-use state;
- keep PV Elite as source-blocked / reference unavailable with no invented values/tolerance;
- move exact technical IDs/hashes to advanced details while retaining audit access.

## LEG-005 — qualification/equivalence closure

Required proof:
- raw-token browser gate passes;
- Pressure is exactly 5 identities × 2 value columns;
- grouped Reference/Load tables remain correct;
- new comparators do not increase default primary-assessment height;
- CAUx eight-point values, frozen tolerance, hashes and governing location unchanged;
- route registration/engineeringUseAuthorized unchanged;
- WRC numerical/source/sign/axis/applicability and code/release authority unchanged;
- accessibility/keyboard/table semantics retained;
- truthful PASS/FAIL/NOT_RUN/NOT_APPLICABLE reporting.

## Merge discipline

Every material leg requires its own append-only material receipt and accepted successor endpoint. Merge authority remains Owner-only unless explicitly granted. No `.github/workflows/**` mutation is in scope.
