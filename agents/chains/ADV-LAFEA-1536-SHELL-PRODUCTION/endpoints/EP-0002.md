# EP-0002 — Sparse-direct shell path implemented; executable qualification pending

CHAIN_STATE_VERSION: 3
CHAIN_ID: ADV-LAFEA-1536-SHELL-PRODUCTION
ENDPOINT_ID: EP-0002
PARENT_ENDPOINT: EP-0001
CUSTODY_EPOCH: 2
MATERIAL_BATCH_HEAD: 71e00503f1c35745b72dbf07210839a2eaac4448
COMMON_PROTOCOL: engineering-pr-delivery-v2
COMMON_PROTOCOL_BASIS: 9573ec2bba234cc7cd6abcf73c4c3f3e2bc0892c
COMMON_PROTOCOL_STATUS: CURRENT
ROADMAPS: docs/conceptcumroadmapLAFEA.md@088f4cebfd954e5d1e37da855c95142712463a31
ROADMAP_REVIEW_STATUS: COMPLETE
HANDOVER_READY: TRUE

## Material result

Leg A1 is implemented on draft PR #1541 without changing LAFEA.4 formulation authority.

### Production changes

- `src/core/local-shell/assembly.js`
  - retains dense assembly for small systems;
  - switches above 1,536 DOF to governed sparse lower-symmetric storage from `src/core/lafea-linear-solve/**`;
  - exposes storage-independent stiffness action;
  - retains JSON-safe sparse execution evidence instead of leaking `Map` objects into result hashing.
- `src/core/local-shell/solver.js`
  - reuses governed sparse prescribed-DOF partitioning and sparse Cholesky;
  - preserves dense Cholesky for small systems;
  - preserves reaction, residual and force/moment equilibrium semantics;
  - maps non-positive sparse pivots back to `ShellSingularSystemError`.
- `src/core/local-shell/recovery.js`
  - global strain-energy reconstruction now uses storage-independent `u·Ku/2`;
  - element membrane/bending/stress recovery is unchanged.
- `src/core/local-shell/calculate.js`
  - retains serializable stiffness evidence for sparse results; small dense result shape/value remains unchanged.
- `scripts/lafea.4-solver-loads-check.mjs`
  - same-model dense↔sparse displacement/reaction/energy/stress parity;
  - automatic production sparse routing above threshold;
  - sparse singular fail-closed path.

### Protected domains confirmed unchanged by diff scope

CST/DKT equations, five-DOF convention, pressure mapping, stress/von-Mises definitions, qualification tolerances, MITC code, LAFEA.4 registry/composition and UI presenter were not modified.

## Review finding during implementation

The generic sparse Cholesky package is genuinely sparse in storage but explicitly has no fill-reducing ordering and is scoped to local/patch-scale systems. Therefore A1 removes the dense global-memory wall but does **not** establish final very-large-free-DOF production scalability. Iterative/fill-reduced solve remains a later #1536 leg.

## Roadmap governance

`docs/conceptcumroadmapLAFEA.md` says material solver changes require a same-PR roadmap update, but Common owner-roadmap policy requires explicit authorization for owner-controlled roadmap mutation. No such mutation authority has been inferred. Chain-local proposal `roadmap-proposals/RP-0001.md` records the exact observed-status refresh that would be appropriate only after executable qualification passes.

## Validation ledger

| Check | State | Evidence |
|---|---|---|
| Source/diff inspection | COMPLETE_NONEXECUTABLE | first wrong field (`qualification.actual`) found and corrected before endpoint |
| `node scripts/lafea.4-solver-loads-check.mjs` | NOT_RUN | no runnable repository checkout; container DNS cannot resolve GitHub |
| existing LAFEA.4 focused checks | NOT_RUN | same environment limitation |
| `npm run check:lafea-core` | NOT_RUN | same environment limitation |
| `npm run check:lafea-solver` | NOT_RUN | same environment limitation |
| `npm run check:imports` | NOT_RUN | same environment limitation |
| `npm run build` | NOT_RUN | same environment limitation |
| `git diff --check` | NOT_RUN | no local checkout |
| GitHub workflows on head | NOT_APPLICABLE | attached runs are unrelated EMP.1 workflows; not counted as shell evidence |

No executable engineering PASS is claimed.

## Changed-file ledger

- `agents/chains/ADV-LAFEA-1536-SHELL-PRODUCTION/ACTIVE.md` — chain state.
- `agents/chains/ADV-LAFEA-1536-SHELL-PRODUCTION/endpoints/EP-0001.md` — pre-work/Q-set.
- `agents/chains/ADV-LAFEA-1536-SHELL-PRODUCTION/roadmap-proposals/RP-0001.md` — advisory observed-status refresh.
- `src/core/local-shell/assembly.js` — dense/sparse storage owner.
- `src/core/local-shell/solver.js` — dense/sparse solve dispatch.
- `src/core/local-shell/recovery.js` — storage-independent global energy.
- `src/core/local-shell/calculate.js` — JSON-safe retained stiffness evidence.
- `scripts/lafea.4-solver-loads-check.mjs` — parity/auto-route/fail-closed tests.

### Handover snapshot

Repo: `reallaksh19/Advanced_Analysis`
Task: #1536 LAFEA.4 shell production
Chain: `ADV-LAFEA-1536-SHELL-PRODUCTION`
Endpoint: `EP-0002`
PR: #1541
PR status: Draft, mergeable; validation NOT_RUN
Branch / PR head / main: `codex/lafea-1536-shell-scalability` / `71e00503…` material head / `80dcfe83…`
Merge authority: OWNER_ONLY
Engineering / custody / qualification / write state: IN_PROGRESS / HELD / NOT_REQUIRED / WRITE_ALLOWED
Roadmap: `conceptcumroadmapLAFEA.md@088f4ceb…`; RP-0001 proposed, no write authority
Inputs: active CST/DKT v1 route
Benchmarks: existing LAFEA.4 gates; solver-loads gate extended
Governing docs / sources: issue #1536; Common `9573ec…`
Current blocker: exact-head executable shell validation unavailable; roadmap mutation not authorized
Exact next action: run focused/aggregate checks on PR head; if green, decide next solver-scalability leg without promoting MITC.

Q1: Trace the new auto storage dispatch through sparse BC partition, factorization, reaction and energy; identify the retained dense fallback.
Q2: Reconstruct the 1,536-DOF threshold and the generated 310-node/1,550-DOF auto-route fixture; prove only one free DOF is solved.
Q3: Explain why sparse-direct PASS would not authorize MITC, recovery changes, roadmap mutation or production release.
Q4: Independently verify dense↔sparse parity quantities and the 12,500 N pressure oracle remain protected.
Q5: State the next safe patch only after exact-head checks, including a NO-PATCH condition if generic sparse-direct limitations invalidate the intended production-scale claim.

### Takeover qualification pack

#### Q1 — Production trace
Repository anchors: modified `assembly.js`, `solver.js`, `recovery.js`, `calculate.js`; existing `src/core/lafea-linear-solve/{sparse-matrix,bc-elimination,sparse-cholesky}.js`.
Production object/case: LAFEA.4 CST/DKT model with DOF count above `DENSE_SHELL_STIFFNESS_DOF_LIMIT`.
Required technical work: trace `assembleGlobalSystem()` → `assembleSparseGlobalSystem()` → `partitionSparseSystem()` → `sparseCholeskyFactorize()`/`sparseCholeskySolve()` → `globalStiffnessAction()` → reaction/equilibrium → recovery energy.
Required evidence: show dense fallback still owns <=1,536 DOF and no MITC function enters this path.
First authority boundaries: shell formulation remains `element.js`; generic sparse package owns sparse linear algebra.
Fail if: retained sparse result evidence is confused with the internal `Map` matrix or if formulation authority is inferred from solver selection.

#### Q2 — Current scaling reconstruction
Repository anchors: threshold constant and `sparseStripSource()` in solver-loads check.
Calculation: columns=`floor(1536/10)+2=155`; nodes=`2×155=310`; DOF=`1,550`; all DOFs are prescribed except tip `UX`, so reduced sparse system size is 1.
Required evidence: global sparse matrix size 1,550, free DOF count 1, solver method `DETERMINISTIC_SPARSE_CHOLESKY`.
First wrong boundary if failure: distinguish assembly/storage failure from BC partition or factorization failure.
Falsifier: production result stays dense or free system contains more than one DOF.
Fail if: test size or constraint arithmetic is guessed.

#### Q3 — Authority invariant
Repository anchors: `AGENTS.md`, stage registry, RP-0001, generic sparse-Cholesky module docs.
Required technical work: separate storage/linear solve qualification from CST/DKT mechanics, MITC adoption, roadmap and release authority.
Protected invariant: this leg cannot change element equations, loads, recovery semantics, tolerances or route authority.
Falsifier: diff reaches those protected paths/semantics or a result labels MITC/production-scale authority.
Invalid shortcut: call sparse-direct “production-scale complete” despite no fill-reducing/iterative evidence.
Fail if: roadmap-write or merge permission is inferred from implementation.

#### Q4 — Independent validation
Repository anchors: solver-loads parity additions plus existing pressure check.
Independent oracle: 2.5 N/mm² × 5,000 mm² = 12,500 N for the pressure patch; dense production route is the same-model numerical comparator for backend parity, not an independent physical oracle.
Required evidence: load vector exact parity; displacement/reaction/energy/recovered stress parity within existing repository tolerances; sparse singular route throws `ShellSingularSystemError`.
Falsifier: any backend-dependent engineering quantity exceeds the existing tolerance or singular sparse solve is accepted.
Fail if: tolerance is loosened.

#### Q5 — Next contribution / minimal patch
Repository anchors: PR #1541 exact head after executable validation, generic sparse-solver limitations, issue #1536 required actions.
Safe patch boundary: only after A1 checks pass, choose between (a) a separately qualified iterative/fill-reduced linear-solve owner for large free systems or (b) a narrower documented sparse-direct application envelope. Do not yet promote MITC.
Expected evidence: exact-head A1 green first; new scalability benchmark must exercise realistic free-DOF growth rather than a mostly constrained matrix.
Protected domains: MITC/formulation, recovery, source/benchmark authority, registry/UI and roadmap unless separately authorized.
Negative test: large under-constrained/ill-conditioned model remains fail-closed.
Rollback boundary: A1 parity/regression failure or evidence that the proposed next solver cannot preserve deterministic residual/equilibrium semantics.
NO-PATCH condition: executable A1 evidence is unavailable or red, or next solver requires an unapproved change to protected engineering authority.
Fail if: numerical leg advances past red/NOT_RUN prerequisite evidence.
