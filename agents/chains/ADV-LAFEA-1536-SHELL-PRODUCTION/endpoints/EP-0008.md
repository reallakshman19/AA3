# EP-0008 — MITC consistent pressure evidence implemented; global-solve leg pre-work

CHAIN_STATE_VERSION: 3
CHAIN_ID: ADV-LAFEA-1536-SHELL-PRODUCTION
ENDPOINT_ID: EP-0008
PARENT_ENDPOINT: EP-0007
CUSTODY_EPOCH: 8
MATERIAL_BATCH_HEAD: c589a61b6d5cab49553fd1565a5c06cae5715d51
COMMON_PROTOCOL: engineering-pr-delivery-v2
COMMON_PROTOCOL_BASIS: 9573ec2bba234cc7cd6abcf73c4c3f3e2bc0892c
COMMON_PROTOCOL_STATUS: CURRENT
ROADMAPS: docs/conceptcumroadmapLAFEA.md@088f4cebfd954e5d1e37da855c95142712463a31
ROADMAP_REVIEW_STATUS: COMPLETE
HANDOVER_READY: TRUE

## Material result

The second experimental MITC adoption batch adds consistent reference-configuration pressure-load evidence without changing the registered shell route.

`src/core/local-shell/mitc-adoption-loads.js` now:

- validates the existing explicit experimental MITC model and pressure-load semantics;
- retains global shell DOF order `[UX, UY, UZ, R1, R2]`;
- integrates MITC4 pressure as `integral(N_i p n detJ dxi deta)` using the existing Q4 shape/Jacobian owner;
- integrates MITC3 through its three-point triangle rule, recovering the exact `p*A/3` special case;
- retains the canonical element node order alongside nodal weights/forces so no consumer infers topology from vector position;
- reconstructs force and first moment both from consistent nodal loads and direct quadrature and fails closed if those representations disagree;
- labels geometry as `UNDEFORMED_REFERENCE_CONFIGURATION` with `followerLoadAuthority: false`;
- retains `EXPERIMENTAL_NONPRODUCTION` / production-qualification `false`.

Focused `scripts/lafea.4-mitc-adoption-pressure-check.mjs` includes independent hand/polygon oracles:

- rectangular Q4: `p*A/4` emerges as a special affine case;
- distorted Q4 `(0,0),(4,0),(5,3),(0,2)`: nodal area weights `[5/2,17/6,3,8/3]`, shoelace area `11`, centroid `(79/33,43/33)`, and for `p=2`, force `[0,0,22]`, moment `[86/3,-158/3,0]`;
- reversed pressure sense reverses force and moment while preserving nodal area weights;
- right TRI3 `100 x 50`: exact `A/3` shares, total force `5000 N` at `p=2 MPa`, and hand-calculated first moment;
- negative pressure magnitude, unresolved target and duplicate per-element pressure application fail closed.

## Validation truth

Executable checks remain `NOT_RUN` in the current environment; no software PASS is claimed. Independent hand/polygon arithmetic is retained in the focused check but has not been executed by a Node process here. Parent #1541 exact-head validation remains blocked before step 1.

No recovery, global solve, result contract, registry, presenter, UI, roadmap or workflow file changed.

## Next bounded material batch — assembly + solve only

Build an experimental MITC global system/linear-solve boundary that consumes the existing parent shell assembly and solver rather than creating a second linear-algebra owner.

Required limits:

1. constraints and nodal loads are supplied through an experimental execution request; do not rewrite `local-shell-model/v1`;
2. MITC element evidence supplies `globalStiffness` and `globalDofOrdering` to the generalized parent assembly;
3. pressure force vectors come only from `mitc-adoption-loads.js`;
4. nodal `[FX,FY,FZ,M1,M2]` mapping preserves the current shell global generalized-load convention;
5. dense/sparse selection, prescribed-DOF partition, Cholesky/PCG, reactions and equilibrium remain owned by the parent shell/shared-solve implementation;
6. no stress recovery/result-contract/UI work enters this batch.

### Handover snapshot

Repo: `reallaksh19/Advanced_Analysis`
Task: #1536 — MITC adoption package
Chain / endpoint: `ADV-LAFEA-1536-SHELL-PRODUCTION` / `EP-0008`
PR: #1542 Draft stacked on #1541
Branch / material head: `codex/lafea-1536-mitc-adoption` / `c589a61b…`
Merge authority: OWNER_ONLY
State: IN_PROGRESS / HELD / NOT_REQUIRED / WRITE_ALLOWED / AUTO RUNNING
Roadmap: `docs/conceptcumroadmapLAFEA.md@088f4ceb…`
Inputs: qualified-shape MITC element evidence + consistent pressure evidence + parent shell assembly/solver
Benchmarks: existing MITC checks; adoption element/pressure checks
Blocker: executable qualification NOT_RUN
Exact next action: add experimental global assembly/solve adapter and focused equilibrium/rigid-motion checks; no recovery/UI.

Q1: Trace MITC global element matrices and mixed TRI3/QUAD4 DOF identities into the parent shell assembly and free/constrained partition without topology inference.
Q2: Hand-reconstruct one pressure-loaded Q4 force vector, constrained/free indices, reaction balance and `Kq-F` sign convention.
Q3: Prove reuse of parent assembly/solver is numerical custody reuse, not MITC formulation or production-route promotion.
Q4: Independently verify rigid-motion zero-action and fully-fixed pressure force/moment reaction equilibrium; include a singular control.
Q5: Define the smallest solve adapter and NO-PATCH boundary before recovery/result-contract work.

### Takeover qualification pack

#### Q1 — Assembly production trace
Anchors: `mitc-adoption-element.js`, `assembly.js`, `solver.js`, `lafea-linear-solve/**`.
Required reconstruction: map each element's retained `globalDofOrdering` into canonical model node-order global indices, assemble 15x15 and/or 20x20 transformed matrices into one symmetric global system, then show the exact dense/sparse selection boundary inherited from parent #1541.
Fail if: topology is inferred from matrix size or a second MITC-specific global solver is introduced.

#### Q2 — Numerical partition/reaction reconstruction
For one planar MITC4 element with known pressure load and explicit restraints, enumerate global DOFs, constrained/free indices, prescribed values and effective RHS. Reconstruct reactions as `Kq-F` and demonstrate sign-consistent global force/moment balance.
Falsifier: any reaction sign or DOF-order change required only to make the example balance.

#### Q3 — Authority invariant
The parent shell assembly/solver owns storage, partition, Cholesky/PCG, residual and reaction mechanics. MITC owns element stiffness and load/recovery semantics only. Reusing the parent numerical owner does not make MITC registered, qualified for production, or UI-visible.
Invalid shortcut: copying PCG/partition code into an MITC module to avoid adapting the existing owner.

#### Q4 — Independent validation
Rigid-body fully prescribed MITC model with zero external load must produce zero `Kq` action/reactions within retained tolerance. Fully fixed pressure case must return reactions equal and opposite to the independently reconstructed applied force/moment. Under-constrained model must fail closed through the existing shell solver state rather than regularization or fallback.
Fail if: solver output is its own only oracle.

#### Q5 — Minimal patch
Safe boundary: experimental execution-request canonicalization + adapter composing existing element evidence, assembly, load evidence and solver. No recovery, stress invariant, result schema, stage composition, presenter, UI, roadmap or workflow mutation. Roll back if parent CST/DKT solver behavior changes, MITC needs a separate numerical recurrence, pressure evidence is recomputed differently, or fail-closed singular behavior is weakened. NO-PATCH if the generalized parent assembly cannot consume variable 15/20-DOF element evidence without altering existing CST/DKT semantics.
