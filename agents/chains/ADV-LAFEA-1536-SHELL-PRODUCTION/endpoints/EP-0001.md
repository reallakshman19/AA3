# EP-0001 — LAFEA.4 shell scalability pre-work

CHAIN_STATE_VERSION: 3
CHAIN_ID: ADV-LAFEA-1536-SHELL-PRODUCTION
ENDPOINT_ID: EP-0001
PARENT_ENDPOINT: NONE
CUSTODY_EPOCH: 1
HEAD_BASIS: 80dcfe8311b7cee367873fe2794ab059a242921b
COMMON_PROTOCOL: engineering-pr-delivery-v2
COMMON_PROTOCOL_BASIS: 9573ec2bba234cc7cd6abcf73c4c3f3e2bc0892c
COMMON_PROTOCOL_STATUS: CURRENT
ROADMAPS: docs/conceptcumroadmapLAFEA.md@088f4cebfd954e5d1e37da855c95142712463a31
ROADMAP_REVIEW_STATUS: COMPLETE
HANDOVER_READY: TRUE

## Mission

Start issue #1536 with the smallest formulation-independent production change: replace the LAFEA.4 dense-only global stiffness/solve bottleneck with a deterministic sparse production path while preserving the existing `CST_DKT_TRI3_THIN_SHELL_V1` formulation, loads, recovery, tolerances, registry authority, presenter semantics, and MITC nonproduction status.

## Owner-roadmap alignment

Classification: `ALIGNED`.

`docs/conceptcumroadmapLAFEA.md` requires exact-route qualification and identifies the active LAFEA.4 route as legacy CST+DKT with dense-solve scalability as a known limitation. This leg does not promote MITC, change shell kinematics, alter source/applicability authority, or grant release authority.

## Protected unchanged domains

- CST membrane and DKT bending formulation/equations.
- Five-DOF ordering `UX, UY, UZ, R1, R2`.
- Local/global basis and sign conventions.
- TRI3 pressure-load mechanics.
- Stress recovery and von-Mises semantics.
- Qualification tolerances and benchmark/oracle expected values.
- LAFEA.4 registry authority and UI/presenter wording.
- MITC3/MITC4 production authority remains absent.

## Implementation boundary

1. Reuse the governed `src/core/lafea-linear-solve/**` sparse matrix, BC elimination and sparse Cholesky owners; do not copy a second solver.
2. Add storage-aware assembly under `src/core/local-shell/assembly.js`, retaining dense storage for small systems and switching to sparse storage beyond an explicit deterministic DOF threshold.
3. Make `src/core/local-shell/solver.js` storage-aware while preserving prescribed-displacement, reaction, residual and equilibrium semantics.
4. Make global strain-energy reconstruction storage-aware without changing element recovery.
5. Add focused sparse/dense equivalence, automatic sparse-route, singular/fail-closed and deterministic checks.

## Validation plan

Focused: new LAFEA.4 sparse-shell check; existing `lafea.4-contract`, membrane, bending, cylindrical, pressure, solver-loads, stress-energy, determinism and orientation checks. Aggregate: `npm run check:lafea-core`, `npm run check:lafea-solver`, `npm run check:imports`, `npm run build`, `git diff --check`. Record unavailable execution as `NOT_RUN`, never PASS.

### Handover snapshot

Repo: `reallaksh19/Advanced_Analysis`
Task: #1536, Leg A shell scalability
Chain: `ADV-LAFEA-1536-SHELL-PRODUCTION`
Endpoint: `EP-0001`
PR: pending
PR status: not opened
Branch / PR head / main: `codex/lafea-1536-shell-scalability` / pending / `80dcfe83…`
Merge authority: OWNER_ONLY
Engineering / custody / qualification / write state: IN_PROGRESS / HELD / NOT_REQUIRED / WRITE_ALLOWED
Roadmap: `docs/conceptcumroadmapLAFEA.md@088f4ceb…`
Inputs: LAFEA.4 v1 CST/DKT route
Benchmarks: existing `lafea.4-*`; production sparse-route check to add
Governing docs / authoritative sources: issue #1536; exact Common protocol above
Current blocker: dense global K + dense Cholesky only
Exact next action: wire governed sparse matrix/BC/Cholesky owners into LAFEA.4 without changing formulation authority.

Q1: Trace `calculateLocalShell()` through element assembly, partition/solve, recovery and result evidence for `pressurePatchSource()`; identify every dense-matrix dependency.
Q2: For 308 shell nodes, compute DOF count and raw dense-double storage; identify the first allocation that creates the scaling failure.
Q3: Prove why changing storage/solver backend cannot alter CST/DKT formulation, load, recovery or MITC authority.
Q4: Reconstruct the `pressurePatchSource()` 2.5 MPa resultant (`2.5×100×50`) and state the sparse/dense invariants that must remain identical.
Q5: Define the minimal sparse patch, negative singular test, rollback falsifier and NO-PATCH condition.

### Takeover qualification pack

#### Q1 — Production trace
Repository anchors: `src/core/local-shell/calculate.js`, `assembly.js`, `solver.js`, `recovery.js`, `loads.js`, `scripts/lafea.4-pressure-check.mjs`, `scripts/lafea.4-fixtures.mjs` at `80dcfe83…`.
Production object/case: `pressurePatchSource('ALONG_ELEMENT_NORMAL')`, load case `PRESSURE`, elements `E1/E2`.
Required technical work: trace canonical model → CST/DKT element evidence → global K → pressure F → partition → Cholesky → reactions → energy/stress recovery.
Required numerical/technical evidence: identify where dense K is allocated, where free-free dense K is copied, and where `quadratic(u,K)` requires dense storage.
First authority/ownership boundaries: formulation in `element.js`; storage in `assembly.js`; solve in `solver.js`; recovery in `recovery.js`.
Fail if: MITC or UI is treated as part of the active numerical route.

#### Q2 — Scaling reconstruction
Repository anchors: `assembly.js`, `solver.js`.
Calculation/reconstruction: 308 nodes × 5 DOF/node = 1540 DOF; raw dense doubles = 1540²×8 = 18,972,800 bytes before JS array overhead and before free-free copies/factor storage.
Required numerical/technical evidence: distinguish global allocation from factorization cost.
Predicted first wrong boundary: `zeros(dofOrdering.length, dofOrdering.length)` in shell assembly.
Falsifier: evidence that production assembly is already sparse at this basis.
Fail if: only Cholesky is blamed while dense assembly is ignored.

#### Q3 — Authority invariant
Repository anchors: `AGENTS.md`, `element.js`, `loads.js`, `recovery.js`, `lafea-stage-registry.js`.
Required technical work: separate numerical storage/solve ownership from formulation/load/recovery/publication ownership.
Authority/source trace: current registry authority is `CST_DKT_TRI3_THIN_SHELL_V1`; MITC remains nonproduction.
Protected invariant: changing K storage/factorization must not change stiffness equations, load vector, recovered stress definitions, sign/basis conventions or registry authority.
First wrong boundary: any patch that edits those domains merely to make sparse tests pass.
Falsifier: dense/sparse disagreement exceeding existing qualification tolerances.
Invalid shortcut: wire MITC or loosen tolerance to obtain a convenient SPD/sparse result.
Fail if: solver PASS is claimed as formulation or release authority.

#### Q4 — Independent validation
Repository anchors: `scripts/lafea.4-pressure-check.mjs`, fixture `pressurePatchSource()`.
Required technical work: independently reconstruct pressure resultant and dense/sparse response parity.
Independent oracle: pressure × represented area. Two TRI3 facets cover 100×50 = 5000 mm²; at 2.5 MPa = 2.5 N/mm², total +Z force = 12,500 N. Sense reversal must produce −12,500 N with moment sign reversal.
Required numerical/technical evidence: same load vector, displacement, reactions, equilibrium residuals, energies and recovered stresses for dense and sparse backends within existing tolerances.
Units/sign/tolerance: N, mm, MPa; use repository qualification profile, do not invent looser tolerances.
Falsifier: any backend-dependent engineering result outside the current tolerance.
Fail if: sparse output is used as its own oracle without dense/closed-form comparison.

#### Q5 — Minimal patch
Repository anchors: `assembly.js`, `solver.js`, `recovery.js`, governed `src/core/lafea-linear-solve/**`.
Required technical work: identify the smallest owner seams for sparse storage, partition/solve and stiffness action.
Safe patch boundary: reuse generic sparse matrix + BC elimination + sparse Cholesky; keep dense path for small systems; storage-aware reaction/energy action.
Expected before/after evidence: small cases remain dense and numerically unchanged; >threshold production case retains sparse K and solves through sparse backend.
Protected unchanged domains: formulation, loads, recovery definitions, tolerances, MITC/registry/UI authority.
Validation required: dense/sparse equivalence + auto sparse-route + singular/fail-closed + existing LAFEA.4 regression aggregate.
Negative test: under-constrained sparse model must remain `SINGULAR_SYSTEM`/fail closed.
Rollback/falsifier boundary: any regression or dense/sparse discrepancy outside existing tolerances.
No-patch condition: generic sparse owner cannot preserve current prescribed-displacement/reaction/equilibrium semantics without changing protected engineering authority.
Fail if: a duplicate solver is introduced or tolerance/oracle authority is modified.
