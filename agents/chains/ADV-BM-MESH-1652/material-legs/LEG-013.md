# LEG-013 — register BM-MESH in the benchmark validation program

STATUS: COMPLETE_TASK_006_REGISTERED
DATE_UTC: 2026-09-06
CHAIN_ID: ADV-BM-MESH-1652
WORK_ITEM_KEY: github:reallaksh19/Advanced_Analysis#1652
PREWORK_ENDPOINT: EP-0023
OWNER_PROGRESSION_COMMAND: PROCEED_NEXT
QUESTION_POLICY: NO_NEW_QUESTIONS_OWNER_INSTRUCTION

MATERIAL_BASELINE: c2b53fe43e2c701281177e45f0c7a4fc326547da
MATERIAL_HEAD: 474eb8b633e2afabf8a4773cfb5e17bd8a44b0ae
LIVE_MAIN_HEAD_OBSERVED: f8d051c989c8a0627db7560f996baf72987775d4

## Material delta

Exactly one TASK-006 file changed:

- `validation/lafea-benchmark-program/program.json`

No production `src/**`, threshold, solver/compiler, workflow, roadmap, M3/M4 fixture, release, temperature, or merge authority changed.

## Registration behavior

LEG-013 semantically rebases the branch registry onto live main's benchmark-program state before adding BM-MESH:

- preserves existing B01 unchanged in meaning;
- preserves live-main B02 registration and `activeCaseId: B02`;
- preserves the B03-B06 future queue and its activation relationships;
- appends registered case `BM-MESH` with `definitionState: READY`;
- binds its one program method to `node scripts/lafea-mesh-benchmark-run.mjs --stage M4`;
- hash-binds the staged runner, bucket manifest, source registry, frozen geometry/oracle, mesh ladders, fixed probes, and exact-code negative definitions through `sourceRefs`;
- declares expected runner summary schema `lafea-mesh-benchmark-program-run/v1`;
- retains program release and temperature authority as false.

BM-MESH registration does not make it the active benchmark case and does not reorder B02/B03 sequencing.

## Validation

PASS_STATIC:

- candidate `program.json` parsed as JSON before write;
- compare `c2b53fe43e2c701281177e45f0c7a4fc326547da...474eb8b633e2afabf8a4773cfb5e17bd8a44b0ae` is one commit and exactly one changed file, `validation/lafea-benchmark-program/program.json`;
- committed registry retains `activeCaseId: B02`, B01, B02, and the B03-B06 future queue while adding BM-MESH;
- evidence policy still states `releaseAuthorityGrantedByProgram: false` and `temperatureAuthorityGrantedByProgram: false`.

NOT_RUN:

- `BM-MESH` through the outer benchmark-program runner;
- live M0-M4 benchmark execution;
- M3 shell h/t acceptance;
- M4 mesh-driven solver convergence.

A live BM-MESH PASS is not inferred from registration. The full BM-MESH method is expected to remain fail-closed until the already-declared M3/M4 authority blockers are resolved.
