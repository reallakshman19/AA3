# EP-0010 — expanded analytical oracle coverage + merge tooling blocker checkpoint

CHAIN: `ADV-LAFEA12-ANALYTICAL-QUALIFICATION`  
ISSUE: #1533  
PR: #1545  
BASE: `b6a0075fe004a634b8e2877bd8a95b6901c1d763`  
PRE-WORK ENDPOINT: `EP-0009`  
MATERIAL HEAD BEFORE ENDPOINT: `22d97c8e8caf45e5bf84400eabeceb8904b7c689`

## Owner merge instruction and tooling disposition

Owner explicitly authorized **merge and proceed**.

Pre-merge live checks at head `bbafe60a23ea5769fa5f82e30574b989ecb4ff1a` showed:

- PR mergeable: true;
- branch ahead-only from exact base;
- no submitted reviews;
- no review threads;
- no commit status contexts;
- numerical analytical qualification still `NOT_RUN_INFRA` because the dedicated runner reported zero steps.

The GitHub Ready-for-review mutation failed in connector tooling because it queried nonexistent GraphQL field `Repository.fullDatabaseId`. An exact-head squash merge was then attempted directly and GitHub rejected it solely with HTTP 405 `Pull Request is still a draft`. The merge therefore remains **TOOLING_BLOCKED**, not engineering-rejected. No merge occurred.

## Next work performed instead of idling

Issue #1533 was re-audited against its required actions. The initial seven benchmark IDs did not by themselves exercise every mandated independent subcase. The frozen oracle ledger was therefore expanded without touching production numerical mechanics.

### Expanded frozen oracle expectations

`ORACLE_BENCHMARKS.json` now retains:

- `LAFEA1-RLT-01`: translated, additionally rotated, and load-scaled covariance variants plus the sign falsifier;
- `LAFEA1-LAME-01`: inner / mid-wall / outer Lamé stress values;
- `LAFEA1-END-01`: closed, open, and explicit-axial-resultant semantics;
- `LAFEA2-SEC-01`: exact annulus properties plus outer elastic section modulus;
- `LAFEA2-COMB-01`: principal stresses, outer/mid/inner radial states, pure axial, pure My, pure Mz, and pure torsion subcases;
- `LAFEA2-ENV-01`: load scaling, complete reversal, and split-term superposition invariants;
- `LAFEA1-LAFEA2-HANDOFF-NEG`: retained fail-closed pressure-thrust provenance behavior.

### Extended one-way comparator

Added `scripts/lafea.12-independent-oracle-extended-check.mjs`.

It observes live LAFEA.1/.2 outputs against the frozen independent expectations for:

1. RLT translation/rotation/load scaling;
2. Lamé mid-wall and all three end-condition semantics;
3. exact section modulus reconstruction;
4. independent pure axial / My / Mz / torsion cases;
5. combined outer/mid/inner stress tensors and principal stresses;
6. full-tensor ×2 scaling;
7. complete tensor reversal with invariant von Mises;
8. 0.4 + 0.6 mechanical-term superposition back to the base tensor.

The script does not generate or rewrite expected oracle values.

### Qualification-path binding

`lafea.12-analytical-qualification-check.mjs` now requires the original independent comparator **and** the extended comparator before analytical result-authority checks.

`lafea.12-analytical-qualification-registration-check.mjs` now statically requires the extended comparator and verifies the expanded variant/subcase structures remain in the frozen ledger.

`.github/workflows/lafea12-analytical-oracle.yml` now includes `node --check scripts/lafea.12-independent-oracle-extended-check.mjs` in its syntax gate.

## Validation truth

| Gate | State | Evidence |
|---|---|---|
| Exact branch topology after continuation | PASS_STATIC | branch remains ahead-only; 29 commits ahead, 0 behind |
| Production LAFEA.1/.2 mechanics touched | NONE | compare still contains no `src/core/local-stress/**` or `src/core/local-attachment-screening/**` changes |
| Expanded oracle expectation custody | PASS_STATIC | all required variants/subcases are frozen in the independent ledger |
| Extended comparator attached to aggregate | PASS_STATIC | aggregate and registration guard require it |
| Workflow syntax-gate attachment | PASS_STATIC | dedicated workflow now `node --check`s extended comparator |
| Exact-head analytical workflow | NOT_RUN_INFRA | latest analytical job `99121991800` again reports `steps: []` |
| Numerical external-oracle agreement | NOT_RUN | no executable runner evidence |
| Owner merge authorization | AUTHORIZED | user instruction `merge proceed next` |
| Actual merge | TOOLING_BLOCKED | Ready mutation connector failure; direct merge rejected because PR remains Draft |

## Authority boundary

No LAFEA.3+ FEA, WRC/SCF, weld/fatigue/code, local-attachment discontinuity, or release-state authority is added. Existing stage `REGISTERED_MANIFESTS` claims remain unchanged because executable oracle qualification is absent.

## Next bounded leg — pre-authorized

1. keep PR #1545 merge-ready in content while Draft→Ready tooling is unavailable;
2. do not create a false PASS from zero-step runner failures;
3. if a qualified runner begins executing, run the complete analytical aggregate and inspect the first actual numerical disagreement before touching mechanics;
4. if Draft→Ready mutation becomes available, re-check exact head/reviews/threads/statuses and perform the owner-authorized exact-head squash merge;
5. after merge, branch the successor from the actual merged main SHA and continue only the still-unresolved #1533 qualification/release disposition.

## Successor expert questions

**Q1.** Why does the RLT translated variant need to preserve target moment exactly even though both absolute reference-point coordinates change?

**Q2.** For the rotated RLT variant, which quantities rotate covariantly and which scalar/vector residuals must remain invariant?

**Q3.** Why must a full ×2 envelope scaling test multiply both mechanical resultants and pressure factor, rather than only the mechanical load-case factor?

**Q4.** Under complete reversal of the full stress tensor, why do all tensor components change sign while 3-D von Mises remains unchanged?

**Q5.** What exact evidence would justify the first production mechanics modification after the oracle aggregate finally executes, and what evidence is insufficient?