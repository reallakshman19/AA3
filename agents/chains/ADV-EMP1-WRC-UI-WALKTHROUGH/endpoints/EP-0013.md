# EP-0013 — PRE_MUTATION: WRC route reauthorization invalidates stale suspension browser expectation

CHAIN_ID: ADV-EMP1-WRC-UI-WALKTHROUGH
LEG_ID: LEG-005
PREVIOUS_ENDPOINT: EP-0012
REASON: PRE_MUTATION
ISSUE: #1559
PR: #1565 DRAFT
BRANCH: codex/emp1-wrc-ui-1559
PRE_MUTATION_HEAD: 4c97a30d21457a87b83b1f504fa0b86c7516cecb
BASE_MAIN: 0676f6b145dad164869d2979f69b4a4491e8d803
COMMON_PROTOCOL_BASIS: 10d667ce715bb52e1f73035c6fa326db77d0f9dd

## Demonstrated defect

The dedicated browser regression still calls the bounded WRC route "live suspended" and expects the qualification sample to render `LOCAL METHOD BLOCKED`, `LOCAL RESULT NOT CALCULATED`, a route-authority blocker notice, no C result evidence, and a disabled C-run control.

That expectation is stale against current production authority:

- `EMP1_WRC537_GAMMA5_ZERO_DP_ROUTE_AUTHORIZED === true`;
- `EMP1_WRC537_GAMMA5_ZERO_DP_ROUTE_SUSPENSION_REASONS` is empty;
- the bounded route registry entry is `registered: true`;
- `engineeringUseAuthorized: true`;
- registry `suspensionReasons` is empty;
- `loadEmp1QualificationSample()` imports A/B/C source binding and then calls the real `runEmp1Product()` transaction.

Therefore the qualification sample should now exercise the current authorized bounded route, not a suspended route.

## First wrong boundary

`e2e/emp1-professional-workflow.spec.js` test `live WRC suspension is explained as route authority, not missing geometry`.

The presentation classifier remains useful fail-closed protection for future authority loss; the defect is the browser test and narrative claiming suspension is the current production state.

## Minimal hypothesis

Update the browser journey to assert current qualified-route behavior after the complete qualification sample:

- SOURCE CURRENT;
- TRANSFER CURRENT;
- SCREENING CURRENT;
- LOCAL METHOD QUALIFIED;
- LOCAL RESULT CURRENT;
- RELEASE PROFILE NOT QUALIFIED;
- CODE COMPLIANCE NOT ASSESSED;
- no stale/blocked currentness notice;
- current C result evidence visible;
- C rerun control enabled.

Retain synthetic/fail-closed authority-suspension classification in the focused Node checker, but stop labeling that synthetic reason as the current live route state.

## Prediction / falsifier

Prediction: production source trace and current route authority make the qualification sample a current bounded WRC result.

Falsifier: if the current route authority or sample transaction still produces a suspended/blocked C state at this exact head, do not change the browser expectation; instead record the exact blocker.

## Expected material files

- `e2e/emp1-professional-workflow.spec.js`
- `scripts/emp1-professional-workflow-check.mjs` only for terminology/fixture naming if required

## Protected files

No changes permitted in this leg to:

- `src/core/emp1/**`
- WRC coefficient/numerical/source/oracle/tolerance authority
- bounded-route registry/authorization
- `runEmp1(...)` or workbench execution mechanics
- code-compliance/release/deployment authority
- `.github/workflows/**`

## Validation truth at entry

- current main/head grounding: PASS_STATIC
- full checkout: NOT_RUN_DNS_BLOCKED
- browser Playwright: NOT_RUN
- route-authority production trace: PASS_STATIC

## Exact next action

Patch the stale browser expectation only, then run syntax/source-contract checks. Keep #1565 Draft.