# EP-0036 — post-merge execution Q1–Q5 answered from live repository; exact journey still environment-blocked

CHAIN_STATE_VERSION: 3
CHAIN_ID: ADV-EMP1-WRC-UI-WALKTHROUGH
ENDPOINT_ID: EP-0036
PREVIOUS_ENDPOINT: agents/chains/ADV-EMP1-WRC-UI-WALKTHROUGH/endpoints/EP-0035.md
ENDPOINT_KIND: QUALIFICATION_RESULT
PR: #1610 DRAFT
BRANCH: codex/emp1-wrc-ui-1559-postmerge-execution
LIVE_MAIN_OBSERVED: 2bade51fc8207db4cc7934de3b8cb6771e41e9e8
SOURCE_PR: #1604 MERGED
RECOVERY_PR: #1607 MERGED
MERGE_COMMIT: 2bade51fc8207db4cc7934de3b8cb6771e41e9e8
QUALIFICATION_STATE: ACCEPTANCE_EXECUTION_Q1_Q5_PASS_LIVE_REPOSITORY
TAKEOVER_AUTHORITY: ACCEPTANCE_EXECUTION_ONLY
WRITE_AUTHORITY: PRE_MUTATION_ENDPOINT_REQUIRED_BEFORE_ANY MATERIAL SOURCE CHANGE
ISSUE_1559_GITHUB_STATE: CLOSED_COMPLETED
ISSUE_1559_TECHNICAL_ACCEPTANCE: NOT_PROVEN
HANDOVER_READY: TRUE

## Q1 — exact executable production/browser trace — PASS

The current checked-in execution chain is:

1. `node scripts/run-playwright.mjs e2e/emp1-professional-walkthrough-evidence.spec.js --workers=1` starts `scripts/run-playwright.mjs`.
2. `scripts/run-playwright.mjs` does **not** use a global runner. It resolves `${process.cwd()}/node_modules/playwright/cli.js`, invokes `node <that-cli> test ...`, sets `PLAYWRIGHT_BROWSERS_PATH=0`, and has no fallback. Required package boundary: project-local `playwright`.
3. `playwright.config.js` imports `defineConfig` and `devices` from project-local `@playwright/test`, selects project `chromium`, and starts the checked-in dev server with `npm run dev -- --port 5173` (or `PLAYWRIGHT_SERVER_PORT`). Required package boundaries: `@playwright/test`, `playwright`, and Vite.
4. `package-lock.json` binds the repository dev-runtime declarations to `@playwright/test ^1.59.1`, `playwright ^1.59.1`, and `vite ^7.3.1`. The browser test therefore requires the locked project dependency graph, not only a system browser binary.
5. The target spec imports `{ expect, test }` from `@playwright/test`, navigates to `/e2e/fixtures/lafea-guided-workbench.html`, and that checked-in fixture supplies the real host `#lafea-guided-browser-host`.
6. `mountEmp1Workbench()` dynamically imports `/src/workspace/lafea-workbench-controller.js`, constructs `new LafeaWorkbenchController(root, { analyticalOnly: true })`, and calls `controller.init()`.
7. `LafeaWorkbenchController.init()` wires the real view callbacks, including `onLoadEmp1QualificationSample` and `onRunEmp1`.
8. The spec clicks the visible `emp1-load-complete-qualification-sample` control. `loadEmp1QualificationSample()` imports the checked-in qualification sample, imports A and B documents, applies typed EMP.1 run input, selects LAFEA.2 and calls the real `runEmp1Product()`.
9. `runEmp1Product()` first evaluates `projectEmp1WorkbenchRunReadiness(...)`; when ready it dynamically imports `executeEmp1WorkbenchProduct(...)` from `emp1-workbench-product-run.js`.
10. `executeEmp1WorkbenchProduct(...)` builds the governed A/B/C adapters and calls core `runEmp1(...)` imported from `src/core/emp1/index.js`. `runEmp1(...)` remains the engineering transaction authority. The workbench product layer explicitly returns `globalEmp1CRouteAuthority: false`, `codeComplianceProduced: false`, and `releaseQualified: false`.
11. The controller retains the execution; the real view/currentness/presentation path projects the WRC workflow. The test then drives the seven professional tasks and the visible upstream geometry edit.
12. At each of 12 checkpoints the exact spec attaches one viewport PNG, one full-page PNG, and one workbench ARIA snapshot. It finally attaches manifest schema `emp1-wrc-ui-walkthrough-evidence/v3`.

First checked-in/runtime boundary before checkpoint 01 is therefore:

```text
repository source tree
  -> node_modules/playwright/cli.js
  -> @playwright/test config/runtime
  -> Vite dev server
  -> fixture HTML
  -> LafeaWorkbenchController module graph
  -> initialized real workbench
  -> checkpoint 01
```

A system Chromium binary alone cannot satisfy this chain.

## Q2 — current unresolved problem / first-wrong-boundary isolation — PASS

The current environment fails **before repository runner startup**:

```text
faithful repository checkout = NOT AVAILABLE
git ls-remote github.com = DNS BLOCKED
project-local node_modules/playwright = NOT AVAILABLE
project-local @playwright/test = NOT AVAILABLE
project-local Vite = NOT AVAILABLE
npm registry recovery = EAI_AGAIN / DNS BLOCKED
```

System Chromium and Python Playwright were independently smoke-executed successfully in EP-0034, so browser installation is not the first wrong boundary.

Current first wrong boundary is **repository/project-runtime materialization**, before:

- repository Playwright CLI startup;
- config loading;
- Vite web-server startup;
- fixture navigation;
- `lafea-workbench-controller.js` module import;
- workbench initialization;
- checkpoint 01.

Disposition remains:

```text
EXACT_WRC_PLAYWRIGHT_SPEC = NOT_RUN_ENVIRONMENT_PREREQUISITES
APPLICATION_WRC_JOURNEY_REACHED = FALSE
WRC_UI_RESULT = NOT_EVALUATED
```

Do not convert that infrastructure state into a WRC UI FAIL.

## Q3 — authority/invariant — PASS

None of the following can substitute for technical #1559 acceptance:

- a Python reimplementation of the journey;
- a reduced/mock DOM;
- a manually reconstructed partial source tree;
- direct invocation of presentation helpers instead of the real controller path;
- stale retained C numerical evidence;
- GitHub issue administrative closure.

Reason: #1559 acceptance is specifically about the real engineer-facing transition path and its visible/currentness/accessibility behavior. The governed spec binds that path to the real repository runner, Vite-served fixture, production controller, real EMP.1 transaction and exact 12-checkpoint evidence contract. A substitute can at most provide diagnostic evidence for one layer; it cannot prove the integrated sequence or create WRC numerical, route, code-compliance, release or UI-acceptance authority.

Stale retained C remains historical evidence only. `runEmp1(...)`, route/currentness contracts and `projectEmp1WorkbenchCState(...)` remain separate from acceptance evidence. Administrative issue state is not an engineering result.

## Q4 — independent validation / executed layers — PASS WITH NOT_RUN PRESERVED

Actually executed in this custody:

```text
Node runtime availability = PASS
system Chromium launch = PASS_EXECUTION_SMOKE
Python Playwright browser smoke = PASS_EXECUTION_SMOKE
live repository source trace through GitHub connector = PASS_STATIC_LIVE_REPOSITORY
```

Not executed because prerequisites are unavailable:

```text
project-local Playwright CLI startup = NOT_RUN
playwright.config.js runtime loading = NOT_RUN
Vite dev-server startup = NOT_RUN
fixture load = NOT_RUN
LafeaWorkbenchController browser initialization = NOT_RUN
checkpoint 01 = NOT_RUN
12 viewport attachments = NOT_RUN
12 full-page attachments = NOT_RUN
12 ARIA snapshots = NOT_RUN
manifest v3 generation = NOT_RUN
qualified live/recorded human observation = NOT_RUN
```

The current evidence therefore proves only environment/browser capability and the checked-in execution architecture. It does not prove the actual WRC walkthrough.

## Q5 — next contribution / minimal-patch decision — PASS

No application defect is presently observable because the exact journey has not reached the application. Therefore:

```text
PRODUCT_PATCH = NOT_AUTHORIZED
PRE_MUTATION_PRODUCT_ENDPOINT = NOT_JUSTIFIED_YET
```

The smallest valid next contribution is **execution only**:

```bash
node scripts/run-playwright.mjs e2e/emp1-professional-walkthrough-evidence.spec.js --workers=1
```

from a faithful then-current checkout with locked project dependencies.

If that run reaches the real application and exposes a concrete hierarchy/navigation/accessibility/currentness defect:

1. retain the first failing checkpoint/transition and artifact identity;
2. create a new PRE_MUTATION endpoint;
3. identify the first wrong presentation/controller/evidence boundary;
4. patch only that boundary;
5. do not touch `src/core/emp1/**`, WRC equation/coefficient/source/oracle/tolerance authority, bounded-route registry/authorization/currentness mechanics, semantic-hash authority, code-compliance authority, release authority, or the eight-point result-domain limitation unless a separately authorized engineering issue requires it.

## Qualification verdict

`ACCEPTANCE_EXECUTION_Q1_Q5 = PASS_LIVE_REPOSITORY`

This authorizes **acceptance execution only**. It does not authorize a production patch and does not establish technical #1559 acceptance.

## Exact next action

On an environment with a faithful checkout of then-current `main` and project-local dependencies:

```bash
node scripts/run-playwright.mjs e2e/emp1-professional-walkthrough-evidence.spec.js --workers=1
```

Then verify 12 viewport PNGs + 12 full-page PNGs + 12 ARIA snapshots + manifest v3 and complete qualified pressure-vessel/local-stress live observation or review of an actual recording. Playwright/static evidence alone remains insufficient for final PASS.
