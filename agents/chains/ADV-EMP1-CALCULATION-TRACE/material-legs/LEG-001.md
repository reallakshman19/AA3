# LEG-001 — Calculation Trace Projection Material Receipt

CHAIN_ID: ADV-EMP1-CALCULATION-TRACE
LEG_ID: LEG-001
BASE_MAIN: 694088625c8cfdbd357b5c43fc1dbdb12f4f6800
MATERIAL_HEAD: 0cddcf804e73f949d62e447c8822902dc8def131

## Material scope

- `src/core/emp1/emp1-calculation-trace-projection.js` — added, 201 lines.
- `src/core/emp1/index.js` — one-line export of the projection API.
- `scripts/emp1-calculation-trace-projection-check.mjs` — added focused deterministic guard.

No WRC authority-owner, source, applicability, registry, route, numerical, oracle/tolerance, release, deployment, or workflow file changed.

## Projection behavior

The projection consumes retained values supplied by the existing transaction/currentness owners:

- source hash;
- `runEmp1()` retained A/B/C result objects;
- immutable `emp1-assessment/v1.parents` ancestry;
- workbench invocation counts;
- retained execution route-authority hash;
- existing execution/C currentness projections.

It reports compact A/B/C layer identities and compares already-retained layer/source hashes with the assessment's already-retained parent hashes. Comparison states are `MATCH`, `MISMATCH`, `INCOMPLETE`, or `NOT_RETAINED`; aggregate consistency is explicitly a custody/trace statement, not engineering PASS, code compliance, or release authority.

## Authority boundary

The module has zero imports and does not call or reproduce:

- `runEmp1()`;
- `semanticHash()`;
- result-hash reconstruction;
- WRC geometry/load transformation;
- WRC applicability evaluation;
- WRC stress numerics;
- method/source/release qualification.

All authority-creation flags in the projection are false.

## Code-quality review

PRE: existing A/B/C execution, assessment ancestry, route authority, currentness and detailed engineering evidence already have separate owners. A small pure projection is the correct boundary; no second calculation engine is needed.

POST: production module is 201 physical lines, below the 300-line review threshold. Helper functions are responsibility-bounded. No hidden singleton/global authority is introduced.

## Staged prerequisite disposition

The Owner explicitly selected the `Calculation Trace Projection` phase and directed `proceed`. Draft PR #1617 concurrently owns `emp1-professional-result-presentation.js`; forcing trace rendering there would create unnecessary active-PR overlap. This leg therefore exposes the projection through the established `src/core/emp1/index.js` API and defers engineer-facing rendering to a later presentation slice after dependency disposition.

## Validation truth

`node scripts/emp1-calculation-trace-projection-check.mjs` is encoded but **NOT_RUN** in this connector session. No PASS is claimed.
