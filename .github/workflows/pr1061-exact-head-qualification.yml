name: PR1061 Exact-Head Support Placement Qualification

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pr1061-exact-head-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  qualify-support-placement:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      TARGET_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
      PLAYWRIGHT_BROWSER_CHANNEL: chromium

    steps:
      - name: Checkout exact head
        uses: actions/checkout@v4
        with:
          ref: ${{ env.TARGET_HEAD_SHA }}
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies and Chromium
        run: |
          set -euo pipefail
          npm ci
          npx playwright install --with-deps chromium

      - name: Verify exact head and qualification sources
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$TARGET_HEAD_SHA"
          node --check tests/topology-edit-support-placement-command.test.mjs
          node --check tests/topology-edit-table-support-placement.test.mjs
          node --check tests/topology-edit-support-placement-writeback.test.mjs
          node --check e2e/topology-edit-table-support-placement.spec.js
          git diff --check

      - name: Execute focused support-placement Node qualification
        run: |
          set -euo pipefail
          node --test \
            tests/topology-edit-support-placement-command.test.mjs \
            tests/topology-edit-table-support-placement.test.mjs \
            tests/topology-edit-support-placement-writeback.test.mjs

      - name: Execute production Chromium support-placement lifecycle
        run: |
          set -euo pipefail
          npx playwright test \
            e2e/topology-edit-table-support-placement.spec.js \
            --project=chromium --workers=1 --retries=0 --trace=on --reporter=line

      - name: Upload qualification evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pr1061-support-placement-${{ env.TARGET_HEAD_SHA }}-${{ github.run_attempt }}
          path: |
            playwright-report
            test-results
          if-no-files-found: warn
          retention-days: 30
